# Cargo
target/
debug/
**/*.rs.bk
*.pdb

# Cargo.lock は bin を含むのでコミットする (実装計画書 6 章)

# 中間キャッシュ (設計書 6.15)
.pxcache/

# 環境
.env
.DS_Store

# 校正の評価データセット (px-calib gen で作り直せる)．
# **先頭の / が要る** — 無いと testdata/grid-eval/ (実データ枠．こちらは追跡する) まで
# 巻き込む
/grid-eval/

# 実データ枠の検証結果 (px-calib real / diagnose で作り直せる)
testdata/grid-eval/real/results.csv
testdata/grid-eval/real/diagnose.csv

# lint の測定結果 (px-calib lint で作り直せる)
testdata/grid-eval/seeds/lint.csv
testdata/lint-cases/negative/lint.csv
testdata/grid-eval/seeds/jaggy.csv
testdata/lint-cases/negative/jaggy.csv

# 再配布できない実データ (game-materials.com 等)．入手元は SOURCES.md に記録する
testdata/grid-eval/real/local/
