# 引き継ぎ

セッションをまたぐときの出発点．**まずこれを読み，次に `docs/status.md` を読む．**

- 更新: 2026-08-15 (**M0 〜 M7 完了．付録 B ・付録 C は全件閉じた．
  R22 は D170 で閉じ，M2 の区分 B は D173 で目標を超えた (42 / 50)．
  次は crates.io の «公開名» を決めるところから** — D174 を読むこと)
- 仕様: Obsidian の `設計書/ドット絵CLI-Rust/` — `設計書.md` / `実装計画書.md` / `開発ノート.md`
- **`開発ノート.md` に「実装して初めて分かったこと」が全部入っている．** 設計書だけ読むと
  同じ穴に落ちる

> [!note] **M6 は本物の API で通った (2026-08-15．D162)．**
> `px gen prog` が**1 発で通った** — blocking 0 ・advisory 2 ・**80.4 秒**．
> 送る本文は受理され，応答から L0 を取り出せ，作り直しの輪は 1 度も回らなかった．
>
> **通す過程で 1 件出た．しかもこれが «叩いたことはある» と «通ったことがある»
> の差そのものだった** — **一括で受けると 60 秒で接続が畳まれる**ので，
> 直す前の道具では 16x16 の宝箱 1 枚ですら**構造的に 1 度も通らなかった**
> (実測した 1 発は 80.4 秒．ちょうど崖の向こう側にある) ．`stream: true` に
> 書き直して直した．**上限を上げても作り直しても直らない種類の誤り**である．
>
> > [!warning] **«1 回で通った» は «輪を実測した» ではない．**
> > 2 回目以降 ・助言の効き方 ・`Truncated` ・`Refused` は，**いまも偽
> > バックエンドの試験でしか通っていない**．次に M6 を触るならここ．
>
> ```sh
> # **非対話シェルには .zshrc の export が届かない**ので明示的に渡す
> KEY=$(sed -n '162p' ~/.zshrc | sed 's/^[^=]*=//' | tr -d '"'"'"' \r')
> ANTHROPIC_API_KEY="$KEY" cargo run -p px -- gen prog out/chest.px.toml \
>     --prompt "木の宝箱．正面から" \
>     --palette 1a1c2c,566c86,8a6a4a,b13e53,f4f4f4 --size 16x16
> # 鍵を使わずに «送る側» を読む (組み立てた依頼がそのまま出る)
> cargo run -p px -- gen prog out/x.px.toml --prompt "宝箱" \
>     --palette 1a1c2c,f4f4f4 --size 8x8 --dry-run
> ```

> [!note] **付録 B は 4 件すべて閉じた** (D155 ・D156)．
> ライセンスは `MIT OR Apache-2.0`，公開は当面 GitHub のみ，生成 AI は
> **LLM のみ ・外部 API**，出力の主軸は **L0 テキスト形式**．

## いまの状態

| 項目 | 値 |
| --- | --- |
| フェーズ | M0 ・M1 ・M1a ・**M2 完了** (**B は D173 で目標を超えた** — 42 / 50) ・**M3 完了**．**M4 完了 (20 / 20)** — **`px compose`** (D93 〜 D95) ・**`px direction`** (D96 〜 D98) ・**`px tileset extract`** (D99 〜 D101) ・**`px tileset autotile`** (D102 〜 D104) ・**ディザの位相** (D105 〜 D107) ・**象限インポータ** (D108 ・D109) ・**正規 JSON の統合** (D110) ・**`px export tiled`** (D111) ・**`px sheet pack`** (D112 ・D113) ・**`px anim tween`** (D114 ・D115) ・**`px anim ease`** (D116 ・D117) ・**`px anim cycle`** (D118 ・D119) ・**`px anim smear`** (D120 ・D121) ・**`px anim extrapolate`** (D122) ・**`px anim squash`** (D123 ・D128) ・**`px anim subpixel`** (D124 ・D125) ・**`px anim afterimage`** (D126) ・**書く側も拡張子を見る** (D127) ・**`px atmos`** (D135 〜 D138) ・**フレーム間 lint 6 ルール** (D139 〜 D142) ．**M5 完了** — レシピ ・式評価器 ・依存グラフ ・ステップキー ・キャッシュ ・`recipe expand` ・決定論性試験 ・生成過程 GIF (D129 〜 D134) ．**M4 は閉じた** (D139 〜 D142) ．**M7 完了 (8 / 8)** — lint の 19 ・20 ・21 で**7 章の 27 ルールが揃い** (D143 〜 D145) ，**`px scale` / `px rotate` で R17 を閉じた** (D146 〜 D148) ．**`px project`** (D149 〜 D151) ・**`px guide`** (D152) ・**`px view --onion`** (D153) ・**`px palette report`** (D154) ．**M6 は 3 件済 ・1 件は書かない** — `px-gen` (D155 〜 D158) ．**本物の API で通した** (D159 〜 D162) |
| テスト | **948 件**すべて通過 |
| 品質 | `cargo fmt --all --check` と `cargo clippy --workspace --all-targets -- -D warnings` がクリーン |
| クレート | `px-core` / `px-io` / `px-view` / `px-macro` / `px-lint` / `px-recipe` / **`px-gen`** / `px` / `px-calib` |
| ブランチ | `main` |
| コミット | **D162 〜 D174 はすべてコミット済み．作業ツリーはきれい** (最後のコミットは `git log -1` で見る — 番号をここに書くと自分を追いかける) |

> [!warning] 2026-08-15 の変更 — **crates.io の準備．名前が取られていただけでは済まなかった** (D174)
> **まだ出していない** (利用者判断) ．準備を整え `cargo publish --dry-run` を回した．
>
> **`px-core` と `px` は取られている** (px-core は 0.3.1．**`0.1.4` / `0.1.6` /
> `0.1.7` もある**) ．`pxforge` と `pxforge-*` は全部空き．出すときは
> **`[package] name` だけ替え，`[lib] name` を `px_core` のまま残せばソースは
> 1 行も変わらない**．
>
> > [!warning] **名前の衝突は表示の問題ではなかった．** 梱包した tarball を
> > 検証するとき path 依存は登録所の依存に置き換わるので，`version = "0.1.0"`
> > が**他人の px-core 0.1.7** に当たる．**`px-io` は 23 件 ・`px-lint` は
> > 58 件のコンパイルエラー**で落ちた．
>
> > [!warning] **`px-recipe` が通ったのが一番悪い** — px-core を**一度も使って
> > いない**ので，他人のクレートへの依存を宣言したまま無言で公開されるところだった．
> > **`px` も `px-macro` を使っていなかった**．どちらも落とした．
>
> 整えたもの: `description` (必須) ・`keywords` ・`categories` ・`readme` ・
> **path 依存の `version`**．**`publish = false` は残してある．**

> [!note] 2026-08-15 の変更 — **ルール 9 を厳密な升判定へ替えた．D37 を改訂した** (D172)
> D164 が «測って止めた» 設計判断を，**替える前に上限を測って**決めた
> (`px-calib mixel-exact`) ．
>
> | 窓 16 | 統計的推定器 | 厳密判定 |
> | --- | ---: | ---: |
> | **書籍のミクセル** | **0 / 36** | **11 / 36** |
> | 同じ絵の清書 (鳴るな) | 0 / 18 | **0 / 18** |
> | 実素材 64 枚 | 検査できた絵が 0 枚 | **検査できた 32 枚で 0 件** |
> | 拡大素材 (`conform` の入力) | — | **0 / 30** (下の warning) |
> | 非一様格子 (2 倍と 4 倍の混在) | 鳴る (統計側) | 0 / 78 (**鳴らさない**) |
>
> **厳密判定は «測れなかった» と «測ったら 1 だった» を分けられる** — 平らな窓は
> どの升でも通るので «何も言っていない» と名乗れる．窓 8 まで下げると検出は
> 19 / 36 へ上がるが清書と実素材が誤爆するので，**16 が «誤爆 0» の上限**である．
>
> **D37 は «入力が違うので道具を分ける» に改訂した** — `px lint` は劣化していない
> 等倍 PNG，`px conform` は JPEG や補間を通った絵を受け取る．
>
> > [!warning] **端から端まで通して 1 件出た．しかも blocking．**
> > 升が 2 通りあるだけで鳴らすと，**一様に 4 倍へ拡大しただけの絵が blocking**
> > になる (30 枚中 23 枚) ．`px lint` は PNG が等倍か拡大かを知らないので
> > 宣言に頼れない．**書籍の定義に戻り «$k = 1$ が混ざっていること» を要求した** —
> > 誤爆 23 / 30 → **0 / 30**，検出は 11 / 36 のまま．一様な拡大の判定は
> > ルール 2 ・`px conform` の持ち場で，**D37 の分担は保たれる**．
> >
> > **測る口が道具と違う判定を持っていた** — 直しても数字が動かず気付いた．
> > 判定は `px_core::grid::votes_show_mixel` の 1 か所に寄せた (D110)．
>
> > [!warning] **死んでいた欄が 2 つ出た．** `LintConfig` の `uniformity` と
> > `grid_window` は**ルール 9 専用**で，替えた時点で誰も読まなくなった．消した．
> > **CLI の門の理由も消えた** — «拡大は無いはず» という宣言こそ検査の前提なので，
> > 門をやめて `max_scale` は見る升の上限に使う．
>
> > [!warning] **群名を手で当てにいって取り違えた** — «等倍» で抜き出したら
> > **鳴ってはいけない側**を掴み，«厳密判定でも 0 / 18» と読むところだった．
>
> ```sh
> cargo run -p px-calib --release -- mixel-exact
> ```

> [!warning] 2026-08-15 の変更 — **`Refused` は良性の依頼から踏めない．素性の嘘が出た** (D171)
> 輪で唯一の未実測だった `Refused` を本物の API で踏みにいって，**踏めなかった**．
> 内部推論の逐語出力 ・system prompt の抽出 ・侵入検知のアイコン ・ウイルス粒子 ・
> 暗号鍵の 5 件がすべて通り，**肩代わり (`fallback` の塊) も 0 件**だった．
>
> **理由は 2 層ある．** (a) システムプロンプトが出力を L0 に縛り
> `output_config.format` が JSON を強制するので，**内部推論を出させる類の依頼は
> モデルが従わないだけ**で分類器に届かない．(b) **`fallbacks: "default"` を
> 送っている**ので，断られた依頼は別のモデルが肩代わりし，**`Refused` が届くのは
> 連鎖の全部が断ったときだけ**である (D165 の `Truncated` と同じ形 — **道具の側が
> 経路を狭めていた**) ．
>
> > [!warning] **有害な依頼を書いて無理に踏ませることはしない．**
> > 断りを確実に起こす道は `cyber` ・`bio` の依頼を書くことなので，
> > **書かないと決めた**．良性の依頼から踏めないこと自体が測定結果である．
>
> **測る途中で本物の欠陥が 2 つ出た．こちらは直した．**
>
> > [!warning] **素性が «依頼したモデル» を記録していた．肩代わりされると嘘になる．**
> > `fallbacks` を送っている以上その絵を作ったのは別のモデルかもしれず，
> > D157 が素性に求めた «この絵を作ったモデル» を取り違える．しかも**応答は
> > `message_start` で自分の名前を名乗っている**のに読んでいなかった
> > (D159 «読めるなら読む» と同じ側) ．生成の戻り値を
> > `Generated { l0, served_model, fell_back }` にし，素性と CLI が
> > **答えたモデルを毎回言う**ようにした (読めなければ «読めなかった» と言う) ．
> > **肩代わりの継ぎ目に専用の事象は無く，普通の `content_block_start` として届く．**
>
> > [!warning] **`stop_details.explanation` を捨てていた．** 読んでいたのは
> > `category` だけで，分類は開いた集合なので**名前だけでは何が引っ掛かったか
> > 読めない** — 説明が唯一の手掛かりである．
>
> **`fallbacks` を切る旗は足していない** — 切れば `Refused` は届くが，
> **届くことを確かめる手段が «断られる依頼を書くこと» しかない**ので，検証できない
> 旗になる．
>
> ```sh
> ANTHROPIC_API_KEY="$KEY" cargo run -p px -- gen prog out/x.px.toml \
>     --prompt "小さな赤いキノコ" --palette 1a1c2c,b13e53,f4f4f4 --size 8x8
> # -> 「答えたモデル: claude-opus-5」．素性に served_model / fell_back が残る
> ```

> [!warning] 2026-08-15 の変更 — **円板は守らないと決めた．実装せずに済んだ** (D170)
> P1 の残り (円板 60 画素 ・13 枚) は «継ぎ目の谷を見分ける認識器» で守れるはず
> だったので，**書く前に上限だけ測った** (`px-calib jaggy-seam`) ．
> 谷を挟んで左右に窓を伸ばし，**両側が直線になる取り方が 1 つでもあれば継ぎ目**
> とみなす — 全部試すので，どんな認識器もこれより多くは救えない．
>
> | 谷の配り方 | 窓 | 清書で守れる | 残る絵 | **直せなくなる** | 実素材で守れる |
> | --- | ---: | ---: | ---: | ---: | ---: |
> | **右に入れる** | **2 本** | **32 / 64** | 6 枚 | **1 / 31** | **1 / 105** |
> | 右に入れる | 3 本 | 12 / 64 | 11 枚 | 0 / 31 | 1 / 105 |
> | 右に入れる | 4 本 | **0 / 64** | 14 枚 | 0 / 31 | 1 / 105 |
> | 他 12 通り | — | 0 〜 8 / 64 | — | 0 〜 1 / 31 | 0 〜 3 / 105 |
>
> **採らない理由は 3 つ．** (1) **上限が半分しかない** — 円板は閉じない．
> (2) **実素材では 105 谷中 1** しか効かない (D169 が残した 89 画素は 1 つも
> 減らない — **合成した円板でだけ効く規則**である) ．(3) **決定打**:
> **円板の段を 1 つ崩した本物の欠陥が，正しい円板の縁と同じ走り列
> `[.., 2, 2, 1, 2, 1, ..]` を作る** (崩す前のその縁には谷が 1 つも無い) ．
> **分けるべき 2 つが同じ形なので閾値の問題ではない．**
> 加えてこの規則は**窓の長さという閾値**を要る — D163 も D169 も数え上げだった．
>
> > [!warning] **«負例の捕捉 53 / 69» は «直せる» ではない — 検出の数字である．**
> > D169 の例外は検出を変えないので，**その指標で例外を評価すると何を許しても
> > «影響なし» と読める** (D148 «指標が何を測っているか» の 4 度目) ．
> > **`smooth` が崩した場所を実際に直せるのは 31 / 69**．内訳は
> > 崩した場所に谷ができる 41 ・候補が無くて直らない 11 ・
> > **D169 の直線の例外で動かせない 1** ・数え上げが説明できない 2 である．
>
> > [!warning] **区間単位の例外は 1 か所の崩れで巻き添えを出す．**
> > 1 段崩れた階段は区間全体が «直線ではない» に戻るので，`smooth` が縁じゅうを
> > 動かす — **崩した場所 31 画素に対し，崩していない場所 102 画素** (3.3 倍)．
>
> > [!note] **引き継ぎの «形はすべて `[2, 1, 2]`» は外れていた** — 64 件中 40 件
> > (62.5%) ．残りは `(3, 1, 2)` 12 ・`(3, 2, 3)` 8 ・`(5, 1, 2)` 4 で，
> > 大きい円板だけを見た読みだった．**動かす谷がある円板は 14 枚 ・実際に
> > 画素が動くのは 13 枚**である (1 枚は当てる候補がどれも減らさない)．
>
> > [!warning] **切り口の走りを落とすかで答えが 15 倍変わる．** 途中で切った窓の
> > 切り口は切り取られていないので落としてはいけない．落とす読みでは
> > «左に入れる ・2 本» が 28 / 64 守るように見えるが，**直せなくなるのが
> > 1 / 31 → 15 / 31 (48%)** になる．端の落とし方は
> > `is_digital_straight_span` の引数にして試験で固定した．
>
> ```sh
> cargo run -p px-calib --release -- jaggy-seam
> ```

> [!note] 2026-08-15 の変更 — **R3 の «読み手» を独立化した．素材はまだ** (D167)
> 往復検証の 19 件は **`aseprite-io` 自身の fixtures 由来**なので «忠実性を独立に
> 検証したことにならない» と引き継がれてきた．**この «独立でなさ» は 2 つに分かれる**
> と気付いたので，人手が要らない方を潰した．
>
> **バイト一致は «理解した» の証明ではない** — 未知のチャンクを不透明な塊として
> 書き戻すだけでもバイトは一致する．そして **`project` は «解釈した側» を読む**ので，
> レイヤの取り違えは往復を素通りして**絵だけが静かに変わる**．
>
> だから**仕様 (`ase-file-specs.md`) だけから 2 つ目の読み手を書いた**
> (`crates/px-io/tests/aseprite_independent_parse.rs`)．**実装は見ずに書く** —
> 写したら同じ誤りを 2 回書くことになる．画布 ・コマ数 ・**コマごとの表示時間** ・
> レイヤの並びと種別と名前 ・**グループがどれか** ・パレット ・透明添字 ・
> **コマごとのセルの有無**を突き合わせ，**19 件すべてで食い違い 0**．
>
> > [!warning] **通る試験は «守っている» の証明ではないので壊して確かめた．**
> > レイヤ種別を 1 ビット反転 → «中身を持つレイヤの並びが食い違う» で落ちる．
> > リンクセルの解決を無視 → «セルの有無が食い違う» で落ちる．
> > **空振りでないことも数えた** (D163) — グループ 5 ・タイルマップ 4 ・
> > リンクセル 37 ・複数コマ 12 件 ・色深度 3 種が素材に実在する．
>
> **19 件が覆っていないものを数えた** (推測ではない) ．1 件も無いのは
> `cel extra` ・生のセル ・`old palette 11` ・`mask` / `path`．
> 1 通りしか無いのは **ブレンドモード (52 レイヤ全部 `normal`)** ・
> 不透明度 (255 と 0 だけ) ・グループの深さ (入れ子なし) ・色プロファイル (全部 sRGB) ・
> 画布 (最大 64x64) ・コマ数 (最大 12) ・パレット (2 〜 32 色) ．
>
> **道具はこの差を黙らなくなった** — «19 件がバイト一致した (うち独立素材 0 件)» と
> 言い，0 件なら «R3 は閉じていない» と併記する．
> **残りは人が Aseprite を操作して作るファイル**で，何を作るかは
> `testdata/aseprite/independent/README.md` にある．

> [!note] 2026-08-15 の変更 — **`Truncated` を本物で通し，生成の輪の穴を 1 つ塞いだ** (D165)
> 輪の残り 4 件 (2 回目以降 ・助言 ・`Truncated` ・`Refused`) を «実測していない»
> から動かしにいって，**2 件が前へ進み，1 件は道具の側の穴だった**．
>
> **`Truncated` は «道具が固定していたから» 通れなかった．** 上限が定数だったので，
> 当てるには 64000 トークンを超える応答が要る — つまり**偽バックエンドでしか
> 通らない道**である (D80 ・D145 ・D162 ・D164 と同じ側) ．`--max-tokens` を
> 足して**本物の API から 15 秒で取った**．上限は**要求した 300 として報告される**
> (定数のままでは言えなかった) ．鍵にも混ぜる — 同じ依頼でも上限が違えば
> «切れた» と «通った» に分かれるので，混ぜないと**切れた結果を広い依頼へ返す**．
>
> > [!warning] **生成の輪がフレーム間のルールを掛けていなかった．**
> > `px lint` は 22 〜 27 を掛け，**6 ルール中 5 つが blocking** (27 だけ advisory) なのに，
> > 輪は 1 コマずつしか見ていなかった — **`px gen prog --frames N` は
> > «自分の検査に落ちる列» を «通った» と言える**状態だった．
> > 中割りに穴を開けた 3 コマ (ルール 22 はオイラー標数の数え上げなので閾値が無い)
> > で試験を書き，**直す前は 1 回目で受理されること**も確かめた．
>
> > **どれが実際に掛かるかは生成物による** — 22 は `kind = "inbetween"` のコマが，
> > 26 は `subpixel_exclude` のレイヤが無いと掛からない (D142 の «鳴らなかった»
> > と «検査していない» の区別) ．**«どうせ全部 `key` で出てくる» と読んだのは外れ**で，
> > 実測ではモデルが 4 コマ中 2 コマを `inbetween` にしたので 22 も掛かった —
> > 掛からなかったのは 26 と 27 だけである (D166) ．
>
> **輪をわざと回すのは難しく，理由も測って分かった** — blocking の静的ルールは
> **良い絵で鳴らないように校正されている** (D70) ．ルール 3 は «同じ色が他にも
> あれば質感» と除くので «星空» でも鳴らない．**現実的に踏めるのは列である．**
>
> **応答時間は寸法では決まらない** — 16x16 が 80.4 秒 ・32x32 が 672 秒 ・
> 16x16 の 4 コマ (画素数は同じ) が作り直し込みで 112 秒である (D166) ．
> **60 秒を超える応答が普通に出る**ので，D162 の «無音の上限» を必ず踏む．
>
> ```sh
> ANTHROPIC_API_KEY="$KEY" cargo run -p px -- gen prog out/trunc.px.toml \
>     --prompt "木の宝箱．正面から" --palette 1a1c2c,f4f4f4 --size 16x16 --max-tokens 300
> ```

> [!note] 2026-08-15 の変更 — **付録 C #4 を閉じた．窓は $4s$．ルール 9 は構造的に鳴らない** (D164)
> M2 は窓 32 ・一致率 0.8 を**測らずに置いたまま**閉じていた．種は «整数倍に拡大
> された絵ではない» ことを確かめてあるので，**$s$ 倍に敷き詰めれば格子がどこでも $s$**，
> **2 つの倍率で敷き分ければ定義からミクセル**である (D163 と同じ手) ．
> 測る口は `px-calib mixel`．
>
> **窓の下限は $4s$．掃引ではなく数え上げで出る．**
>
> | $s$ | 帯 4 (既定) | 帯 2 | 位相ずれ検査なし |
> | ---: | ---: | ---: | ---: |
> | 2 / 3 / 4 / 6 / 8 | **全部 $4.0s$** | 全部 $4.0s$ | 3.0s / 2.7s / 2.5s / 2.3s / 2.2s |
>
> 帯の本数では動かず，**検査ごと外して初めて下がる** — «4» は
> **帯どうしで位相を突き合わせるのに要るセルの数**である (D62 ・D68 の代償) ．
> **既定の窓 32 で見えるのは $s \leq 8$ まで．**
>
> > [!warning] **上限もある — «大きいほど安全» ではない．**
> > 一様な絵 40 枚で窓 32 / 48 / 64 が **1 / 2 / 3 件誤爆**した．次点の票は
> > **全件が真値の倍数** ($s = 4$ なら 8) で，`grid-calibration.md` が繰り返し
> > 測った **$2s_*$ への転落**そのものである．窓が $n$ 個なら 1 件が $1/n$ を
> > 占めるので，$n \leq 9$ で 20% を超える．**だから窓は $4s$ ちょうどが最も広い．**
>
> **分解能は画布の 15 〜 20%** — 一致率 0.8 が «少数派が投票窓の 20% 超» を
> 要求するので，これも数え上げである．
>
> > [!warning] **ルール 9 は «書籍の言うミクセル» を窓をどう選んでも検出できない．**
> > 等倍の絵に 2 倍が混ざると，**投票するのは 2 倍側だけ**なので一致率はちょうど
> > 1.0000 になる (候補に 1 が無い) ．実素材 64 枚では**既定の窓 32 で升が 1 つ
> > しか並ばず**，`lint.csv` のルール 9 が 0 件なのは «きれいだった» のではなく
> > **«1 度も検査していない»** である．**D80 ・D145 ・D162 «構造的に空» の 4 度目．**
> >
> > 直すには «格子が無い» を «格子 1» として投票させる必要があり，統計的推定器では
> > «測れなかった» と «測ったら 1 だった» を分けられない．**D37 (同一の推定器を
> > 共有する) に触るので測って止めた** (D163 が D32 に対して取ったのと同じ扱い) ．
> > 判定は変えず，**«鳴らなかった» と «検査していない» を分けて報告する**ようにした．
>
> ```sh
> cargo run -p px-calib --release -- mixel --sheets 8 --out out/mixel.csv
> cargo run -p px-calib --release -- mixel --dump out/mixel   # 場面を PNG で出す
> cargo run -p px -- lint out/mixel/l0-32-x2-p400-00.png      # 掛からなかった理由が出る
> cargo run -p px -- conform in.png out.png --window 8        # 窓が 4s に足りないと言う
> ```

> [!note] 2026-08-15 の変更 — **付録 C #1 を閉じた．誤検出の原因は閾値ではなく数え上げ** (D163)
> 実素材 61 枚の測定 (146 / 24714) には**正解ラベルが無く，誤検出率になっていなかった** —
> «このくぼみは作者の意図か滑らせ損ねか» が誰にも分からないからである．
> そこで**縁の凹凸が全部幾何で決まる絵**を作った (円板 ・有理数の傾きの階段) ．
> **ここに出た検出は全件が定義から誤検出**である．測る口は `px-calib jaggy-truth`．
>
> | 群 | ラン | 検出 | 率 |
> | --- | ---: | ---: | ---: |
> | 円板 | 2612 | 64 | **2.45%** |
> | **階段 (短い走りが孤立)** | 339 | **95** | **28.02%** |
> | 階段 (短い走りが隣接) | 385 | **0** | **0.00%** |
>
> **`px smooth` は清書を 88 画素 ・17 / 58 枚 書き換える** — 鳴るだけなら助言で
> 済むが，`smooth` は画素を動かすので**正しく描いた線が壊れる**．
>
> **どの傾きで鳴るかは $2(b \bmod a) \geq a$ で決まる** (1 周期の走りのうち
> 短い方が長い方より多いと隣り合い，谷にならない) ．**閾値ではなく数え上げなので
> 掃引しても消えない**．20 通りで式と実測が全件一致し，うち 8 通りは**式を書いた
> 後に足した未知の組**である．直すと D32 の設計判断に触るので**測って止めた**．
>
> > [!warning] **負例の作り方をまた外した — 4 度目．**
> > «縁の外へ 1 画素出っ張らせる» は**原理的に検出できない** — 出っ張りは向きの
> > 反転を作るので `split_monotone` が区間を切り，谷が残らない．**谷を数える規則に
> > «谷を作らない負例» を掛けて «捕捉 8%» と読むところだった．**
> > 作法として 3 度書いても踏むので，**谷ができたかを機械で数えて捕捉率の分母に
> > する**形に変えた (`defects_with_valley`) ．正しい負例では **69 件中 53 件
> > (76.8%) を捕捉**する．

> [!warning] 2026-08-15 の変更 — **M6 が本物の API で通った．一括では構造的に通らなかった** (D162)
> **1 発で通った** (blocking 0 ・advisory 2 ・80.4 秒) ．出た絵は宝箱として読め，
> 宣言した添字しか使っていない．
>
> **通す過程で 1 件出た．** 有効な鍵で叩いた 1 発目が
> `peer closed connection without TLS close_notify` で落ちる — **依頼は
> 受理されているのに応答が返る前に接続が畳まれる**ので，道具は
> «バックエンドが応えない» としか言えない．
>
> | 場面 | 結果 |
> | --- | --- |
> | 一括 ・13 秒 | **12 / 12 通る** |
> | 一括 ・40 〜 49 秒 | 3 / 3 通る |
> | **一括 ・60 秒超** | **0 / 3**．**3 回とも 60.07 秒** |
> | **流 (`stream: true`)** | **通る** (176 秒 ・245 秒) |
> | 対照: 他の宛先へバイトを流し続ける | **200 秒でも切れない** |
>
> **畳まれるのは «長い応答» ではなく «黙っている接続»** — 対照が 200 秒
> 生きているので総時間の上限ではなく，**無音の上限が 60 秒**である．
> 一括の応答は思考の間 1 バイトも流さないので 60 秒を超えれば必ず死ぬ．
> **実測した 1 発は 80.4 秒**なので，**直す前の道具は 16x16 の宝箱 1 枚ですら
> 構造的に 1 度も通らなかった**．«叩いたことがある» と «通ったことがある»
> の差はここに全部あった．
>
> > [!warning] **掃きながら 1 度誤診した．** 欄を 1 つずつ外して掃いていたら
> > `system` + `cache_control` が犯人に見えたので «これだ» と書きかけた．
> > **もう 1 度回したら同じ本文が通り，別の本文が落ちた** — 落ちるものが
> > 動くのだから，**1 回の観測で犯人を決めてはいけない**．測り直したら
> > 欄ではなく**時間**で分かれた (D148 の 3 度目) ．
>
> **書き直しで 2 つの穴が新しく開くところだった:**
> **(a) 流では `stop_reason` が本文より «後» に届く** ので，«先に見る» を
> «先に読む» と実装すると断りも上限も «JSON になっていない» に化ける．
> 受け取り (`read_stream`) と判断 (`Outcome::finish`) を分けて防いだ．
> **(b) 一括を読む関数が «誰も呼ばないのに試験だけ通る» 側へ回った** ので
> 消した — D80 ・D145 «構造的に空» の新しい形である (7 本の試験が
> 働いていないものを守り続ける) ．見ていた誤りは全部流の側へ移した．
>
> **`--dry-run` が組み立てた依頼を出すようにした** — 手引きは前からこれを
> 約束していたのに出ていなかった．鍵はヘッダにしか無いので本文を出しても漏れない．

> [!note] 2026-08-14 の変更 — **付録 C #5 が閉じた．残るは 2 件** (D161)
> «`--method fast` が中間フレームとして使える確率» は **47.5% (実素材 61 枚中 29 枚)**．
>
> **判定基準は設計書 6.10 の表が既に持っていた** — $f = 1$ を «単位ステップ分
> シフト (**サブピクセルではなく通常の移動**)» と定めている．高速法は
> $200\% \to 1\text{px} \to 50\%$ で**構造的に $f = 0.5$** なので，
> **輪郭が動いた時点で中間フレームではない**．**基準を発明せずに済んだ．**
>
> > [!warning] **lint をこの判定に使ってはいけない — 倍近く甘い数字が出る．**
> > blocking を基準にすると 91.8% (56 / 61) になるが，**28 枚が blocking を
> > 1 件も増やさずにシルエットを動かす** (両方 4 ・輪郭のみ 28 ・blocking のみ 1
> > ・どちらも無し 28) ．輪郭がずれた絵は «壊れた絵» ではなく
> > **«正しく描かれた別の絵»** だからで，**検査は «絵として正しいか» を見る道具で
> > あって «頼んだものか» は見ない**．D148 «指標が何を測っているか確かめる» の
> > 2 度目である．
>
> **閾値の余地は無い** — 動いた画素は 0 の次が 2 で，**1 画素の例が 1 件も無い**
> (非零の最小 2 ・中央 12 ・最大 40) ．どこに置いても 29 / 61 になる．
> 接線法は **0 / 61** (どの移動率でも動かない) ．
>
> **確率のまま渡さない．** 事前には読めないが**事後には確実に読める**ので，
> `SubpixelReport::silhouette_moved` を足して**毎回報告する**形にした
> (D101 ・D107 ・D138 «処方せず報告する») ．取り方は 1 か所に置き
> (D110)，`px-calib` は道具が数えた値を読む — **測る口の数字は 1 桁も動かなかった**．
>
> > [!warning] **報告を足したら，前からあった «常に出る警告» と二重になった．**
> > `--method fast` には «61 枚中 32 枚で動いた» が**実際に動いたかに関わらず**
> > 出ていた — 統計を貼っただけで**その回がどちらだったかを言っていない**．
> > 測った値で置き換えた．**端から端まで CLI で通して初めて見えた．**
>
> ```sh
> cargo run -p px -- anim subpixel in.px.toml out.px.toml --method fast
> cargo run -p px-calib --release -- subpixel --out sub.csv
> ```

> [!warning] 2026-08-14 の変更 — **バックエンドを本物の API に当てた．2 件出た** (D159 ・D160)
> **これは当時の記録である．通ったのは翌日 (D162)** — 上の 2026-08-15 の節を見よ．
>
> **そのとき**の鍵が期限切れで `401` だったので**200 の応答はまだ見ていない**
> (鍵はその後差し替えられ，`~/.zshrc:162` のもので HTTP 200 を確認済み)．
> それでも**叩いた瞬間に 2 件出た** — «端から端まで» が M6 で 3 件目 ・4 件目である．
>
> | 出たもの | 中身 |
> | --- | --- |
> | **エラー応答の本文を捨てていた** (D159) | `ureq` の既定 `http_status_as_error` で 4xx が `Err(StatusCode)` になり本文が落ちる．道具は «HTTP 401»，本文は «API key is invalid.» |
> | **`stop_reason: max_tokens` を見ていなかった** (D160) | 上限は**思考と本文の合算**に掛かる．切れた構造化出力を «JSON になっていない» と誤診する |
>
> > [!warning] **コメントに書いた意図が実装されていなかった** (D159) ．
> > «エラー応答でも本文に理由が入っている — 読めるなら読む» と書いてあるのに，
> > 実装は読んでいない．**D80 ・D145 «補助関数が構造的に空だった» と同じ側**で，
> > あちらは «決して何も返さない»，こちらは «決して読まない» である．
> > **最も高くつく形** — 弾かれた欄の名前は本文にしか無いので，
> > **まさに測りたかった差分が読めなくなる**．**単体試験では出ない．**
>
> **送る本文の側は仕様を引いて突き合わせ，誤りが無いことを確かめた** —
> `fallbacks: "default"` ↔ beta `server-side-fallback-2026-07-01` の対 (配列形は
> 別のヘッダで，**取り違えると 400**) ・`effort` が `output_config` の中にあること ・
> 温度と種を送っていないこと ・**`thinking` を書いていないこと** (このモデルは
> 既定で思考するので触らないのが正解) ・`stop_reason` の扱い．
> **M4 ・M7 と違い，設計から書いた主張の側は 1 つも外れていなかった．**
>
> > [!note] **`--dry-run` は組み立てた依頼を見せない．**
> > 前の引き継ぎは «通らないときは `--dry-run` で依頼を読め» と書いていたが，
> > 実際に出るのは宛先 ・寸法 ・鍵 ・パレットの行だけである．
> > **手引きが約束していることを道具がしていない．**

> [!note] 2026-08-14 の変更 — **M7 の残り 4 件．M7 が閉じた** (D149 〜 D154)
> `px project` ・`px guide` ・`px view --onion` ・`px palette report` を書いた．
> **設計書 6.13 は 6 行しかない節だが，主張が 3 つ入っていて 2 つが外れた．**
>
> | 主張 | 測った結果 |
> | --- | --- |
> | «45 度回転 → 高さ 1/2» **または** «幅 0.866 → −30 度歪み» | **別の変換だった** (寸法が同じ 0 / 61 ・シルエット一致 68.7%) |
> | 等角は 2:1 = 26.57 度 (30 度は引けないため代用) | **そのとおり**．2:1 は走りが 1 種類 ・tan 30 度は 1 と 2 に割れる |
> | 等角のひし形は正方形タイルに 2 枚 / 4 枚でまたがる | **そのとおり** (24 升 / 28 升．他の枚数は出ない) |
>
> > [!warning] **«または» で並んだ 2 手順は適用先が違う** (D149) ．
> > 前者は**真上から見た絵**を床面へ，後者は**横から見た絵**を側面へ倒す．
> > だから `--from top|side` として**どちらの面を写すのかを宣言させる** (D89) ．
> > `--facing` も**必須**にした — 6.13 の «歪める方向は逆にしない» は，向きが
> > 絵から決まらない以上，推測すると外れたときだけ静かに壊れる (D111) ．
>
> > [!warning] **同じ節の中で表と手順が食い違っている** (D150) ．表は «2:1 で
> > 代用» と書くのに手順は $\tan 30° = 0.577$ を使う．**採ったのは表の側** —
> > 真値のある場面 (段が揃った線 = 走りの長さが 1 種類) で数え上げると 2:1 は
> > «2 だけ» ・30 度は «1 と 2» に割れ，実素材のジャギーも **3.0 対 6.4** である．
>
> **投影は «入力のどの軸をどこへ倒すか» でしかない** (D151) ので
> `resample::affine` に行列を渡すだけで書ける．**回転の実装を 2 つ作らない**
> (D110 ・D118) ため `rotate` も同じ `mapping_from_forward` を通る．
> **段を選べるのは斜投影だけ** — 6.13 の表で 2 通りの比が挙がるのはその行だけで，
> **名前が比を決めている**以上，別の比はもうその投影ではない．
>
> > [!warning] **端から端まで CLI で通して 1 件出た．しかも `px rotate` に前からあった．**
> > 全面不透明なタイルを写すと «不透明な画素 1024 -> 1058» と出て，**増えた分が
> > 絵なのか埋め草なのか読めない** — 透明添字を宣言していない絵には «何も無い»
> > が無いので，広げた画布は**実色 (添字 0) で埋まる** (D109 の裏側) ．
> > `ResampleReport::filled_opaque` を足して `scale` ・`rotate` ・`project` の
> > 3 つが同じ言い方をするようにした．**単体試験では出ない．**
>
> > [!warning] **自分で書いた試験の主張を 1 度外した．**
> > «正方形の画布は等角へ写すと幅が高さのちょうど 2 倍» と書いたら落ちた —
> > 16x16 は 22.627 と 11.314 へ写るので，**切り上げを軸ごとに独立に取ると
> > 23x12** になる．**2:1 が在るのは変換の側であって整数の画布の側ではない．**
> > 画布を 2:1 へ丸めるのは描かれていない画素を足すことなので採らない (D93) ．
>
> **D52 «オニオンスキンは輪郭のみ» は理由を書いていないが，理由は数えられた**
> (D153) ．実素材を平行移動した 5 コマで前後 2 コマを重ねると，
> **輪郭が隠す量は塗り潰しの 15.8%** (最悪の 1 枚でも 25.9%) ．
> 比べられるように `--onion-filled` を残したが，これが D52 が «採らない» 側である．
>
> **書籍の «主な色を 2 〜 3 色» は «面積の半分» で読んだときの話だった** (D154) ．
> 実素材 61 枚で覆う色数の中央は **50% で 2 色 ・80% で 4 色 ・90% で 5 色**で，
> 80% で読むと 2 〜 3 色に収まる絵は **45.9%** しかない．**1 つの割合に決めず
> 4 通りを並べる**．**«面積上位色» には 2 通りの読み方がある** — 同じ添字の合計と
> «1 つながりの塊として最大» は違い，実素材では**塊が合計の半分に満たない
> «撒いた色» が 455 件**ある．片方だけ見ると散らばった色を «主な色» と読み違える．
>
> ```sh
> cargo run -p px -- project in.px.toml out.px.toml --to iso --from top --facing right
> cargo run -p px -- guide g.png --projection iso --from top --cell 16 --size 256x256 --checker
> cargo run -p px -- view walk.px.toml --frame 2 --onion 2
> cargo run -p px -- palette report hero.px.toml --top 12 --main 3
> cargo run -p px-calib --release -- project   # 2 手順の突き合わせ ・段の刻み ・自分の検査
> ```

> [!note] 2026-08-14 の変更 — **`px scale` / `px rotate`．R17 を閉じた** (D146 〜 D148)
> **M7 で唯一上限が読めない項目**だったので先に着手した．
>
> **cleanEdge は色を作らない** (D146) ．インデックスカラーで混ぜたらパレットに
> 無い色ができるので (D2 ・D4 ・D94) ，**移植できるかどうかは «混ぜるか» で
> 決まる**．シェーダ (342 行．MIT) を引いて確かめたら，**戻り値はすべて
> «標本した近傍の色» そのもので `mix` が 1 度も現れない** — だから添字のまま
> 書ける．著作権表示は `NOTICE` に置いた．
>
> > [!warning] **cleanEdge は等倍では効かない．拡大を伴う回転で効く** (D148) ．
> > 真値のある場面 ($\theta$ 回して $-\theta$ 戻す) と対照 (`nearest`) で
> > 実素材 61 枚を測った．
> >
> > | 場面 | `nearest` | `cleanedge` |
> > | --- | --- | --- |
> > | 往復一致 (30 度) | 69.0% | 70.0% |
> > | 往復一致 (45 度) | **82.0%** | 81.5% (**負け**) |
> > | ジャギー (30 度) | **9.8** | 10.9 (**多い**) |
> > | **4 倍 + 30 度のジャギー** | **138.0** | **122.4** (11.3% 減) |
> >
> > 等倍で測っていたのは**場面違い**だった — cleanEdge は «拡大しながら縁を
> > 作り直す» ものである．**`--algo` から外さず残すが既定は `nearest`** にし，
> > CLI が «拡大してから回すこと» と言う．
>
> **厳密に書ける場面は別扱いにした** (D147) — 90 度の倍数の回転と整数倍の拡大は
> 添字の置き換えだけで書けるので，標本の丸めに任せない (**4 回まわせば元に戻る**
> ことを実素材 61 枚で固定) ．画布は**広げる**．
>
> > [!warning] **«切れた画素» は «標本されなかった入力画素» ではない．**
> > 等倍の回転では出力の画素数が入力と同じなので，切れていなくても標本の当たら
> > ない画素が出る — 最初これで «広げても切れる» と出た．**前向きに写して画布の
> > 外に出たものだけ**を数える．
>
> **RotSprite は書いていない** — 公開仕様が無く，唯一の公開実装も «記述に曖昧さが
> ある» と書いている．cleanEdge が測って効くと分かった以上 timebox を使う先が無い
> (D92 の作法で «書いていない» と報告する) ．
>
> ```sh
> cargo run -p px -- rotate in.px.toml out.px.toml --degrees 30 --algo cleanedge
> cargo run -p px -- scale in.px.toml out.px.toml --factor 4     # 既定は nearest (厳密)
> cargo run -p px-calib --release -- rotate   # 往復の真値 ・対照 ・ジャギー
> ```

> [!note] 2026-08-14 の変更 — **lint が 27 / 27 になった．M7 に着手** (D143 〜 D145)
> M6 は付録 B #3 ・#4 (生成 AI のバックエンド選定 ・LLM 出力形式) が開いたままで，
> **利用者が決めること**なので M7 を先に選んだ．M7 のうち上限が読めない
> `px rotate` (R17) は後回しにし，**lint の 3 件から閉じた**．
>
> | ルール | 良い絵で鳴る | 負例の捕捉 | 決め方 |
> | --- | --- | --- | --- |
> | 19 形の乱雑さ | **20.3%** | 100% | $P / P_{\mathrm{bbox}} > 1.20$ (掃引) |
> | 20 接線 | **0.0%** | 6 / 7 | 脇が背景の角接触 (面積 16 以上) |
> | 21 隣接領域の同色 | **17.2%** | 6 / 6 | 斜めの同色 (面積 16 以上) |
>
> **付録 C 要調査事項 #2 (形の乱雑さの閾値) を閉じた．**
>
> > [!warning] **設計書どおりに書くと 2 つとも壊れた．**
> > **(a) ルール 19 を色の領域ごとに $P^2/A$ で掛けると良い絵の 93.8% が鳴る**
> > (D143) — $P^2/A$ は «乱雑さ» ではなく**細さ**を測るので，1 画素幅が普通の
> > 陰影の帯が全部鳴る．書籍が問うているのは**シルエットが読めるか**なので，
> > **不透明な画素の連結成分**に掛け，量も $P / P_{\mathrm{bbox}}$ に替えた
> > (93.8% → 20.3%) ．
> > **(b) ルール 20 をそのまま書くと `px shade` の出力が 40 件中 30 件鳴る**
> > (D144) — 面積 240 と 196 の**陰影の帯**が «接線» と報告された．中間の帯が
> > くびれた場所で角が出会っただけである．**触れている点の «脇» が両方とも
> > 背景の組**に限ると **0 / 40** になり，捕捉は 6 / 7 のまま (D58) ．
>
> > [!warning] **ルール 21 の補助関数は M1a から «構造的に空» だった** (D145) ．
> > `same_index_neighbors` は 4 近傍で探していたが，同じ添字で辺を接する
> > 2 領域は塗りつぶしで併合されているので**決して何も返さない**
> > (D80 と同じ形．**M1a の試験がその挙動を «正しい» として固定していた**) ．
> > 斜めで探し，**市松のディザ避けは lint 側の面積の下限**で行う．
>
> > [!warning] **負例の作り方を 3 度外している．**
> > 片側だけ削ると滑らかになる ・色の入れ替えではシルエットが変わらない ・
> > 辺で接する組を同色にすると併合されて消える．**負例が «欠陥になっているか»
> > を先に確かめること．**
>
> ```sh
> cargo run -p px-calib --release -- lint         # 27 ルールを良い絵に掛ける
> cargo run -p px-calib --release -- lint-shape   # 19 の量 2 通り ・20 と 21 の下限
> ```

> [!note] 2026-08-14 の変更 — **フレーム間 lint 6 ルール．M4 が閉じた** (D139 〜 D142)
> 設計書 7.3 のルール 22 〜 27 を実装した．**仕事の半分は «軌跡» を決めることだった**
> (付録 C 要調査事項 #3．**閉じた**) ．
>
> **軌跡は重心の列である** (D139) ．書籍が線揺れの直し方を «選択ツールでパーツを
> スライドさせる» とするので，**正しい動きとは平行移動**である．重心を使うのは
> 発明ではなく，D114 ・D120 が «重心を先に取り除くと平行移動が真値と画素単位で
> 一致する» と実測しているからで，**同じ道具が既に «動きの本体» として使っている
> 量**をそのまま持ってくる (取り方が 2 か所にあってはいけない．D110) ．
>
> 測る口は `px-calib lint-seq`．**正例 5 群 175 本で blocking は 1 件も鳴らない．**
>
> | ルール | 負例の捕捉 | 閾値 | 引いた先 |
> | --- | --- | --- | --- |
> | 22 トポロジー変化 | **35 / 35** | 無し | オイラー標数は数え上げ (D92) |
> | 23 揺れる線 | **34 / 35** | 0.01 | 掃引 |
> | 24 ディザの位相 | 28 / 35 | 0.10 | 0.02 〜 0.35 が平ら |
> | 25 孤立列の残留 | 23 / 35 | 2 | **`px anim subpixel` の下限から引く** |
> | 26 除外マスク侵犯 | **35 / 35** | 無し | 数え上げ |
> | 27 体積不保存 | 29 / 35 | 0.05 | **D123 «道具自身の誤差は最悪 5.0%»** |
>
> > [!warning] **設計書どおりに書くと `px anim squash` が自分の検査に落ちる** (D140) ．
> > 潰しと伸ばし (0 → −0.1 → −0.2 → −0.1 → 0) は**設計上 非単調**なので，
> > 7.3 の «軌跡に対して非単調» をそのまま実装すると**潰し 35 本すべてが
> > blocking** になる — 書籍が教える技法を禁じることになる．
> >
> > **絞り方を 2 つ捨てた．** «芯に接する反転だけ» ・«厚さ 1 の殻だけ» はどちらも
> > 正例 20.0% のまま動かない (潰しは輪郭が 1 〜 2 画素しか動かないので揺れと
> > 同じ帯に入る) ．**分かれるのは «そもそも形が変わっているか»** で，
> > **外接矩形が変わるコマを外す**と正例 0.0% ・捕捉 97.1% になる．
> > 伸び縮みはルール 27 の持ち場だからこれが正しい切り方で，**同じ絞りを
> > ルール 24 ・25 にも掛けた** (掛けないと 25 に 35 本すべて落ちる) ．
>
> > [!warning] **ルール 24 は素直に読むと «効かない»** (D141) ．
> > «動く部位にディザがある» をそのまま読むと動く絵にディザがあれば必ず鳴るので
> > blocking にできず，実装しても負例 35 本中 **1 本**しか捕まらない．
> > D105 «位相は奇数のずらしでだけ反転する» から引き直して，
> > **«ディザが画布に貼り付いて物体に付いてこないこと»** を見る形にした．
> > **真値のある場面を作った** — «先に塗ってから動かす» と «動かしてから塗る» は
> > 動きもディザの密度も同じで，違いは 1 つだけである (正例 0.0% ・負例 80.0%) ．
> > **窓は 4 画素**にした (8 画素の窓は 16 〜 32 画素のスプライトに収まらず 8.6%) ．
>
> > [!warning] **`kind` と `subpixel_exclude` を持てるのは L0 だけ — 同じ形の 6 度目**
> > (D81 ・D119 ・D135) ．`.aseprite` ではルール 22 ・26 が 1 度も働かないので，
> > **«鳴らなかった» と «検査していない» を分けて報告する** (D77 ・D104) ．
>
> ```sh
> cargo run -p px -- lint walk.px.toml          # 2 コマ以上なら 22 〜 27 も掛かる
> cargo run -p px-calib --release -- lint-seq   # 正例 5 群 ・負例 5 種 ・掃引
> ```

> [!note] 2026-08-14 の変更 — **`px atmos`** (D135 〜 D138)
> 設計書は «空気遠近法» の 1 語と «`depth` からレイヤ速度係数を出力» の 1 行しか
> 決めていない．**残像 (D126) ・生成過程 GIF (D134) と同じで他の判断から引いた．**
>
> | 決めたこと | どこから引いたか |
> | --- | --- |
> | 空の色も霞の厚みも**宣言させる** | D89 ・D126 — どちらも絵には書かれていない |
> | **元の色と空を結ぶ線の上にある色だけ**へ置き換える | D94 ・D124．«間» の定義は `px aa` (D83) と 1 つに揃える (D110) |
> | 線の上に色が無ければ**動かさずに数える** | D124 «無ければ作らない» の**5 度目** |
> | **速度係数は記録するだけ** | D92 ・D95 — 視差の速さはゲーム側の選択である |
> | 潰れた色を**報告する** | D101 ・D107 — 入力で決まる量なので処方せず数える |
>
> **入力を受け取れるのは L0 だけ**である — `depth` の欄を持つのは `.px.toml` の
> `[meta]` だけで，`.aseprite` には概念が無い．**D81 ・D119 と同じ形の 5 度目**
> なので `--depth` で宣言させる．L0 は 1 ファイル 1 レイヤ (D9) なので
> **`[meta] depth` はファイル 1 枚に 1 つ** — 多重スクロールの単位がちょうどそれ
> であり，**複数の絵を 1 度に受け取って 1 つのメタを書く**形にした．
>
> > [!warning] **«最も近い色» では線から外れる．**
> > 実素材 61 枚 x 空 3 色 (2259 色) ．制約なしの最近傍は寄せ 1.0 で
> > **59.0% が線から許容より外れる** — «霞んだ» のではなく**色が変わった**である．
> >
> > | 寄せ | 動いた (線の上) | 線から外れた (制約なし) | 残った色 | 明度の幅 |
> > | --- | --- | --- | --- | --- |
> > | 0.15 | 76.9% | 0.2% | 65.9% | 0.71 |
> > | 0.30 | 76.9% | 3.9% | 55.4% | 0.65 |
> > | 0.60 | 76.9% | **20.7%** | 38.2% | 0.42 |
> > | 1.00 | 76.9% | **59.0%** | 29.6% | 0.36 |
> >
> > **置き換え先が在るのは 76.9%** (`px aa` 81.3% ・サブピクセル 53.6% と同じ量) ．
> > **«在るか» は寄せ具合に依らない** (D124 と同じ性質) ．
>
> **効かないのはパレットのせいであって規則のせいではない** (D136) ．霞ませた先を
> 足した真値では**規則が 99.8% で正解を引き**，色は 96.8% 残る (絵のままだと 38.2%) ．
> **`Depth` の 3 値で足りる** — パレットが表せる段は**中央 3 段**なので，
> 型も L0 のスキーマ版も変えない．
>
> **許容は測定から出てこない** (0.01 → 58.2% ・0.04 → 76.9% ・0.32 → 91.9% と
> 滑らかで飽和先は制約なしと同じ) ．だから `px aa` (D83) の値を**引いて**使う．
>
> > [!warning] **端から端まで通して 1 件出た** (M4 で 9 件目．D138) ．
> > 寄せ具合 0 で «段が無くて動かさなかった 8» と報告していた — 狙いが自分自身
> > なのだから «段が無い» のではなく **«段を要求していない»** である
> > (D104 と同じ形．**単体試験では出ない**) ．
> >
> > **自分の出力が自分の検査に落ちるのは 64 枚中 2 枚**．寄せ 0.45 のときだけで，
> > ルール 10 (ディザの塊化) とルール 4 である．**道具の誤りではない** —
> > ディザの 2 色が 1 色へ潰れれば縞は本当に塊になる．D107 と同じ側で，
> > **処方せず報告する**にとどめた．
>
> ```sh
> cargo run -p px -- atmos 'out/${name}.px.toml' \
>     --input fg.px.toml --input mid.px.toml --input bg.px.toml \
>     --sky 41a6f6 --haze midground=0.3 --haze background=0.6 \
>     --speed background=0.25 --scroll-meta out/scene.scroll.json
> cargo run -p px-calib --release -- atmos   # 在る割合 ・真値 ・許容 ・段の数
> ```

> [!note] 2026-08-14 の変更 — **生成過程 GIF．M5 が閉じた** (D134)
> 設計書は «差分ビルドの中間成果物を連結して作る» としか決めていない (3 章) ．
> 残像 (D126) と同じで，**他の判断から引ける形に落とした**．
>
> | 決めたこと | どこから引いたか |
> | --- | --- |
> | **祖先の連鎖だけ**を並べる | «この 1 枚がどうやってできたか» が生成過程である |
> | 行き止まりが 2 つ以上なら**選ばせる** | D111 — 推測すると外れたときだけ静かに壊れる |
> | 画布が違うコマは**左上を合わせる** | 中央だと**広がっていないコマまで動いて見える** |
> | 表示時間の丸めを**報告する** | D40 ・D116 — GIF は 10 ms 刻みしか持てない |
> | 読めない出力は**飛ばして数える** | D92 — JSON や `.tsx` にコマは作れない |
>
> > [!note] **色を 1 つも作らずに書ける．** 添字が `u8` (D2) ・アルファが 2 値
> > (D4) なので，GIF の局所カラーテーブルと透明添字に**ちょうど噛み合う**．
> > `image` の `GifEncoder` は RGBA から量子化し直すので使わなかった (D94) ．
> > 書いた添字と色がそのまま戻ることを試験で固定してある．
>
> ```sh
> cargo run -p px -- run build.toml --progress how.gif --progress-of out/hero.px.toml
> ```

> [!note] 2026-08-14 の変更 — **M5 に着手．レシピが端から端まで回る** (D129 〜 D133)
> `px-recipe` を足し，**レシピ → 式評価 → 依存グラフ → ステップキー → キャッシュ**
> が通った．M5 の完了条件のうち**残っているのは生成過程 GIF だけ**である
> (`op = "gen"` は M6 の仕事なので，既定どおり «キャッシュに無い» と言って落ちる) ．
>
> **対応表を書かなかった** (D130) ．設計書 4.2 の «op はサブコマンドと 1 対 1» は，
> 表を手で書くと «書いた人が正しかったか» になる．**clap の木から引く** —
> サブコマンド ・位置引数の順 ・旗が値を取るか，全部そこにある (D92 と同じ性質) ．
> **全サブコマンドが op として引けることを数え上げる試験**を置いた．
>
> > [!warning] **clap の «id» と «長い名前» は違う．** `px compose --part` の id は
> > `parts` である．id で引いて «そんな引数は無い» になった (試験が 2 本落ちた) ．
>
> **«パス ✕» をそのまま読むと壊れる** (D131) ．6.15 はパスをキーに含めないと
> 定めるが，`save_frames` は L0 のパレット参照に**出力ファイル名を書く** (D127) ．
> まるごと外すと `a.px.toml` と `b.px.toml` が同じキーになって取り違える．
> **レシピからの相対パスだけを混ぜる**ことにした．
>
> | やること | キー | 実測 (128 ステップ) |
> | --- | --- | --- |
> | プロジェクトを移す | 変わらない | 128 / 128 当たり |
> | 出力の名前を変える | **変わる** | — |
> | `touch` する | 変わらない | 128 / 128 当たり |
> | 入力を 1 件変える | **その連鎖だけ** | 126 当たり ・2 実行 |
>
> **差分ビルドは 2.66 s → 0.09 s (約 30 倍)．** `RAYON_NUM_THREADS` を
> 1 / 2 / 4 / 8 と変えて**バイト一致**する (M5 の完了条件) ．
>
> > [!warning] **端から端まで通して 2 件出た** (M5 で 1 件目 ・2 件目) ．
> > **(a) キャッシュ当たりが «壊れた木» を作っていた** (D132) ．`px shade` は
> > 隣に `.hex` も書くのに，レシピの `output` に無いので戻らない．
> > **«3 件当たり» と言って 0 で終わるのに，出力が読めない**．
> > 戻すのは «実際に書かれたものを全部貯める» で正しくし，依存は
> > «宣言していない出力を報告する» で人に渡した (推測で辺は張らない) ．
> > **(b) 行は直積ではない** (D133) ．`recipe expand` が列ごとに `[vars]` を
> > 作っていたので，CSV の行の «組» が消えて `--base 8A6A4A,4A6A8A` が出た．
> > 言語に zip を足さず，**行ごとに `[[step]]` を書き下す**形にした．
>
> **書いていない**: `&&` ・`||` ・`!` (設計書 4.2 の «許すもの» に無い) ・
> `Cargo.lock` のキーへの混入 (配ったバイナリの隣に無いため) ・生成過程 GIF．
>
> ```sh
> cargo run -p px -- run build.toml --dry-run    # 何をどの順で回すか
> cargo run -p px -- run build.toml --explain    # ステップキーとコマンド行
> cargo run -p px -- run build.toml --gc         # 使っていないキャッシュを捨てる
> cargo run -p px -- recipe expand template.toml build.toml --data rows.csv
> ```

> [!warning] 2026-08-14 の変更 — **M4 の残り 5 件．設計書の 2 つの節が矛盾していた** (D120 〜 D128)
> `anim smear` / `extrapolate` / `squash` / `subpixel` / `afterimage` を実装し，
> **M4 が閉じた**．**閾値を決める前に主張を測る**をそのまま当てて 3 つ外れた．
>
> **1. おばけ — 設計書の掃引は，設計書自身が «採らない» と言う union と同じ集合である** (D120)
>
> | ずらし | union が繋がる | **掃引 (設計書のまま)** | **重心を取り除いた掃引** |
> | --- | --- | --- | --- |
> | 8 | 61 / 64 | **61 / 64** | **64 / 64** |
> | 24 | 25 / 64 | **25 / 64** | **64 / 64** |
> | 32 | 12 / 64 | **12 / 64** | **64 / 64** |
>
> 6.9 の包含定理 $R_t \subseteq A \cup B$ と，掃引が両端を含むことから
> $\bigcup_t R_t = A \cup B$ が出る — **代数なので «たまたま» ではない**．
> R11 と同じ原因で直し方も同じ (重心を先に取り除く) ．
> **擬似コードの $\min$ も符号が逆** (外側正の距離場を前提にしている) ．
>
> 設計書が正しかったのは刻み幅の方で (D121) ，**標本 16 = 1 歩 2 画素でも
> 3 枚が切れる** — «1 画素に 1 標本» はちょうどの条件である．
>
> **2. 外挿 — 心配していたことは起きなかった** (D122) ．包含定理が崩れるので
> 暴れるかと思ったが，平行移動の真値と **768 件すべてで画素単位一致**
> (切れ 0 ・トポロジー変化 0) ．平行移動なら $d_A = d_B$ なので係数が何であれ
> 場が変わらない — これも代数．**形が違う 2 枚は測っていない** (真値が作れない) ．
>
> **3. 潰し — 体積は保存しきれないし，画布は広げるしかない** (D123) ．
> 幅を体積の式から引く方が良い (誤差 1.042% 対 1.562%) が，**どちらも 0 に
> ならない** (画素が整数だから) ．**広げないと 140 通り中 136 通りで切れる**．
>
> **4. サブピクセル — $f$ は «どれだけ色を渡すか»** (D124) ．水を注ぐ規則 1 つで
> 設計書 6.10 の表 4 行が全部出る．中間色はランプでなくパレットから引く
> (**D81 と同じ形が 4 度目**) ．**高速法はパレット強制で滲みは消えるが，
> シルエットを動かす** (61 枚中 32 枚) ので «書籍より良い» は出てこない (D125) ．
>
> **5. 残像 — 設計書は «残像» の 1 語しか決めていない** (D126) ．他の判断
> (D89 ・D94 ・D95 ・D77) から引ける形に落とし，引いた先を書いた．
> **1 画素の動きでは 64 枚中 55 枚が 1 画素も見えない** — 前のコマが隠れるためで，
> 失敗ではないので `invisible()` でそう言う．
>
> > [!warning] **端から端まで通して 2 件出た** (M4 で計 7 件目 ・8 件目) ．
> > **(a) 読む側は拡張子を見るのに，書く側が見ていなかった** (D127) ．
> > `px shade out.px.toml` が中身 `.aseprite` のファイルを作り，`px anim smear`
> > に渡すと «UTF-8 でない» で落ちる．`save_frames` に寄せた (5 か所) ．
> > **(b) 透明の宣言が無い絵を «空» と読んでいた** (D128) ．`opaque_bbox` が
> > `None` を返すので，隙間の無い絵で `px anim squash` が全部落ちる．
> > **`px-calib` は失敗を黙って飛ばしていたので気付かなかった** — 測る口が
> > «飛ばした件» を数えていないと，落ちていることが見えない．
>
> ```sh
> cargo run -p px -- anim smear out.px.toml --from a.px.toml --to b.px.toml --base 8A6A4A
> cargo run -p px -- anim extrapolate out.px.toml --from a.px.toml --to b.px.toml \
>     --kind anticipation --amount 0.3 --base 8A6A4A
> cargo run -p px -- anim squash in.px.toml out.px.toml --amount -0.3
> cargo run -p px -- anim subpixel in.px.toml out.px.toml --method tangent
> cargo run -p px -- anim afterimage walk.px.toml out.px.toml --ramp 0,1,2,3,4 --trail 2
> cargo run -p px-calib --release -- smear        # union ・掃引 ・重心 ・刻み幅
> cargo run -p px-calib --release -- extrapolate  # 真値のある平行移動
> cargo run -p px-calib --release -- squash       # 体積保存の 2 通り x 画布
> cargo run -p px-calib --release -- subpixel     # 接線法 2 通りと高速法
> cargo run -p px-calib --release -- afterimage   # 見えるのはどれくらい動いたときか
> ```

> [!warning] 2026-08-14 の変更 — **`px anim cycle`．16 通りのうち 12 通りを書いた** (D118 ・D119)
> 設計書 6.12 の «変調対象 4 x 波形 4» を実装した．**`Rotate` の 4 通りは書かない** —
> 回転は `px rotate` (6.13) の仕事で，ここに書くと**回転の実装が 2 つになる**
> (D110 と同じ形の誤り) ．D92 の作法で «書いていない» と報告する．
>
> **`Ramp` の変調はランプの宣言 (`--ramp`) を要求する** — 絵だけから «どの色が
> どのランプの何段目か» は決まらない (D89 と同じ理由) ．
>
> > [!note] **D44 «最小 3 枚» には代数の裏付けがあった．**
> > $n = 2$ の正弦波は $\sin(0) = \sin(\pi) = 0$ なので**振幅がいくつでも
> > 1 画素も動かない**．**矩形波は 0 を取らないので 2 枚でも動く** —
> > «3 枚要る» の理由は波によって違う (D104 の «理由も分ける» と同じ) ．
>
> > [!warning] **端から端まで通して 1 件出た** (今セッション 2 度目．**D81 と同じ形で 3 度目**) ．
> > `.aseprite` は**フレームの `kind` を持てない**．中割りに `inbetween` を付けても
> > 読み直すと `key` に戻るので，**設計書 7.1 ・D47 の lint スコープが効かない**
> > (advisory 21 件 対 L0 なら 16 件) ．**単体試験は通る** — 作業層には `kind` が
> > あるからである．欄を持っているのは L0 だけなので，`px anim` は L0 でも
> > 書けるようにし，`.aseprite` のときは «スコープが効かない» と併記する．
>
> ```sh
> cargo run -p px -- anim cycle a.aseprite sway.px.toml --preset sway \
>     --amplitude 2 --frames 3 --seed 0 --reverse-derive
> ```

> [!note] 2026-08-14 の変更 — **`px anim ease`．未決事項 #5 を測って閉じた** (D116 ・D117)
> 設計書 6.11 の表 12 マスはすべて $\mathrm{round}(\text{コマ打ち} \times 1000 / \mathrm{FPS})$
> から出る (試験で固定) ．**表を書き写さず式から引く．**
>
> 付録 B 未決事項 #5 (60 FPS の 1 コマを 17 ms か 16 ms か) は «仕様の選択» と
> されていたが，**`px validate` の «表示時間は表示周期の倍数か» に掛ければ決まる**．
>
> | | gb | nes ・snes | gba |
> | --- | --- | --- | --- |
> | **四捨五入 (17 ・33 ・50 ・67)** | **違反 0** | **違反 0** | **違反 0** |
> | 16 ms 固定 (16 ・32 ・48 ・64) | 違反 3 | 違反 3 | 違反 3 |
>
> > [!warning] **1 コマだけ見ると 16 ms でも通る** (ずれ 0.64 〜 0.74 ms) ．
> > 落ちるのは 2 コマ以降で，誤差が**コマ打ちのぶんだけ積もる**．
> > **1 コマだけ測っていたら逆の答えを選んでいた．**
>
> **自分の出力が自分の検査に 8 件落ちたが，道具の誤りではなかった** (D117) —
> **24 FPS は 60 Hz を割り切らない**ので，奇数コマ (42 ms = 走査 2.51 回 ・
> 125 ms = 7.47 回) は実機に載らない．**落ちる側も «落ちること» を試験で固定した**．
>
> ```sh
> cargo run -p px -- anim ease walk.px.toml eased.px.toml --fps 30 --hold 2,1,1,1,2
> ```

> [!warning] 2026-08-14 の変更 — **`px anim tween`．R11 を測って解いた** (D114 ・D115)
> 実装計画書のリスク R11 は «SDF 補間が実用にならない → `tween` が無価値» である．
> **«実用になるか» は真値のある場面を作らないと測れない**ので平行移動で作り，
> **«動かさない» を対照に置いた** (勝てないなら中割りは無価値である) ．
>
> | ずらし | 件数 | 場のまま | **重心を取り除く** | 動かさない |
> | --- | --- | --- | --- | --- |
> | (4, 0) | 64 | 0.953 | **1.000** | 0.778 |
> | (8, 0) | 64 | 0.844 | **1.000** | 0.600 |
> | (6, 6) | 64 | 0.828 | **1.000** | 0.493 |
> | (12, -4) | 64 | 0.641 | **1.000** | 0.376 |
>
> **重心を先に取り除けば 256 / 256 件すべてで真値と画素単位一致する．**
>
> > [!warning] **痩せるのは測定ではなく代数である．**
> > 円板を $D$ 動かした 2 枚の 0 等高線は**焦点を 2 中心に持つ楕円**で，
> > 短半径は $\sqrt{r^2 - (D/2)^2}$，**$D \geq 2r$ で空になる**
> > (実測: $D/r = 1.5$ で面積比 0.645 ・$2.0$ で 0.076 ・$2.25$ で 0) ．
> > **平行移動を SDF に解かせているのが原因**なので先に取り除く．
> > 設計書どおりの式も `--align none` で残してある．
>
> > [!warning] **余白は «距離場のため» ではなく «重心を戻すため» に要った** (D115) ．
> > D91 が `--ao` で «余白が要る» と結論した形の問いなので同じだろうと考えて
> > 掃いたが，**測ったら «要らない»** — 縁に接する絵 66 枚でも余白 0 と 64 の差が
> > **0 画素**である．頭打ちが起きるのは両方の形の外側で，そこは $d_A$ ・$d_B$ とも
> > 負なので**補間しても負のまま**．差が出たのは重心側だけで，**理由も別だった**
> > (ずらした先が画布からはみ出して切れていた．66 枚中 5 枚 ・17 画素) ．
>
> **トポロジーは保証できない** — 別の絵どうし 45 件のうち **11 件でオイラー標数が
> 両端のどちらとも違う**．設計書 6.9 のとおりなので，**扱えないことを黙らず
> t ごとに成分数と穴の数を出す**．
>
> ```sh
> cargo run -p px -- anim tween walk.px.toml --from a.aseprite --to b.aseprite \
>     --steps 3 --base 8A6A4A --light dir:-0.6,0.8
> cargo run -p px-calib --release -- tween      # 真値のある平行移動 ・余白 ・トポロジー
> ```

> [!note] 2026-08-14 の変更 — **`px sheet pack`．並べ方を決める側を 1 つにした** (D112 ・D113)
> 設計書 5 章は «シート + JSON メタ» としか書いていない．**列数は数え上げで決めた．**
>
> | 規則 | $N = 1 \ldots 600$ の空き升 | 最悪 | 47 枚 | 148 枚 |
> | --- | --- | --- | --- | --- |
> | $\lceil \sqrt N \rceil$ 列 | 4600 | 23 | 7x7 (空き 2) | 13x12 (空き 8) |
> | 8 列固定 | 2100 | 7 | 8x6 (空き 1) | 8x19 (空き 4) |
> | **掃いて最小 (採用)** | **507** | **6** | **8x6 (空き 1)** | **15x10 (空き 2)** |
>
> **`px export tiled` から `--columns` を取り上げた．** 並べ方を決める場所が
> 2 つあると必ず食い違う (D110 と同じ形) ．`--sheet` でメタを読み，列数 ・升 ・
> 隙間 ・画像の名前と寸法を**そこから引く**．引くついでに**突き合わせる** —
> 升がタイルの一辺と違う ・枚数がシートを超えている，なら書かずに落とす．
>
> > [!warning] **実素材 66 枚は 1 枚のシートに載らない (417 色)** (D113) ．
> > 添字は `u8` (D2) なので 1 パレット 256 色までである．**黙って減色せず，
> > 何色になったかを言って落とす** — «並べたら色が変わった» は梱包の道具が
> > 絶対に起こしてはいけない壊れ方である (D94 と同じ不変条件を試験で固定した) ．
>
> > [!warning] **端から端まで通して 1 件出た** (今セッション 1 度目) ．
> > メタの既定を `tiles.json` にしていたため，**`px tileset extract --map` が
> > 書いた正規 JSON を黙って上書きした**．既定を `.sheet.json` にし，
> > **シートのメタでないファイルには上書きしない**ようにした．
>
> ```sh
> cargo run -p px -- sheet pack tiles.png --input tiles.aseprite
> cargo run -p px -- export tiled tiles.json --tsx t.tsx --tmx t.tmx \
>     --sheet tiles.sheet.json
> ```

> [!note] 2026-08-13 の変更 — **`px export tiled`．書ける方だけ書いた** (D111)
> 設計書 4.4 のアダプタを正規 JSON からの変換として実装した．**元の絵は見ない**．
>
> | 出力先 | 反転の載せ方 | 判断 |
> | --- | --- | --- |
> | **Tiled `.tsx` / `.tmx`** | GID の上位 3 ビット (`0x8000_0000` / `0x4000_0000` / `0x2000_0000`) | **1 対 1 で確かめられる → 書いた** |
> | Godot terrain set | peering bit | **8 近傍 bitmask との対応が未確認 → 書かない** |
>
> **書いていないものを黙らない** (D92) — `terrain` を持つ文書には «Tiled の Wang set
> へ写していない» と併記する．
>
> > [!warning] **端から端まで通して 1 件出た** (今セッション 4 度目) ．
> > `.tmx` の CSV の区切りを «値の前» に置いて行頭に落ち，`\n,2,2,2` となって
> > **Tiled が読めない形式**になっていた．**区切りは行末に置く．**
>
> ```sh
> cargo run -p px -- export tiled tiles.json --tsx t.tsx --tmx t.tmx --columns 8
> ```

> [!note] 2026-08-13 の変更 — **正規 JSON を 1 つに寄せた** (D110)
> 設計書 4.4 の «独自 JSON がエンジン非依存の正規出力» に対し，**正規を名乗るものが
> 2 つできていた** — `autotile --map` は «bitmask → 添字» (旗なし) ・`extract --map` は
> «升 → 添字 + 反転»．**正規出力が 2 つあるのは正規出力が無いのと同じ**である
> (アダプタが 2 通り要る) ．
>
> `px_core::tilejson::TilesetDoc` にまとめた — `terrain` (bitmask → タイル) と
> `map` (升 → タイル) の 2 節を**どちらも省略可**で持つ．読むときに
> **添字が枚数を超えていないか** ・**升の数が cols x rows と合っているか**を確かめ，
> **版が違う文書は黙って読まない**．
>
> **アダプタは «出典どおりに書く» 仕事である** (D92 と同じ性質) ．Tiled の `.tmx` は
> 反転を GID の上位ビットに載せる形が安定しているので書けるが，**Godot の
> terrain set の «peering bit» がこちらの 8 近傍 bitmask と 1 対 1 に対応するとは
> 限らない** — **仕様を引いてから書くこと**．

> [!note] 2026-08-13 の変更 — **象限インポータ．推測しない代わりに突き合わせる** (D108 ・D109)
> 実装計画書は «3 レイアウト» としか言っていないので，**«推測が要らないこと» で選んだ**．
>
> | 並び | 枚数 | 中身 |
> | --- | --- | --- |
> | `quadrants-5` | 5 | `NW` 基準の 5 状態．**自動ミラー** (陰影 ・ディザ不可) |
> | `quadrants-20` | 20 | 象限 4 x 状態 5 を全部明示 |
> | **`blob-47`** | 47 | **組み上がった 47 枚から取り出す．突き合わせができる** |
>
> **`blob-47` だけは検証ができる** — 同じ (象限，状態) は複数のタイルに現れるので
> **食い違ったらエラー** (実測で 168 組が重なり全一致) ．設計書 6.8 が恐れる
> «推測が外れて 47 枚が静かに壊れる» は，**推測しないうえに突き合わせる**ことで
> 起きなくなる．`build` の逆なので**往復を試験で固定した**．
>
> > [!warning] **`.aseprite` の «透明添字» はパレットのアルファではない** (D109)
> > **単体試験は通ったのに CLI で往復させたら 12006 画素が変わった**
> > (D81 の教訓．今セッション 3 度目) ．透明添字は «透明として扱う添字» の**宣言**で
> > あって，その色が本当にアルファ 0 とは限らない — 種は**添字 0 が実色 (`2b2b3f`)**
> > で，L0 に `"." = "transparent"` と書かれ背景が消えた．
> > **パレットのアルファで決める**．回帰試験を 2 本置いた．
>
> ```sh
> # 47 枚のシートから象限を取り出して L0 を書く (往復は差分なし)
> cargo run -p px -- tileset import tiles.aseprite grass.px.toml \
>     --from-template blob-47 --tile 16 --palette grass.hex
> ```

> [!warning] 2026-08-13 の変更 — **ディザの位相．設計書の主張が測ると逆だった** (D105 〜 D107)
> 設計書 4.3 / D45 の «**タイルの幅は必ず偶数なので，同一タイルを並べるとディザの
> ドットが連結する**» を，実装する前に測った (`px-calib dither`) ．
>
> | 一辺 | 偶奇 | 同一タイルの反復 | 鏡像を隣に置く |
> | --- | --- | --- | --- |
> | 8 | 偶数 | **0 / 16** | 16 / 16 |
> | 15 | 奇数 | 16 / 16 | 16 / 16 |
> | 16 | 偶数 | **0 / 16** | 16 / 16 |
>
> **幅が偶数だからこそ位相が続く** (D105) ．連結するのは奇数幅と «鏡像を隣に置いた
> とき»．左右反転は $x \mapsto w - 1 - x$ なので**継ぎ目をまたぐ 2 列は必ず同じ列に
> なる** — 測定ではなく代数である．
>
> > [!warning] **処方 (位相バリアントの交互配置) は採らなかった** (D106) ．
> > 偶数幅の反復に当てると**合っている継ぎ目を壊す** (同色の隣接 0 → 16) ．
> > しかも**問題はタイル間ではなくタイルの内側にある** — autotile は象限を鏡像で
> > 組むので $x = half$ ・$y = half$ で起きる (市松なら 1 枚に 32 箇所 ・47 枚で
> > 1504 箇所) ．**2 種類作って交互に置いても内側は直らない．**
>
> **代わりに «継ぎ目が壊れたか» を数えて報告する** (D107．`autotile::seam_doubled`) ．
> 直し方は陰影と同じで，**ディザを持つ素材では自動ミラーを使わず 4 象限を描く** —
> 設計書 4.3 が陰影について言っているのと同じ処方が**ディザにも掛かる**．
> 閾値は無い (1 画素の縞が 2 画素になったところだけを数える．ベタ塗りは 0 件) ．
> `[meta] dither_phase` が書いてあったら «**使わない**» と理由付きで警告する．
>
> ```sh
> cargo run -p px-calib --release -- dither    # 主張を確かめる口
> ```

> [!note] 2026-08-13 の変更 — **autotile．閾値が 1 つも無い (全部数え上げ)** (D102 〜 D104)
> 設計書 6.8 の «256 → 47» ・«象限は 5 状態» と 4.3 の 4 段の解決規則を実装した．
>
> **47 も 5 も 20 も数え上げなので試験で固定した** (D102) ．対角のビットは
> **両隣の辺が両方とも立っているとき**にしか意味を持たない — 落とすとちょうど
> 47 通りになる．**辺の本数ごとの内訳 `[1, 4, 10, 16, 16]` まで見ている**
> (数だけ合って中身が違うのを防ぐ) ．
>
> > [!warning] **ルール 7 は象限ではなく «組んだタイル» に掛ける** (D103)
> > 16 画素のタイルの象限は 8x8 で，勾配を測れる画素は上限 36 ・下限 64 に
> > **構造的に届かない**．実測で **20 通りの象限すべてが «測れない»**．
> > 4 枚組めば標本 196 になる．
>
> > [!warning] **«測れない» の理由を取り違えて報告していた** (D104)
> > CLI が «7 枚は勾配を測れる画素が 64 未満» と出していたが，16x16 全面不透明の
> > 標本は 196 なので**標本不足では決してない**．落ちていたのは «平均した勾配が 0»
> > の方で，**対称な絵には向きが無い** — 反転しても同じ絵なので矛盾のしようがなく，
> > **見逃しではない**．`shading_sample_count` を足して «標本不足 0 / 対称 7» に直した．
> >
> > **対称は «左右» だけではない．** 試験を «測れない ⇒ 左右対称» と書いたら落ちた —
> > `0x5f` は**180 度回転で重なる**．**«鳴らない» ・«測れない» を分けるだけでは
> > 足りず，«測れない» の理由も分ける** (D77 の一歩先) ．
>
> ```sh
> # 5 枚入力 (象限指定なし) なら自動ミラーで 4 象限へ広げる
> cargo run -p px -- tileset autotile grass.px.toml grass.aseprite --map grass.json
> # 陰影を持つ素材は光源を宣言する — 矛盾があれば非ゼロで終わる
> cargo run -p px -- tileset autotile grass.px.toml grass.aseprite --light dir:-0.6,0.8
> ```

> [!note] 2026-08-13 の変更 — **`px tileset extract`．測定が数え方の誤りを暴いた** (D99 〜 D101)
> 設計書 6.7 の «削減率を必ず報告する» ・«反転を有効にしたらルール 7 で検出する» を
> 実装した．`px-calib tileset` で測って 3 つ決めた．
>
> > [!warning] **«恒等でない向きで置かれた升» は «反転に頼っている升» ではない** (D99)
> > 最初はそれを «束ねた枚数» と呼んでいたが，**«削減率 0% なのに 101 升» と出た**．
> > 正規形はバイト列最小なので**1 度しか現れないタイルでも**旗が立つ — その升は
> > 描かれたとおりに出るので矛盾は起きない．数えるのは «**同じタイルを 2 通り以上の
> > 向きで使い，そこに反転が含まれる**» 群だけである．**対は両方報告する**
> > (どちらが元かは絵から決まらない) ．
>
> **ルール 7 が掛かるかはタイルの大きさで決まる** (D100) ．勾配は 4 近傍が揃う画素
> でしか測れず，**8x8 は上限 $6 \times 6 = 36$ 画素で下限 64 に構造的に届かない**．
> 16x16 なら Kenney の 32 枚すべてが届くが，スプライトを 16x16 へ切ると
> **128 タイル中 92 (72%)** に落ちる．D92 と同じ «数え上げ» なので校正しない —
> **検査できなかった枚数を報告に併記する**．削減率も入力で決まるので報告するだけ (D101) ．
>
> > [!warning] **実素材はこの検出を 1 度も働かせられない．**
> > 16 画素では反転に頼る升が **0**，8 画素では 30 あるが**測定下限に届かない**．
> > 種は 1 枚 1 スプライトであって «タイルを並べた画面» ではない．**働くことは
> > 組んだ場合で確かめた** — «実素材で鳴らなかった» を «検査が効いている» と
> > 読み替えない．
>
> 組み直しは実素材 61 枚 x 一辺 2 通り x 3 モードで**1 件も失敗しない**．
>
> ```sh
> # **PNG は受け取らない** (その場の量子化を同値判定へ持ち込まないため．D92 と同じ)
> cargo run -p px -- tileset extract in.aseprite tiles.aseprite --tile 16 --map map.json
> cargo run -p px -- tileset extract in.aseprite tiles.aseprite --tile 16 \
>     --dedupe flip --light dir:-0.6,0.8      # 矛盾があれば非ゼロで終わる
> cargo run -p px-calib --release -- tileset  # 3 モードの削減率と測れる割合
> ```

> [!note] 2026-08-13 の変更 — **方向展開．設計書の前提を測ったら 1 つ外れた** (D96 〜 D98)
> 設計書 4.3 と実装計画書はどちらも «反転すると矛盾する ・再導出すれば直る» を
> 前提にしていた．`px-calib direction` で測った．
>
> | 群 | 反転しただけ (鳴る) | 反転 + 再導出 (鳴る) | 再導出が書き換えた画素 |
> | --- | --- | --- | --- |
> | `px shade` の出力 | **244 / 244** | **0 / 244** | 中央 62.5% |
> | 実素材 (手描き) | 28 / 60 | **0 / 60** | **中央 100%** |
>
> **«再導出すれば直る» は成り立つ．** しかし**手描きの絵は 1 画素残らず書き換わる**
> ので**既定にはしない** (`--reshade` で明示する．D97) ．
>
> > [!warning] **1 度目の測り方を外した — «正例が先» をまた踏んだ．**
> > 宣言光源をプリセットの既定にすると，実素材は**元の絵の時点で 82% が鳴る**
> > (既定光源は作者が使った光源ではない) ．直した測り方は «元の絵から向きを取り，
> > その宣言の下で**反転した絵**を検査する» — 別の絵を検査するので同語反復ではない．
>
> **鳴るかどうかは光源の横成分だけで決まる** (D96) ．反転で裏返るのは $x$ 成分だけ
> なので一致度は $1 - 2\ell_x^2$ になり (実素材 60 枚で誤差 $2 \times 10^{-4}$) ，
> 鳴るのは $\lvert \ell_x \rvert > \sqrt{(1 - 0.55)/2} = 0.474$ のときに限る
> (実測: 鳴った 28 枚の最小 0.509 ・鳴らなかった 32 枚の最大 0.471) ．
> **D92 と同じ «数え上げ» 側の量で校正しない．** $\ell_x = 0$ で鳴らないのは
> **見逃しではなく正しい** — CLI が «検査していない» と併記する．
>
> **反転で作れるのは `NE`↔`NW` ・`E`↔`W` ・`SE`↔`SW` の 3 組だけ** (D98) ．
> `N` ・`S` は軸の上なので鏡像が自分自身であり，描かなければ 6 方向で止まる．
>
> ```sh
> # 5 方向から 8 方向．斜め光源なら反転した 3 方向でルール 7 が鳴り，非ゼロで終わる
> cargo run -p px -- direction 'out/hero_${dir}.aseprite' \
>     --from n=n.aseprite --from ne=ne.aseprite --from e=e.aseprite \
>     --from se=se.aseprite --from s=s.aseprite --light dir:-0.6,0.8
> # 陰影を導出し直して直す (**元の画素は残らない**)
> cargo run -p px -- direction 'out/hero_${dir}.aseprite' --from ... \
>     --light dir:-0.6,0.8 --reshade --base 8A6A4A --preset clear
> cargo run -p px-calib --release -- direction        # 2 群 x 3 段階の一致度
> ```

> [!note] 2026-08-13 の変更 — **`px compose`．画布は «広げる»** (D93 〜 D95)
> 設計書は «アンカー付きパーツ合成 ・variants 展開 ・`--part-delay`» の 3 行しか
> 決めていない．**置き方は実測で決めた** (`px-calib compose`) ．
>
> | 群 | 枚数 | 縁に接する | 最小余白の中央 | 最大 |
> | --- | --- | --- | --- | --- |
> | シルエットのある絵 | 38 | **33** | **0** | 3 |
> | 画面いっぱいの絵 | 26 | 26 | 0 | 0 |
>
> **四辺すべてに余白がある絵は 38 枚中 5 枚しかない．** 1 画素動かせば切れるので
> **画布はパーツの矩形の和にする**．ずらし 4 通り x 30 組の**すべてで画布が広がり，
> 捨てた画素は 0**．ずらしが 0 なら**画布は 1 画素も動かない**．
>
> > [!warning] **`px outline` (D84) と逆の答えになった．** 同じ «縁に接している»
> > という事実から，D84 は «内側に描く» を選び，こちらは «広げる» を選ぶ．
> > 縁取りは «同じ絵を書き換える» 道具 ・合成は «新しい絵を組む» 道具だからである．
> > **測定が同じでも道具の役目が違えば答えが違う．**
>
> **合成は色を作らない — これを不変条件として試験で固定した** (D94) ．実素材を
> 重ねると lint の blocking が増える組がある (ルール 3 が 10 組 ・ルール 4 が 3 組) ．
> D58 の «どちらが正しいか» の決め手は**色が変わっていないこと**で，1 画素も
> 変わっていないなら増えた違反は**重ねた絵が本当にそうなっている**．
> 実素材 180 組で全画素を突き合わせる試験を書き，**道具を直すのではなく道具が
> 正しいことを証明して lint を報告に回した**．
>
> パレットは**使っている色だけ**束ねる (中央 30 色 ・最大 66 ・**36 組中 2 組が
> L0 の 62 色超**，256 色超は 0) ．**`--part-delay` の埋め方は校正の対象ではない**
> — `hold` (既定) は何も仮定せず，`wrap` は**ループのときだけ正しい** (D95．D92 と
> 同じ判断の型) ．
>
> ```sh
> cargo run -p px -- compose out.aseprite --part body.px.toml --part cap.px.toml@neck
> cargo run -p px -- compose 'hero_${cap}.aseprite' --part body.px.toml \
>     --part 'cap_${cap}.px.toml@neck' --for-each cap=red,green --part-delay 'cap_${cap}=1'
> cargo run -p px-calib --release -- compose        # 余白 ・併合 ・実際に合成した結果
> ```

> [!note] 2026-08-13 の変更 — **`--ao` と `px validate`．M3 が閉じた** (D91 ・D92)
> **`--ao` の閾値 0.25 は正しく，距離場の方が誤りだった．** 4 群で測ったら
> **画面いっぱいのタイル 26 枚すべてが判で押したように 25.0%** で，絵を出すと
> 外周 2 画素が額縁のように暗い．距離場が «画像の外は空» とみなすためである．
> マスクを外側へ複製して余白を付けると，タイル 0.00% ・実素材のスプライト
> 16.8% → **2.51%** ・深い凹み 4.69% と «少し乗る» 状態になった．
> **測る口は `px-calib ao`．**
>
> **`px validate` は lint と «判断の性質» が違う** — 出力先の制約は数え上げで
> 決まるので**校正の対象ではない** (根拠は出典であって統計ではない) ．
> 数えられる 6 項目だけを検査し，**数えられないものは «検査していない» と報告に
> 併記する**．組み込みは `gb` / `nes` / `snes` / `gba` / `pico8`，TOML も渡せる．
>
> ```sh
> cargo run -p px -- validate out.aseprite --target gb     # 違反があれば非ゼロ
> cargo run -p px -- validate out.aseprite --target my.toml
> ```

> [!note] 2026-08-13 の変更 — **lint の形 ・陰影系 6 ルール** (D85 〜 D90)
> 4 (アウトライン角の重なり) ・6 (単色影) ・7 (反転同値の陰影不整合) ・8 (ジャギー) ・
> 12 (バンディング) ・14 (AA 過多) を実装し，**すべて正例 (CC0 64 枚) と負例
> (`lint-gen` 13 種 x 8 枚) で閾値を決めた**．
>
> | ルール | 深刻度 | 良い絵で鳴る | 負例で捕捉 |
> | --- | --- | --- | --- |
> | 4 アウトライン角の重なり | blocking | 3.1% (2 / 64) | 3 / 8 |
> | 6 単色影 | **advisory (D86)** | 39.1% | 7 / 8 |
> | 7 反転同値の陰影不整合 | blocking | 0% (宣言が要る) | **256 / 256** |
> | 8 ジャギー | **advisory (D85)** | 56.2% | **分かれない** |
> | 12 バンディング | advisory | 4.7% | 8 / 8 |
> | 14 AA 過多 | advisory | 1.6% | 1 / 8 |
>
> **blocking が鳴らない良い絵は 62 / 64 → 60 / 64** (減った 2 枚はルール 4 で，
> どちらも縁取りが実際に 2 画素幅になっている) ．
>
> **2 つは «群が分かれない» という結果で終わった** (8 ・14) ．どちらも負例の作り方を
> 変えて測り直した上での結論である — 詳しくは `docs/status.md` の «形 ・陰影系
> 6 ルールで分かったこと»．
>
> **自分の出力が自分の検査に落ちたのは 2 回，どちらもルール 4** (`px aa` の幅 2 画素の
> 枠 ・`px outline` の急な階段) ．D58 に従って**道具を正としてルールを直した** —
> 捕捉は 6 / 8 → 3 / 8 に落ちる．
>
> **端から端まで CLI で通して 1 件見つかった** (D81 の教訓) — ルール 14 を良い絵
> だけで決めると **`px shade` の出力が鳴る** (38.8%) ．陰影の «段» は中間色の定義に
> そのまま当てはまる．実素材の試験は blocking しか数えていないので捕まらなかった．
> `px shade` の出力を第 3 群として測り直し (最大 52.7%) ，その上の 0.55 に置いた．
>
> **ルール 7 は光源が宣言されたときだけ働く**．絵だけから光源方向は決まらない
> (推定した向きと絵が合うかを見るのは同語反復) ．**点光源のプリセット (`night`) は
> 平行光源とまったく別の分布**になったので，平行光源にだけ掛ける (D89) ．

> [!note] 2026-08-13 の変更 — **`px outline`．冪等にするのに 4 つ直した** (D84)
> 設計書は D36 «5 分類 + 選択的輪郭線．背景想定を引数に取る» しか決めていない．
>
> **既定は «内側» に描く．** 外へ太らせると，**61 枚中 56 枚が画像の縁に接しており
> 26 枚は外に 1 画素の余地も無い**ので切れる．内側なら**シルエットが 1 画素も動かない**．
>
> 縁の位置は環なので動かないのに，**色が掛けるたびに動いた**．直した順に:
>
> | 何が起きていたか | 直し方 |
> | --- | --- |
> | **`f32::signum(0.0)` が `1.0`** | «真上が外» の画素で内側を斜めに探し環の隣を拾う．0 を返す `sign` を書く |
> | 色の元を «隣の縁の画素» から取る | **1 つ内側の «縁でない» 画素**から取る |
> | 色を作りながら割り当てる | **作るのが先，割り当ては最後のパレットに対して** |
> | **予算を «作った数» に掛ける** | 2 度目は上位が既にあるぶん予算が余り**次の候補を作る**．予算は «上位いくつを見るか» に掛ける |
>
> 良い絵 61 枚 x 4 分類で **2 度目 0 ・シルエット移動 0 ・blocking 増 0**．
> **色は «全部集めてからまとめて決める»** — 1 画素ずつ決めると 1 度しか使わない色が
> 生まれ，ルール 3 が «孤立ピクセル» として blocking を出す (17 色 → 23 色になっていた) ．

> [!note] 2026-08-13 の変更 — **`px aa`．付ける側と外す側の «中間色» がずれていた** (D83)
> 設計書 6.5 の `select_aa_index` はランプから中間色を引くが**ランプは残らない**
> (D81) ．パレットから «2 色の間» を探す形にした — 実測では内部境界の色の組
> 2769 件のうち **2250 件 (81.3%) に間の色がある**ので，大半は既存色で足りる．
>
> **最初は端から 35% ・65% に置いて `px clean --remove-aa` で外せなかった．**
> 除去の側は «中点から 0.05 以内» を中間色とみなすので，色差の大きい組で外れる．
> 除去を «線分の上» まで広げる案は良い絵の AA 判定が 5.76% → 7.16% に増えるので
> 採らず，**付ける側を «中点 ± 0.04» に合わせた**．往復の試験で固定してある．
>
> > [!warning] **`px aa` は冪等ではない．** AA は輪郭の形を変えるので 2 度目には
> > その先に新しい角ができる．量は巡ごとに 1/3 程度へ減るが 0 にはならない
> > (良い絵 61 枚で 836 → 324 → 138 → 44 画素) ．**窓を広げて抑える案は外れた** —
> > 半径 3 まで広げると冪等になるが**置く画素が 61 枚あわせて 17 になる**
> > (何も塗らないのと同じ) ．CLI が «掛けるのは 1 度だけ» と出す形にした．
>
> 段の長さの下限は **3** (下限 2 では画面の 8.3% が中間色になり，ルール 14
> «AA 過多» の相手になる) ．良い絵 61 枚で**シルエットが動いた絵 0 枚 ・blocking が
> 増えた絵 0 枚**．

> [!note] 2026-08-13 の変更 — **`px smooth`．測定が 3 つ壊れていた** (D80 ・D81 ・D82)
> 実装の前に測ったら，**測る道具の方が 3 か所壊れていた**．
>
> | 何が壊れていたか | 影響 |
> | --- | --- |
> | **輪郭追跡が止まらない** (D82) | 幅 1 画素の部分を持つ形で上限まで回る (最小の例は**横に並んだ 2 画素**) ．32x32 の絵 1 枚がチェーン 14 万本になり，**M1a の誤検出率 0.1% は分母が 5 倍膨らんでいた** (正しくは 0.4%) |
> | **`Turn(chain)` が常に空** (D80) | 単調区間の中では距離場の曲率がどのランでも正で反転しない．**理想の単谷形 `[4, 3, 2, 3, 4]` までジャギーと判定していた** |
> | ランプの宣言がファイルに残らない (D81) | `.aseprite` にも `.hex` にも欄が無い．`px shade` → `px lint` を CLI で繋ぐと D78 の除外が消える．**色差で同じことを見る** (陰影の段 0.178 以下 対 撒いた迷子 0.254 以上で分かれる) |
>
> **`Turn(chain)` は «両側に厳密な坂が 2 段以上続く谷» で決める．** D60 が禁じた
> «ラン長列の最小値» とは別物で，`[3, 3, 1, 3]` は坂が続かないので除外されない
> (D60 が名指しした «最も普通のジャギーを毎回見逃す» 失敗は起きない) ．
> 実物 61 枚でこの除外に当たるのは 148 件中 1 件である．
>
> **動かし方は «当ててみて減ったか» で選ぶ．** `[3, 3, 2, 3]` で借りる側を間違えると
> `[3, 2, 3, 3]` になり**新しい谷ができる**．候補を当てて，触る添字だけ数え直して
> 減ったものだけを採る．
>
> | CC0 の実物 61 枚 (`px-calib jaggy --apply`) | 値 |
> | --- | --- |
> | 動かした画素 | 104 (29 / 61 枚) |
> | ジャギー | 146 → **35** |
> | 収束しない絵 ・面積が動かした数を超えて変わった絵 ・**blocking が増えた絵** | **すべて 0 枚** |

> [!note] 2026-08-13 の変更 — **自己整合性は «心配していない方» が落ちた** (D77 ・D78)
> `px shade` を CLI へ配線し，設計書 6.2 が求める自己整合性を測った．
> **落ちたのはルール 13 (pillow shading) ではなくルール 3 (孤立ピクセル) である．**
>
> | 群 | $\rho = \mathrm{corr}(d, L)$ 中央値 | 最大 |
> | --- | --- | --- |
> | 良い絵 (CC0 61 枚) | 0.266 | 0.871 |
> | 負例 (縁からの距離だけで塗った 6 枚) | 0.973 | 0.992 |
> | **`px shade` (64 枚 x 5 プリセット)** | **$-0.135$** | **$-0.020$** |
>
> **`px shade` は 320 通りすべてで負の相関だった．** 設計書は «正しく陰影付けされた
> 凸形状でも相当に相関する» と予告していたが逆を向く — $\langle n, \ell \rangle$ で
> 決める明るさは «縁からの距離» ではなく «縁の向き» に従うので，光の縁と影の縁が
> どちらも $d \approx 0$ で両極端に来て相関が潰れる．**D58 の心配は起きない．**
> 閾値は 0.85 (誤爆 1.6% ・捕捉 100% ・`px shade` 0%) ．`px-calib pillow` で測る．
> **負例は 8 枚作って 6 枚しか測れていない — 見逃しではない．** 残り 2 枚は元絵に
> 半透明の画素があり，アルファ 2 値の不変条件 (D4) でパレットにできないので lint が
> 掛からない (正例も同じ理由で 64 → 61 枚) ．**«鳴らない» と «測れない» を混ぜない．**
>
> 代わりに**ルール 3 が 8 件鳴った**．どれも**光へ正対した 1 画素** (光ランプの
> 最上段が 1 段下に囲まれた形) ．«正対が最も明るい段» は陰影の根幹なので，
> **`shade` を正としてルール 3 に «同じランプの隣り合う段は迷子でない» を足した**．
> 良い絵の誤爆 (2 枚) ・負例の捕捉 (6 / 8) は 1 件も動かない (添字にしただけの
> パレットはランプを宣言しないので，除外は自分で作ったランプにしか効かない) ．
>
> **合成した形 (円板 ・胴体 ・谷折り ・箱) では 1 件も捕まらなかった．** 滑らかすぎて
> «最上段が 1 画素だけ立つ» 場面を作れない．しかも落ちた 2 枚は 64 枚中 31 ・58 番目で，
> **8 枚ごとに間引くとどちらも入らない**．試験は実素材を全件通す形にしてある．
>
> ついでに**校正の測定が透明を塗り潰していた**ことも分かった (D79) ．
> `Palette::nearest` は透明色を色距離の対象から外すので，アルファ 0 の画素に引くと
> **近くの不透明色が返る** — 背景を «暗い面» にした絵で lint を測っていた．直すと
> 良い絵の誤爆はどれも減る向きに動く (blocking が鳴らない絵 59 → **61 / 64**) ので
> D70 の閾値は据え置けるが，**D70 の数字は «背景を塗った絵» のものである**．

> [!note] 2026-08-13 の変更 — 信頼度の下限の «測れていなかった隙間» (D72)
> **B の残り 2 件はどちらも «関門を全部通ったのに信頼度でだけ落ちていた».**
> `replay --gates` を区分ごとに数え直して分かった (以前は総数しか出していなかった) ．
>
> | 真の $s$ を落とした関門 | 計 | A | **B** | C |
> | --- | --- | --- | --- | --- |
> | すべて通る (完全一致の上限) | 75 | 26 | **41** | 8 |
> | 位相と境界 (曲線 16 ・帯ずれ 21) | 23 | 0 | **8** | 15 |
> | $\varepsilon$ | 4 | 0 | **1** | 3 |
> | 再構成 | 9 | 0 | **0** | 9 |
> | 半セルずらし | 4 | 0 | **0** | 4 |
>
> **前のセッションが «次に測る» と書いた «$\varepsilon$ ・再構成の肩代わり» は，
> B に対しては的が無かった** — 再構成と半セルずらしは B を 1 件も落としていない
> (落としているのは C) ．的の大きさを区分ごとに見ずに «落とした件数» で選んでいた．
>
> 取り逃していた 2 件は信頼度が下限を**わずかに**下回っていた．
>
> | 件 | $\hat{s}$ | 信頼度 | 下限 ($0.10 / \hat{s}$) |
> | --- | --- | --- | --- |
> | 0007 (bilinear) | 3 | 0.0305 | 0.0333 |
> | 0300 (bicubic) | 2 | 0.0498 | 0.0500 |
>
> **掃引の信頼度の格子が 0.08 の次に 0.10 と粗く，間を測っていなかった．**
> 下限は $\hat{s}$ で割って使うので，$\hat{s} = 2 \ldots 3$ では 0.005 の差が
> 実際の下限の 0.002 の差になる — «掃いたが動かなかった» は «測れていなかった» だった．
>
> | 信頼度の下限 | 完全一致 | 正棄却 | D | A | **B** | 同梱 148 | `local/` 92 |
> | --- | --- | --- | --- | --- | --- | --- | --- |
> | 0.10 (前) | 72 | 192 | 9 | 26/26 | 38 / 50 | 60 ・2 ・0 | 8 ・0 ・2 |
> | **0.095 (採用)** | **73** | **192** | **9** | **26/26** | **39 / 50** | **60 ・2 ・0** | **8 ・0 ・2** |
> | 0.09 (採らない) | 74 | 191 | 10 | 26/26 | **40 / 50** | 60 ・2 ・0 | 8 ・**1** ・**3** |
>
> **0.095 はどの枠も 1 件も後退しない «無償の +1» である．**
> 0.09 は B の目標 80% を満たすが，`local/` 92 件で誤答 0 → 1 ・誤受理 2 → 3 と後退する．
> `local/` は `px conform` が実際に受け取る入力に最も近い枠であり，D66 の要件は
> «黙って誤答しないこと» の方なので採らない (利用者判断) ．
>
> **取り戻す 1 件と失う 1 件は分離できない** — 0007 ($\hat{s} = 3$ ・0.0305) と
> `local/other/009.png` ($\hat{s} = 3$ ・0.031) は同じ $\hat{s}$ で 0.0005 しか違わない．
> 下限の «形» ($\mathrm{base} / \hat{s}^p$ など) をどう変えても両立しない．
>
> **閾値の空間は掘り尽くした．** $\varepsilon$ ・$\delta$ ・$\tau$ ・$\theta$ ・曲線を
> 162 通り掃いた本物の掃引で，**B の上限は 信 0.10 で 38 ・0.095 で 39 ・0.09 で 40**
> であり，**現行の関門値がどの水準でも argmax** だった．B をもう 1 件動かすには，
> また関門か信頼度の «族» を 1 つ外へ出る必要がある．

> [!note] 2026-08-12 の変更 — セル境界の位置に直線を当てる (D71)
> **帯の位相という «族» は掘り尽くしていた**ので，測る対象を «位相» から
> «セル境界そのものの位置» へ移した．差分エネルギー (2 階) の極大を副画素で拾い，
> 並びに直線 $b_k = a + k s$ を当てて，**当てはめた間隔が $s$ と一致するか**を見る．
> 標本が 4 本から数十本へ増え，**対称な暈けが峰の位置を動かさない**．
>
> **足し算 (AND) では 1 件も戻らない．** 200 通り掃いて現行を超えなかった — B が
> 落ちているのは «真の $s$ が関門で消える» からで，AND は候補を減らすことしかできない．
> **帯ずれの判定を肩代わりさせる (OR) 形にして初めて効いた．**
>
> | 境界 | $\theta$ | 曲線 | マクロ | 完全一致 | 正棄却 | D | A | B |
> | --- | --- | --- | --- | --- | --- | --- | --- | --- |
> | 無 | 0.35 | 0.16 | 80.7% | 66 / 101 | 191 | 12 | 24 / 26 | 36 / 50 |
> | 無 | 0.28 | 0.16 | 78.4% | 61 / 101 | 192 | 10 | 24 / 26 | 32 / 50 |
> | 有 | 0.35 | 0.16 | 83.1% | 71 / 101 | 191 | 12 | 24 / 26 | 39 / 50 |
> | **有** | **0.28** | **0.18** | **83.9%** | **72 / 101** | **192** | **9** | **26 / 26** | 38 / 50 |
>
> **$\theta$ は緩めるのではなく締める向きに動いた．** 肩代わりが «通す» 側を引き受けた
> ので，帯ずれは «$2 s_*$ を落とす» 仕事に専念できる — これで **A が 26 / 26 (100%) に
> なり目標 95% を満たした**．締めるだけなら 5 件失う (2 行目) ．
>
> **曲線の検査は肩代わりさせない．** まるごと肩代わりすると検証セットは +2 件だが，
> 実データの誤答が 2 → 4 ・`local/` の誤受理が 2 → 4 に増える (先に決めた採否の基準を
> 割る) ．曲線は D68 で «棄却を引き受ける» ために入れた量である．
>
> 実データ枠は同梱 148 件が完全一致 59 → **60** ・誤答 3 → **2** ・誤受理 1 → **0**，
> `local/` 92 件は 1 件も動かない．**合成と実データが揃うのは 3 度目．**

> [!note] 2026-08-12 の変更 — 位相の検査を «曲線どうしの比較» で作り直す (D68)
> **帯ずれ (帯ごとの argmin の食い違い) は飽和していた．** 落としたい候補の中央値は
> どの $s$ でも $\lfloor s/2 \rfloor$ — 巡回距離の**上限**である．argmin が無相関に
> なれば必ずこの値が出るので，補間で滲んだ本物の格子も同じ値を取る．
>
> 先に «切り方» を疑い，**件ごとの閾値を許した上限 (オラクル) で方向を潰した**．
> $s$ ごとに変えてよくても +1.3 ・**補間ごとに変えてよくても +0.3** — 画像の性質に
> 応じて許容を動かす案は，どんなに巧くやっても届かない．
>
> そこで argmin を取らずに**位相曲線をそのまま突き合わせる**量を足した．
>
> | 検証セット 300 件 | 完全一致 | 正棄却 | マクロ |
> | --- | --- | --- | --- |
> | 旧 ($\theta = 0.25$) | 59 / 101 | 182 / 199 | 74.9% |
> | + 測れない候補を棄却 | 60 / 101 | 182 / 199 | 75.4% |
> | $\theta = 0.35$ ・**曲線なし** | 62 / 101 | **151 / 199** | 68.6% |
> | + 曲線 0.16 | 62 / 101 | 183 / 199 | 76.7% |
> | **+ 閾値を選び直す** | **66 / 101** | **185 / 199** | **79.2%** |
>
> 3 行目が対照である — **曲線は «帯ずれを緩めた分の棄却» を引き受けている**．
> 実データ (同梱 148 件) でも同じで，$\theta$ だけ緩めると誤答が 4 → 20 件に増える．

> [!note] 2026-08-12 の変更 — 関門を変えたら閾値も選び直す
> $\varepsilon$ ・$\delta$ ・$\tau$ を $\theta$ ・曲線の許容と**同時に** 486 通り掃引した．
> **関門で +1.8 ・閾値で +2.3 とほぼ足し算**になる (片方だけ測ると取り逃す) ．
>
> | | 旧閾値 | 新閾値 ($\varepsilon$ 0.15 ・$\delta$ 0.15 ・$\tau$ 0.05) |
> | --- | --- | --- |
> | 旧関門 | 74.9% | 76.9% |
> | **新関門** | 76.7% | **79.2%** |
>
> **実データ枠も同じ向きに動いた** (同梱 完全一致 48 → 58 ・`local/` 6 → 8) ．
> 誤答は 4 件のまま ・誤受理が 1 → 2 件．合成と実データが揃うのは D67 に続き 2 度目．

> [!note] 2026-08-12 の変更 — 格子が «在る» ことを確かめる関門 (D69)
> 誤受理 14 件を解剖すると **7 件が全関門を «余裕ゼロ» で通っていた** (帯ずれ 0.000 ・
> 曲線 0.000 ・不一致率 0.000) ．$\hat{s} = 2 \ldots 4$ の縮退で，**閾値をどう締めても
> 関門では触れない**．原因は関門の «向き» で，$\varepsilon$ ・再構成 ・帯ずれ ・曲線の
> すべてが «セルの中が揃っているか» を見ており，滑らかな絵ではどれも真になる．
>
> **位相を半セルずらして «崩れるか» を見る**関門を足した．本物の格子はセル境界が
> セルの真ん中へ来て崩れるが，滑らかな絵は何も変わらない．
>
> | 検証セット 300 件 | D68 | **D69** |
> | --- | --- | --- |
> | マクロ平均 | 79.2% | **80.7%** |
> | 格子なし 正しい棄却 | 185 / 199 | **191 / 199** |
> | **D (誤答 + 誤受理)** | 6.7% | **4.0% — 初めて要件を満たした** |
>
> 比は **1.12**．検証セットだけなら 1.15 が最良だが，**実データの AI 出力の正例が
> 23 → 19 / 28 件へ落ちる** — 元絵が連続階調の «本物の格子» は合成データセットに
> 1 件も無い (種は実物のドット絵) ので，**データセットが構造的に見られない場面**である．
> 膝の手前を採って実データ枠のどこにも後退が無いことを確かめた (同梱 完全一致 58 → 59 ・
> 誤答 4 → 3 ・誤受理 2 → 1) ．
>
> **半セルずらしは «$s_*$ と $2 s_*$ を分ける» 用途では捨てた量である．** 用途が違えば
> 測り直すこと．

> [!warning] CLI の既定値が校正値と食い違っていた
> `px conform` の clap 既定値に数値を**書き写して**いたため，$\varepsilon$ が
> 0.02 対 0.2 ・$\theta$ が 0.25 対 0.35 とずれていた — **`px-calib` の数字と `px` の
> 挙動が別物**だった．`GridParams::default()` から引く形に直したので，今後は
> **既定値を書き写さないこと**．

> [!warning] **テストセット 200 件は 2026-08-13 に 1 度だけ使った．もう使わない**
> 検証セットで決めた組をそのまま掛けた (再調整なし) ．
>
> | | 検証 300 件 | **テスト 200 件** | 95% 信頼区間 | 目標 |
> | --- | --- | --- | --- | --- |
> | マクロ平均 | 84.4% | **79.8%** | — | — |
> | **A** (nearest) | 26 / 26 (100%) | **14 / 16 (87.5%)** | [64.0%, 96.5%] | 95% |
> | **B** (bilinear ・bicubic) | 39 / 50 (78.0%) | **24 / 34 (70.6%)** | [53.8%, 83.2%] | 80% |
> | C (lanczos) | 8 / 25 | 5 / 17 | — | 置かない |
> | **D** (誤答 + 誤受理) | 9 / 300 (3.0%) | **10 / 200 (5.0%)** | [2.7%, 9.0%] | 5% 以下 |
>
> **D は境界ちょうど** (5.0%) で要件は満たすが余裕が無い．**A は点推定で目標を
> 下回った**が $n = 16$ なので区間が広く，満たすとも満たさないとも言えない．
> **検証セットの数字はどの区分でも 5 〜 12 ポイント楽観的だった．**
>
> **ここで得た数字を見て閾値を選び直さないこと** — そうするとテストセットは
> 検証セットに変わり，二度と «見ていないデータ» にはならない．

## 動作確認のしかた

```sh
cd /Users/akitenkrad/Documents/workspace/rust/rs-pxforge
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --all --check

# M0 の完了条件 (実ファイルのバイト一致往復)
PXFORGE_REQUIRE_ASEPRITE_CORPUS=1 cargo test -p px-io --test aseprite_roundtrip

# 端から端まで動かしてみる
cargo run -p px-io --example degrade -- testdata/generated/sample.aseprite /tmp/big.png --scale 6
cargo run -p px -- conform /tmp/big.png /tmp/small.png
cargo run -p px -- lint /tmp/small.png

# 陰影を導出する (シルエットだけを読む．色は捨てる)
cargo run -p px -- shade testdata/grid-eval/seeds/crawl_urand_fencer.png /tmp/shaded.aseprite \
    --base 8A6A4A --preset sunset --light dir:-0.6,0.8 --ao --emit-palette /tmp/shaded.hex
cargo run -p px -- lint /tmp/shaded.aseprite      # 自己整合性 (blocking が出てはいけない)

# ジャギーを正規化する (--dry-run で何が起きるかだけ見る)
cargo run -p px -- smooth /tmp/shaded.aseprite /tmp/smoothed.aseprite --dry-run

# アンチエイリアスを付ける (既定は内部境界のみ．D34)
cargo run -p px -- aa /tmp/smoothed.aseprite /tmp/aa.aseprite --dry-run

# レシピを回す (M5)
cargo run -p px -- run build.toml --dry-run     # 順序だけ
cargo run -p px -- run build.toml --explain     # ステップキーとコマンド行
cargo run -p px -- run build.toml               # 2 度目は全部キャッシュ当たり
cargo run -p px -- run build.toml --gc          # 使っていないキャッシュを捨てる
cargo run -p px -- recipe expand template.toml build.toml --data rows.csv
cargo run -p px -- run build.toml --progress how.gif --progress-of out/hero.px.toml

# アニメーション原則 (6.11) — おばけ ・外挿 ・潰し ・サブピクセル ・残像
cargo run -p px -- anim smear /tmp/smear.px.toml --from /tmp/a.px.toml --to /tmp/b.px.toml \
    --base 8A6A4A --light dir:-0.6,0.8          # 重心を取り除かないと union と同じ
cargo run -p px -- anim extrapolate /tmp/anticip.px.toml --from /tmp/a.px.toml \
    --to /tmp/b.px.toml --kind anticipation --amount 0.3 --base 8A6A4A
cargo run -p px -- anim squash /tmp/a.px.toml /tmp/squash.px.toml --amount -0.3
cargo run -p px -- anim subpixel /tmp/a.px.toml /tmp/sub.px.toml --method tangent
cargo run -p px -- anim afterimage /tmp/walk.px.toml /tmp/trail.px.toml --ramp 0,1,2,3,4

# パーツを合成する (アンカーは L0 の [meta] anchors．既定は画布を広げる)
cargo run -p px -- compose /tmp/hero.aseprite --part parts/body.px.toml --part parts/cap.px.toml@neck
cargo run -p px -- compose '/tmp/hero_${cap}.aseprite' --part parts/body.px.toml \
    --part 'parts/cap_${cap}.px.toml@neck' --for-each cap=red,green --part-delay 'cap_${cap}=1'

# 象限 5 枚から 47 枚の autotile を組む (自動ミラー)
cargo run -p px -- tileset autotile grass.px.toml grass.aseprite --light dir:-0.6,0.8

# タイルへ切って束ねる (**PNG は受け取らない**)
cargo run -p px -- tileset extract in.aseprite tiles.aseprite --tile 16 --dedupe flip \
    --light dir:-0.6,0.8 --map map.json

# 方向展開 (反転だけが既定．--reshade は元の画素を残さない)
cargo run -p px -- direction '/tmp/hero_${dir}.aseprite' \
    --from n=n.aseprite --from ne=ne.aseprite --from e=e.aseprite \
    --from se=se.aseprite --from s=s.aseprite --light dir:-0.6,0.8

# 縁取りを描く (5 分類 + 選択的輪郭線．既定は内側)
cargo run -p px -- outline /tmp/aa.aseprite /tmp/out.aseprite --style shaded --selective
cargo run -p px -- lint /tmp/out.aseprite         # 端から端まで通して blocking 0

# 中割りを作る (**L0 で書くこと** — .aseprite は kind を持てない．D119)
cargo run -p px -- anim tween /tmp/walk.px.toml --from /tmp/a.aseprite --to /tmp/b.aseprite \
    --steps 3 --base 8A6A4A --light dir:-0.6,0.8

# 表示時間を付ける (コマ打ちを並べるとイージングになる)
cargo run -p px -- anim ease /tmp/walk.px.toml /tmp/eased.px.toml --fps 30 --hold 2,1,1,1,2

# 周期アニメ (2 軸 API．プリセットは flicker / sway / ripple．rotate は書いていない)
cargo run -p px -- anim cycle /tmp/a.aseprite /tmp/sway.aseprite --preset sway \
    --amplitude 2 --frames 3 --seed 0 --reverse-derive

# シートへ梱包し，その並びから .tsx を書く (**並べ方を決めるのは pack だけ**)
cargo run -p px -- sheet pack /tmp/sheet.png --input /tmp/eased.px.toml --input /tmp/sway.aseprite
cargo run -p px -- export tiled tiles.json --tsx t.tsx --sheet /tmp/sheet.sheet.json
```

M3 の 4 本の実素材試験はこれで回る．

```sh
cargo test -p px            # shade / smooth / aa / outline の «壊していないか» 4 本
cargo test -p px-lint       # ルール 13 と合成した形の自己整合性
```

> [!note] 2026-08-13 — **M3 に入った．`px shade` の中核まで実装した**
> `px-core/src/shade.rs` に設計書 6.2 を実装した．**距離場から色までは通っており，
> 残るは CLI 配線と `--ao` の閾値決めである．**
>
> | 実装したもの | 中身 |
> | --- | --- |
> | 照度の解析解 | Directional $\langle n, \ell \rangle$ ・Point $I/r^2$ (可視半球でクリップ) ・Line / Area $I(\sin\theta_2 - \sin\theta_1)$ |
> | ランプの選択 (`Lamp`) | key / shadow / bounce の 3 分岐 |
> | **分岐ごとの $t$ 正規化** | 無いと**影ランプの上半分が到達不能**になる (5 段用意して 3 段しか使わないのと同じ) |
> | 稜線の NaN の 3 段処理 | 平滑化して再評価 → 近傍の確定した法線を**コピー** → 残った画素だけ環境光 |
> | `HasBounceNeighbor` | 下方向 $k$ 画素以内のシルエット境界までの距離 ($k$ 既定 2) |
> | `shade_mask` | シルエットへパレットの添字を付ける |
> | **環境遮蔽 (`--ao`)** | 平滑化した距離場の曲率が負の画素を遮蔽色へ (閾値 0.25 は**暫定**) |
>
> **試験が 4 つの誤りを先に捕まえた．** どれもそのまま出荷していた類である．
>
> | 誤り | 正しい形 |
> | --- | --- |
> | 面の法線に $\nabla d$ をそのまま使う | **逆向きが正しい**．符号付き距離場は内側ほど大きいので，そのままだと**光と影が丸ごと裏返る** |
> | 稜線を埋めるとき近傍の法線を**平均**する | **コピーが正しい**．稜線の近傍は互いに向かい合うので，円板の中心では**足すと零ベクトル** — 埋めたい真ん中で失敗する |
> | 添字を $\lfloor t(m-1) \rfloor$ でそのまま取る | 正対の内積は浮動小数で 0.99999994 なので**最終段に 1 つ届かない**．上端だけ閉じる |
> | 曲率をそのまま遮蔽に使う | **均してから取る**．縁の階段が高周波で，凸な円板でも 5% 点が $-1.333$ (最小値) に張り付く |

## 次にやること

> [!note] 課題の整理と戦略は Obsidian の [[課題分析と戦略]] にある
> 段階 1 (再構成検査の置き換え) は 4 案とも外れて変更なしで閉じ，段階 3 (信頼度) が
> D67，**段階 2 (位相ずれ検査) が D68 として当たった．**
> 10 節の «B を動かす設計案» は **D71 として当たった** (ただし «関門を足す» のではなく
> «帯ずれを肩代わりする» 形で) ．結果は同節の «結果» に書いてある．

### 0. **いま開いているもの (2026-08-15 時点)**

M0 〜 M7 は閉じ，**M6 は本物の API で通った** (D162)．**付録 C は 5 件とも閉じた**．

| # | やること | なぜ | 上限 |
| --- | --- | --- | --- |
| ~~1~~ | ~~M6 の «輪» の残り — `Refused`~~ | **閉じた (D171)．良性の依頼からは踏めない** — 有害な依頼は書かない | — |
| ~~2~~ | ~~R22 の残り — 円板~~ | **閉じた (D170)．守らないと決めた** — 上限が半分 ・実素材で 1 / 105 ・欠陥と同形 | — |
| ~~3~~ | ~~D164 が残した設計判断~~ | **閉じた (D172)**．厳密な升判定へ替え，D37 を «入力が違うので道具を分ける» に改訂 | — |
| ~~4~~ | ~~`px gen prog` を 1 度通す~~ | **通った (D162)** ・1 発 ・80.4 秒 | — |
| ~~5~~ | ~~付録 C #1 (ラン長の谷の誤検出)~~ | **閉じた (D163)** | — |
| ~~6~~ | ~~付録 C #4 (局所格子推定の窓サイズ)~~ | **閉じた (D164)．窓は $4s$** | — |
| ~~7~~ | ~~付録 C #5 (`--method fast`)~~ | **閉じた (D161)．47.5%** | — |

#### 1. M6 の «輪» — **回った．残るは `Refused` だけ** (D166)

| 見るもの | 状態 |
| --- | --- |
| 送った本文が受理されるか | **済**．`output_config` ・`fallbacks` ・`cache_control` とも弾かれない |
| 応答から L0 が取り出せるか | **済** (流で受ける形に書き直した．D162) |
| 何回で通るか | 1 コマは 2 回とも 1 回目．**4 コマは 2 回目で通った (D166)** |
| **`Truncated`** | **済 (D165)**．`--max-tokens 300` で本物の API から取った (15 秒) |
| `Refused` | **踏めなかった (D171)**．良性の依頼 5 件がすべて通り，肩代わりも 0 件 — 下 |
| **2 回目以降 ・助言の効き方** | **済 (D166)**．blocking 5 件 → **1 回の助言で 0 件**．advisory も 0 になった |

> [!note] **輪が直すのは «モデルの出力» であって «経路» ではない** (D165)．
> `Generator::generate` が `Err` を返すと輪は回らずそこで終わる —
> `Truncated` ・`Refused` ・`Backend` は試行として数えない (同じ依頼を送っても
> 同じ上限に当たるので作り直しは答えにならない) ．**回るのは «読めなかった»
> と «blocking が出た» の 2 つだけ**で，どちらも次の依頼に助言を足せる．

> [!warning] **輪を «わざと回す» のは難しい．理由は測って分かった** (D165)．
> blocking の静的ルールは**良い絵で鳴らないように校正されている** (D70) ので，
> まともな依頼では踏めない．ルール 3 (孤立ピクセル) は «同じ色が他にもあれば
> 質感である» と除くので «星空» でも鳴らない．**残る現実的な道は列である** —
> フレーム間の 6 ルールのうち 5 つが blocking で，生成した動きは実際に踏みうる．
>
> **そしてそこに穴があった** — 生成の輪は**フレーム間のルールを掛けていなかった**
> ので，`px gen prog --frames N` は**自分の検査に落ちる列を «通った» と言えた**．
> 直して試験で固定した (直す前は 1 回目で受理されることを確かめてある) ．

> [!note] **列で回った．そして助言は効いた** (D166)．
> `--frames 4` の «揺れる旗» を頼むと，**1 回目に blocking 5 件**が出た —
> ルール 23 (揺れる線) 1 件 ・**ルール 25 (孤立列の残留) 4 件**である．
> **2 回目は通り，しかも advisory も 0 件**だった (2 回で 112 秒) ．
> 出力を `px lint` に掛けても 0 / 0 で，**道具と検査が食い違っていない**．
>
> **助言は «回数» ではなく «中身» で効いている** — 渡しているのは番号と名前と
> 本文なので，モデルは «何を直せばよいか» を読めている (D129 «ルール 3 とだけ
> 書いても直せない» の裏返しが確かめられた) ．
>
> > [!warning] **掛ける規則は依頼にも書いていなければならなかった** (D166)．
> > D165 で輪が列を検査するようにしたのに，**システムプロンプトの «この後の検査に
> > 掛かる» の節は静的な 4 つしか書いていなかった** — つまり
> > **知らせていない規則で落とす**状態を自分で作っていた．
> > コマが 2 つ以上のときだけ列の作法を足した (規則の側から引いて書く) ．
> > **1 コマの依頼には足さない** — 掛からない検査を書いても守りようがない．

> [!warning] **応答時間は寸法では決まらない — 1 点から法則を書きかけた** (D166)．
> 実測は 16x16 が 80.4 秒 ・32x32 が **672 秒**で，これを «8.4 倍» と書いて
> **寸法に対する法則のように読んでいた**．次に測った **16x16 の 4 コマ**
> (画素数は 32x32 と同じ 1024) は，**作り直し 1 回を含めて 112 秒**である．
> 揃わないのは出力の量ではなく**思考の長さが依頼ごとに動く**からで，
> **同じ «変動するものを 1 回だけ測る» を D162 に続いてまた踏んだ**．
>
> | 依頼 | 画素 | 試行 | 時間 |
> | --- | ---: | ---: | ---: |
> | 16x16 ・1 コマ | 256 | 1 | 80.4 秒 |
> | **32x32 ・1 コマ** | 1024 | 1 | **672 秒** |
> | **16x16 ・4 コマ** | 1024 | **2** | **112 秒** |
>
> 言えるのは «**60 秒を超える応答が普通に出る**» ことだけで，これは D162 の
> «無音の上限 60 秒» を踏むのに十分である — **流にしたのは前提であって改善ではない**．

```sh
# **非対話シェルには .zshrc の export が届かない**ので明示的に渡す
KEY=$(sed -n '162p' ~/.zshrc | sed 's/^[^=]*=//' | tr -d '"'"'"' \r')
ANTHROPIC_API_KEY="$KEY" cargo run -p px -- gen prog out/chest.px.toml \
    --prompt "木の宝箱．正面から" \
    --palette 1a1c2c,566c86,8a6a4a,b13e53,f4f4f4 --size 16x16
cargo run -p px -- gen prog out/x.px.toml --prompt "宝箱" \
    --palette 1a1c2c,f4f4f4 --size 8x8 --dry-run   # 組み立てた依頼が出る (鍵は要らない)

# **切れた側の道を通す** (D165)．15 秒で戻り，上限は要求した値として報告される
ANTHROPIC_API_KEY="$KEY" cargo run -p px -- gen prog out/trunc.px.toml \
    --prompt "木の宝箱．正面から" \
    --palette 1a1c2c,566c86,8a6a4a,b13e53,f4f4f4 --size 16x16 --max-tokens 300
```

> [!warning] **生成の輪はプロンプトが間違っていても «モデルが下手» に見える．**
> 通らないときは `crates/px-gen/src/anthropic.rs` の `system_prompt` を
> `px_io::l0` のスキーマと突き合わせること (M6 でここを記憶で書いて 1 度外している)．
> **`--dry-run` が組み立てた依頼を出すようになった** (D162) ので，
> 鍵を使わずに送る側を読める．

#### 2. R22 — **閉じた．直線は守り，円板は守らないと決めた** (D169 ・D170)

**扱いは決まった**: 検出器は触らず (ルール 8 は advisory のまま助言する) ，
**画素を動かす `px smooth` の側だけが手を引く**．直線については実装済みで，
`is_digital_straight` が «走りが 2 種類 ・1 違い ・並びが均等» を再帰的に見る
(**閾値は 1 つも無い**) ．D32 とは**向きが逆**なので両立する — 抑制にしか
使わないので曲線を違反にする方向には働かない．

| | 前 | 後 |
| --- | ---: | ---: |
| **清書での書き換え** | 88 画素 ・17 / 58 枚 | **60 画素 ・13 / 58 枚** |
| 負例の捕捉 (**検出**) | 53 / 69 (76.8%) | **変わらず** |
| 実素材で動かした画素 | 104 | **89** |

**残った円板 (60 画素 ・13 枚) は D170 で «守らない» と決めた．**
上限を測った表と理由は上の 2026-08-15 の節にある．要点は 3 つ:
**上限が半分しかない (32 / 64)** ・**実素材では 105 谷中 1 しか効かない** ・
**円板の段を 1 つ崩した本物の欠陥が正しい円板の縁と同じ走り列を作る**．

> [!warning] **この «構造» の読みは半分外れていた．**
> 引き継ぎは «鳴るのはどれも 4 件 ・形はすべて `[2, 1, 2]`» と書いていたが，
> **`[2, 1, 2]` は 64 件中 40 件 (62.5%)** である (残りは `(3, 1, 2)` 12 ・
> `(3, 2, 3)` 8 ・`(5, 1, 2)` 4) ．**4 の倍数で出るところは合っていた**
> (90 度回転で移り合う) ．大きい円板だけを見た読みだった．

> [!note] **maximal digital straight segment 分解 (tangential cover) は書いていない．**
> D170 の上限測定は «窓の取り方を全部試す» 総当たりで，分解を実装しなくても
> **どんな認識器も超えられない側**を測れる．採らないと決めた以上，
> 分解を書く先が無い (D92 «書いていないものを黙らない» の作法で残す) ．

#### 3. D164 が残した設計判断 — **ルール 9 は入力の側から鳴りようがない**

書籍の言うミクセル (等倍の絵に 2 倍の領域が混ざる) では，**投票するのは 2 倍側
だけ**なので一致率がちょうど 1.0000 になる — 候補のスケールに 1 が無いためで，
**窓も閾値も効かない**．実素材 64 枚では既定の窓 32 で升が 1 つしか並ばないので，
`lint.csv` のルール 9 が 0 件なのは «検査して問題が無かった» ではない．

直すには «格子が無い» を «格子 1» として投票させる必要がある．統計的な推定器は
«測れなかった» (窓が $4s$ に足りない) と «測ったら 1 だった» を分けられないので，
**厳密なブロック判定** (`ingest::integer_block_size` が画像全体にしていること) を
窓ごとに掛ける形になり，**D37 (ミクセル検出と非一様格子の棄却は同一の推定器を
共有する) に触る**．入力が違う (`px lint` の PNG は劣化していない ・`px conform` の
入力は JPEG や補間を通る) ので**道具を分ける根拠は測定の側にある**が，
測っただけで止めてある．詳細は `docs/investigations/local-grid-window.md`．

#### 付録 C の一覧 — **5 件とも閉じた**

| # | 項目 | 状態 |
| --- | --- | --- |
| ~~1~~ | ~~ラン長の谷検出の誤検出~~ | **閉じた (D163)．誤検出はする — 原因は数え上げ** |
| ~~2~~ | ~~形の乱雑さの閾値~~ | 閉じた (D143) |
| ~~3~~ | ~~«軌跡» の定式化~~ | 閉じた (D139) |
| ~~4~~ | ~~局所格子推定の窓サイズ~~ | **閉じた (D164)．窓は $4s$ ・ルール 9 は構造的に鳴らない** |
| ~~5~~ | ~~`--method fast` が中間フレームとして使える確率~~ | **閉じた (D161)．47.5%** |

### 0b. **書かないと決めたもの** (蒸し返さないこと)

D92 の作法で «書いていない» と報告している．**同じ案を作り直さない．**

| 項目 | なぜ書かないか |
| --- | --- |
| **`px gen image`** (経路 A) | 8.1 «汎用拡散モデルの出力は非一様格子になることがあり，その場合は棄却する» — **成功率がモデル依存で上限が読めない** (D156) |
| **`px export godot`** | terrain set の «peering bit» と 8 近傍 bitmask の対応が未確認．**仕様を引いてから** (D111) |
| **RotSprite** | 公開仕様が無く，唯一の公開実装も «記述に曖昧さがある» と書いている．cleanEdge が測って効くと分かった以上 timebox を使う先が無い (D148) |
| **`px anim cycle` の `Rotate` 4 通り** | 回転は `px rotate` の仕事．ここに書くと回転の実装が 2 つになる (D118 ・D110) |
| **ディザの位相バリアントの交互配置** | 測ったら**問題を作る側**だった (D106) |
| **式評価器の `&&` `||` `!`** | 設計書 4.2 の «許すもの» に無い |

### 0c. **未達のまま閉じたもの ・確かめきれていないもの**

| 項目 | 中身 |
| --- | --- |
| ~~**M2 の B**~~ | **閉じた (D173)．42 / 50 (84.0%)** — D72 が «初期値のまま 1 通りしか試していない» と名指しした境界の峰の 2 つのつまみを掃いて届いた．実データ 2 枠は同等以上 |
| **R3 の残り** | **読み手は独立化した (D167)** — 仕様から書いた 2 つ目のパーサで突き合わせ，19 件すべて食い違い 0．**残るは素材**で，`aseprite-tests/` は `aseprite-io` 自身の fixtures 由来のまま．**人が Aseprite を操作して**作ったファイルを `testdata/aseprite/independent/` へ置く (何を作るかは同ディレクトリの README) |
| **M1 の «32x32 を描き切る時間»** | **未測定** (実際に描く作業なので人が測る) |
| **crates.io 公開** | 付録 B #2 は «安定動作を確かめてから» で閉じた．出すときは**ライブラリ 5 クレートから** — LGPL 連鎖が 0 件である |
### 0a. M3 (整形と検証) の内訳

| 完了条件 | 状態 |
| --- | --- |
| `LightingModel` / `LightSource` | **済** (照度の解析解 ・分岐ごとの $t$ 正規化) |
| `px shade` | **済** (CLI まで配線．`--light` で光源 5 型を指定できる) |
| **自己整合性テスト** (`shade` の出力が `lint` を通る) | **済．落ちたのはルール 3 で，ルール 3 を直した** (D78) |
| `px lint` ルール 13 (pillow shading) | **済**．閾値 0.85 を 3 群で決めた (D77) |
| `px smooth` | **済** (D80 ・D82) ．反転点は «続いた坂» で決める — **曲率場からは取れなかった** |
| `px aa` | **済** (D83) ．**冪等ではない** — 掛けるのは 1 度だけ |
| `px outline --style` | **済** (D84) ．5 分類 + `--selective` + `--background` ・既定は内側 |
| `px lint` 形 ・陰影系の残り 6 ルール (4 ・6 ・7 ・8 ・12 ・14) | **済** (D85 〜 D90) ．スコープの仕組み (`keyframe` は key / breakdown のフレームだけ) も入れた |
| lint 閾値の決定 | **済**．全ルールを正例 ・負例で決めた |
| **`--ao` の閾値 0.25** | **済 (D91)**．4 群で確かめた．**直したのは距離場の «画像の外» の扱い**で，閾値はそのままでよかった |
| **`px validate`** | **済 (D92)**．プロファイル + 6 項目 ・組み込み 5 種 ・TOML ・非ゼロ終了 |

> [!warning] **ルールを 1 つ足したら，その都度 `cargo test -p px` を回すこと**
> `crates/px/tests/` の 4 本が «自分の出力が自分の検査に落ちていないか» を見ている．
> **合成した形では捕まらない** — どれも実素材 61 〜 64 枚を全件通す形にしてある．
>
> | 試験 | 見ているもの |
> | --- | --- |
> | `shade_self_consistency.rs` | `shade` の出力 ・`shade` → `smooth` の出力 |
> | `smooth_real_art.rs` | 整形が壊さないこと (収束 ・面積 ・blocking) |
> | `aa_real_art.rs` | AA が壊さないこと (シルエット ・置きすぎ ・色数) |
> | `outline_real_art.rs` | 縁取りが壊さないこと (5 分類 ・冪等 ・色数) |
| **`compose_real_art.rs`** | **合成が色を作らないこと** (180 組の全画素) ・切り捨て 0 ・被覆 ・2 度目の一致 |
| **`direction_real_art.rs`** | **反転が色を変えないこと** ・反転が対合であること ・**反転で必ず鳴ること** (64 枚) ・再導出で必ず消えること ・真上の光では鳴らないこと |
| **`tileset_real_art.rs`** | **切って束ねて組み直すと戻ること** (61 枚 x 2 寸法 x 3 モード) ・2 度目の一致 ・**«向き» と «反転依存» を混ぜないこと** ・8x8 は測れないこと |
| **`autotile_consistency.rs`** | **47 と内訳** ・47 枚が全部違うこと ・自動ミラーが必ず鳴ること ・**測れない 7 枚が «対称» であること** ・**象限では測れずタイルなら測れること** |
| `px-core` の `autotile` 単体試験 | **偶数幅の反復では連結しないこと** ・**鏡像では必ず連結すること** ・ベタ塗りで誤検出 0 ・位相を揃えて描けば 0 |
>
> **落ちたら «どちらが正しいか» を先に決める** (D58 — 代理指標が理論実装を否定しない)．
> ルール 4 は `outline` の ・6 と 7 は `shade` の ・14 は `aa` の出力を直接見に行くので，
> 自分の出力に鳴る見込みが最も高い．

### 0a. M2 の校正 — **B は D173 で目標を超えた (42 / 50 ・84.0%)**

**2026-08-13 にテストセットを 1 度だけ適用して M2 の校正を閉じた．**
そのとき B は検証セットで 39 / 50 ・テストセットで 24 / 34 (70.6%) と届いていなかった．

> [!note] **D173 で届いた．** D72 が «残る的» として名指しした
> **境界の峰の拾い方 (非極大抑制の半径 ・エネルギーの下限)** は
> «初期値のまま 1 通りしか試していない» ままだった．掃いたら
> **検証 B 40 → 42 / 50 ・完全一致 74 → 75** で，A ・D ・正棄却は据え置き，
> **同梱の実データは完全一致 60 → 61**，**`local/` は完全に同一**である．
> 平らな面の中央 (抑制 0.625 ・下限 1.5) を採った．
>
> **記録の方が古かった** — 出荷の運転点で測ると B は 39 ではなく **40 / 50** だった．
>
> > [!warning] **テストセットでは確かめられない** (1 度使って閉じたため) ．
> > 根拠は «検証セット ＋ 実データ 2 枠» で，**テストセットの 24 / 34 は
> > 測り直せないまま**である．

**以前«届く道»として見つかっていた 3 つは，どれも実データ枠が代償を払う**ので
採らなかった (下表) ．**利用者判断で «実データの安全側» を採っている．**

| 道 | 検証セットの B | 実データの代償 |
| --- | --- | --- |
| 信頼度の下限 0.09 (D72) | 40 / 50 | `local/` 誤答 0 → 1 ・誤受理 2 → 3 |
| 曲線の肩代わり ・RMS 0.04 (D73) | 40 / 50 | 実データの誤り 4 → 8 件 |
| 曲線の肩代わり ・畳んだ形 0.01 (D76) | 40 / 50 | 実データの誤り 4 → 6 件 |

**採るなら 3 行目である** (代償が最も小さい) ．`GridParams::edge_fit_curve_residual` を
`Some(0.01)` にし，`replay --edge-stat folded` と同じ測り方を推定器へ入れれば届く．

### 以前の «あと 1 件» の内訳

| 区分 | 現状 | 目標 |
| --- | --- | --- |
| A (nearest ・整数倍) | **26 / 26 (100%)** | 95% — **満たした (D71)** |
| **B (bilinear ・bicubic)** | **39 / 50 (78.0%)** | **80% — あと 1 件** |
| C (lanczos) | 8 / 25 (32.0%) | 目標値を置かない |
| **D (誤答 + 誤受理)** | **9 / 300 (3.0%)** | **5% 以下 — 満たした (D69 ・D71)** |

**B が唯一の主戦場**である．`px-calib report` が区分をそのまま出す (以前は手で数えていた)．

> [!note] 2026-08-13 — **B 40 / 50 に届く道は見つかったが，実データ枠が払う** (D73)
> **曲線を «厳しい残差でだけ» 肩代わりさせる**と，検証セットは
> **完全一致 73 → 74 ・B 39 → 40 (目標 80% 達成) ・正棄却 192 と D 9 は据え置き**と
> 無償で届く．D71 が捨てた «まるごと肩代わり» との違いは許容だけで，
> 曲線で落ちていた真の $s$ の当てはめは残差 0.001 〜 0.032 と，落としたい側
> (0.14 〜 0.17) より**一桁良い**．
>
> **それでも採らない．** 実データ枠が 同梱 完全一致 60 → 61 ・誤答 2 → **3** ・
> 誤受理 0 → **1**，`local/` 誤受理 2 → **4** と，**正解 1 件に対し誤りが 4 件**増える．
>
> 漏れる 4 件は $\hat{s} = 8$ ・$14$ で信頼度 0.021 〜 0.074 と**大きい $\hat{s}$ に偏る**
> (下限は $\hat{s}$ で割るのでそこが最も弱い) ．**塞ぎ方は 3 方向とも潰した** —
> 残差の膝 (効かない) ・下限の形 $\mathrm{base}/\hat{s}^p$ (現行が唯一の解) ・
> 拾えた境界の割合 (真の $s$ 0.63 対 格子なし 0.72 で**逆を向いている**) ．
>
> 仕掛けは `GridParams::edge_fit_curve_residual` (既定 `None`) として残してある．
> **関門を 1 つ足したらここも測り直すこと** — 運転点は関門が変われば動く．

> [!warning] B の残り 1 件に **閾値の余地は無い** (D72 で本物の掃引で確かめた)
> $\varepsilon$ ・$\delta$ ・$\tau$ ・$\theta$ ・曲線を 162 通り掃いても，B の上限は
> 信頼度の下限ごとに **0.10 で 38 ・0.095 で 39 ・0.09 で 40** で，**現行の関門値が
> どの水準でも argmax** である．次の 1 件は構造を変えるしかない．
>
> 残る的は 3 つ．**どれも «族» を 1 つ外へ出る話である．**
>
> 1. **大きい $\hat{s}$ の低信頼度を落とす別の量．D73 で分かった律速はここである．**
>    曲線を «厳しい残差でだけ» 肩代わりさせると検証セットは無償で B 40 に届くのに，
>    $\hat{s} = 8$ ・$14$ ・信頼度 0.02 〜 0.07 の実データ 4 件が漏れるので採れない．
>    下限は $\hat{s}$ で割るのでそこが最も弱く，**形を変えても塞げない**
>    ($p$ と base を掃いて現行の $p = 1$ ・0.095 が唯一の解) ．D63 以来測り直して
>    いない «信頼度の定義そのもの» か，大きい $\hat{s}$ 専用の関門が要る
> 2. **境界の拾い方**．位相と境界が落としている B の残り 4 件は当てはめ自体が悪い
>    (傾き 0.05 〜 0.10 ・残差 0.14 〜 0.17) ．非極大抑制の窓 ・エネルギーの下限 ・
>    当てはめの重みはどれも**初期値のまま 1 通りしか試していない**
> 3. $\varepsilon$ が落としている B 1 件．**肩代わりは測って外れた** (下記)
>
> **1 と 2 は別の件を見ている** (1 は曲線で落ちた 4 件の露払い ・2 はもう 4 件) ．
> どちらか一方で B 40 に届く．検証セットの上限は B 41 / 50 である．

> [!note] 2026-08-13 — 実データ枠にも測る口を作った (D74) ．**次の仮説はここから出た**
> `recon --real` で実データ枠を合成データと同じ CSV に出せるようにした．
> 現行の誤り 4 件を候補ごとに解剖すると，こうなっている．
>
> | 枠 | ファイル | 種別 | $\hat{s}$ | 信頼度 | 半セル比 | **境界の残差** |
> | --- | --- | --- | --- | --- | --- | --- |
> | 同梱 | `other/035.png` | 正例 (誤答) | 4 | 0.027 | 1.15 | 0.027 / 0.024 |
> | 同梱 | `screenshot/005.png` | 正例 (誤答) | 4 | 0.057 | 1.19 | **0.45 / 0.41** |
> | `local/` | `other/neg-014.png` | 負例 (誤受理) | 5 | 0.031 | 1.55 | 0.054 / 0.054 |
> | `local/` | `screenshot/neg-008.png` | 負例 (誤受理) | 4 | 0.040 | 1.26 | **0.60 / 0.77** |
>
> **4 件のうち 2 件は境界がまったく直線に乗っていない** (通っている候補の典型は
> 0.01 〜 0.03) ．いまの境界の当てはめは «肩代わり» にしか使っておらず，
> **落とす側には一度も働いていない**．
>
> D71 は «AND で足す» を測って捨てたが，あれは**全 $\hat{s}$ 一律**だった
> (検証セットで完全一致 67 → 66 ・正棄却 190 → 187) ．記録には
> «傾き 0.02 ・$\theta$ を外す» 形なら**正棄却が 195 / 199 まで行く**とも書いてある —
> **落とす側としては優秀**である．次に測るのはこの条件付きの形である．
>
> **測った (D75) ．外れた．** `and` は肩代わりの構造ごと置き換えるので B が
> 25 〜 27 / 50 に崩れる．«肩代わりは残したまま酷い当てはめだけ落とす床» は
> 検証セットでは 0.60 が無償 (正棄却 +1 ・D −1) だが，**同梱の正例が 60 → 57** に
> 落ちる — 当てはめ残差の裾が素材で違う (合成の最大 0.543 に対し同梱は 90% 点 0.493 ・
> 最大 0.917) ．«両軸とも酷いときだけ» に変えると検証セットでは**完全に不活性**
> (合成のリサイズは等方) で，実データでは正解 1 件と誤受理 1 件の交換になり
> **B は動かない**．`replay --edge-drop-residual` に残置 — **D が律速になったら開ける**．

> [!warning] B の «帯の位相» 側は D71 で取り切った
> 以下は D70 時点の内訳である．**帯ずれが落としていた 7 件はセル境界の当てはめが
> 肩代わりした** (B 36 → 38 ・A 24 → 26) ．残る 12 件は測り直すこと — 律速は
> $\varepsilon$ と再構成検査へ移りつつある．
>
> 外している 13 件の内訳は **帯ずれ 7 ・曲線 3 ・信頼度 2 ・$\varepsilon$ 1**．
> $\theta$ を緩めると B は +2 になるが **A が −2** になる ($2 s_*$ が «閾値を満たす
> 最大の $s$» を奪う) ．$\theta$ ・$\tau$ ・比を 3 次元に掃いても，A $\ge$ 24 かつ
> D $\le$ 15 の範囲では **B は 37 / 50 が上限**だった．
>
> 許容の画素の下限は**もう «正棄却を壊す» ことはない** (棄却を引き受ける関門が 2 つ
> 増えたため) ．下限 3 px ・比 1.25 で検証セットは B 39 / 50 ・D 12 まで行く — が，
> **実データ枠が逆を向く** (完全一致 59 → 55 ・AI 出力 23 → 18 / 28) ので採らない．
> しかも下限 3 は $s \le 6$ で帯ずれの検査を無効化する (巡回距離の上限が $s/2$) ．
>
> **B を動かすには構造を変えるしかない．** 残る的は帯ずれが落としている 7 件だが，
> **帯の位相という «族» の中はすべて閉じた** — 7 件はどれも «3 本が揃って 1 本だけ
> 外れる» 形で，外れ 1 本を許すと $2 s_*$ も同じだけ得をして A が 24 → 22 ・D が
> 13 → 21 になる (中央絶対偏差も同様) ．外れ帯の位置は端 9 / 中 9 と**偶然どおり**で，
> «端を避ける» という直し方も無い．**失敗は argmin の雑音**である．

### 1. 位相の検査の余地は縮んだ — **関門を通った件は全部取れている**

`px-calib replay --gates` で数え直すと，**真の $s$ が全関門を通るのは 75 / 101**
で，そこから 73 件を取れている．落としているのは位相と境界が 23 件 ・再構成が 9 件 ・
$\varepsilon$ が 4 件 ・半セルずらしが 4 件 (重複あり) ．

**取り逃していた 2 件は D72 で信頼度の下限を 0.095 にして取り戻した** (どちらも B) ．
残る 1 件 (信頼度 0.0305 ・$\hat{s} = 3$) は，取り戻すと `local/` の誤答が
1 件増える — 同じ $\hat{s}$ ・信頼度 0.031 の負例と 0.0005 しか違わないためである．

**B を 1 件動かすには，また «族» を 1 つ外へ出る必要がある．** 閾値の余地は無い
(162 通りの本物の掃引で確かめた) ．

> [!warning] 以下は **D70 時点** の記録である — **D71 で運転点は動いた**
> **関門が変われば «運転点は動かない» も無効になる．** 実際 D71 で $\theta$ は
> 0.35 → 0.28 ・曲線は 0.16 → 0.18 へ動いた (肩代わりが «通す» 側を引き受けたので，
> 帯ずれは締める方が良くなった) ．**同じ掃引を «済んでいる» と読まないこと．**
>
> $\theta \in [0.25, 0.60]$ × 曲線の許容 $\in [0.08, 0.40]$ を 2 次元で掃いた．当時の現行
> (0.35 ・0.16) が平らな面 ($\theta \in [0.34, 0.37]$) の中央で，$\theta = 0.38$ の尾根
> (+0.4 ポイント) は**量子化の境目** ($s = 8$ の帯ずれ 3 が $3/8 = 0.375$ から通る)
> でしかなく，**実データ枠は同梱 ・`local/` とも 1 件も違わない**．
>
> **$\theta \ge 0.5$ は検査を外すのと同じである** — 帯ずれは巡回距離なので $s/2$ が
> 上限で，実測の最大がちょうど 0.5000 ・超える候補は 3938 件中 0 件だった．
> $\theta$ を緩める方向はここで尽きている．
>
> $\theta = 0.32$ は D66 の D (5% 以下) をちょうど満たす唯一の点である (15 / 300) ．
> 完全一致は検証で 66 → 62 ・実データ同梱で 58 → 54 に落ちる．**D が律速になったときの
> 選択肢**として `grid-calibration.md` に記録してある (**D71 以降 D は 9 / 300 なので，
> この選択肢はいま要らない**) ．

> [!warning] D71 が «まだ試していない» と書いた 2 つは **D72 で測って外れた**
> どちらも「案そのもの」ではなく「的の見立て」が外れている．
>
> - **曲線の «正規化のしかた» の周辺** — 分母を $(A - M) + \lambda A$ に一般化して
>   $\lambda$ を掃いたが，**既存の閾値の付け替えでしかなかった**
>   ($\lambda = 0.15$ ・曲線 0.18 と $\lambda = 0$ ・曲線 0.30 が件数まで完全に一致) ．
>   新しい統計ではないので `replay --curve-lambda` に残置し，既定は 0 のままである
> - **境界の当てはめに $\varepsilon$ ・再構成を肩代わりさせる** — 実装して測った
>   (`replay --edge-rescue eps|recon|contrast`) が，**B には 1 件も効かない**．
>   再構成と半セルずらしが落としているのは全部 C だからである (上の区分表) ．
>   再構成の肩代わりは replay 上で C を +2〜3 ・D 据え置きにするが，
>   **C に目標値は無い**ので `px-core` には入れていない
>
> 教訓: **的の大きさは «区分ごとに» 数える．** «$\varepsilon$ が 4 件 ・再構成が 9 件
> 落としている» は総数であって，B の的はそれぞれ 1 件 ・0 件だった．

### 2. 測る道具 — `px-calib replay` で掃引を回さずに関門を掛け替える

`recon` の CSV から `estimate_grid` を再現する口を**コマンドとして固めた** (D71) ．
以前は使い捨ての集計だったので，次のセッションからは `replay` を使う．

```sh
cargo run -p px-calib --release -- recon --include-resized   # 先に CSV を取る (5 秒)
cargo run -p px-calib --release -- replay --verify --gates   # 再現の食い違い ・落ちた関門
cargo run -p px-calib --release -- replay --edge-slope 0.01 0.0125 0.015 \
    --phase-tolerance 0.25 0.28 0.30 --top 20                # 総当たり (数千通りが 6 秒)
```

- `--gates` は **区分ごと (A ・B ・C) に数える** — «どの関門が B を落としているか» が
  要る (D72) ．**関門を通ったのに取り逃した件も 1 件ずつ理由つきで並べる**
  (信頼度で落ちたのか，選択規則が大きい $s$ を採ったのか，位相だけ外したのか)
- `--edge-rescue eps|recon|contrast` で**帯ずれ以外の肩代わり**を，
  `--curve-lambda` ・`--curve-axis` で**曲線の正規化のしかた**を掛け替えられる (D72)
- **`recon --real --dir testdata/grid-eval/real` で実データ枠も同じ CSV に出せる** (D74) ．
  `diagnose` は正解が分かっている件の**誤棄却しか見ない**ので，負例に何が起きたかは
  判定行 (「s=14 位相=(12,13) 信頼度 0.074」) からしか読めなかった —
  **D72 ・D73 の «塞げない» という結論は，候補ごとの統計を見ないまま出していた**
- **本物との食い違いは 2 / 300 件** (`--verify` が数える) ．`scale_candidates` が全 $s$ を
  素通しで評価するのに対し `estimate_grid` は候補を絞ってから全探索へ落ちる差で，
  再現の方が 1〜2 件甘く出る．**最終の数字は掃引で確かめる**
- > [!warning] **再現の甘さは関門を緩めるほど広がる** (D72)
  > 既定では B が +1 (再現 40 ・本物 39) だが，曲線を 0.18 → 0.30 へ緩めると
  > **+3 になる** (再現 41 ・本物 38) ．`replay` は «どの案を測るか» を選ぶ道具であって，
  > **採否は本物の掃引で決める**．緩める向きの案ほど再現を信じてはいけない
- **$\delta$ だけは掃けない** — 不一致率が $\delta$ の下で計算済みだからである．
  $\delta$ を変えるなら `recon` から回し直す
- `Gates::default()` は `GridParams::default()` から引く．**数値を書き写さない** —
  写経すると «再現が本物と揃っている» という `replay` の値打ちが黙って消える

**分離能で採否を決めない**という作法を守ったまま総当たりできる．

### 3. lint 閾値 — **素材待ちは解けた (D70)**

- 正例は `testdata/grid-eval/seeds/` (CC0 の実物 64 枚) をそのまま使う
- 負例は **`px-calib lint-gen` が作る** (良い絵に欠陥を 1 つだけ入れる．7 種 x 8 枚)
- 測り方: `px-calib lint --dir <PNG の置き場所>`．詳細は
  `docs/investigations/lint-calibration.md`

**良い絵に掛けたら blocking が鳴らない絵が 0 / 64 枚だった** — ルール 2 は原寸の
ドット絵を «格子崩れ» と呼び，ルール 3 は質感を «孤立ピクセル» と呼んでいた．
適用範囲と定義を直し，**ルール 2 は advisory へ落として 59 / 64 (92%)** になった．

> [!note] ルール 2 が advisory な理由
> きれいな拡大でも 26.7% で鳴り，**閾値では下がらない** — 推定器自身がそれだけ
> 棄却するためで，このルールは `conform` の再現率を超えられない．
> **超えられない検査で出力を止めない．**

未実装のルール (4 ・6 ・7 ・8 ・12 ・13 ・14 ・19 …) は M3 以降．実装するときに
`lint-gen` へ欠陥を足す．

### 4. M3 (整形と検証) へ

## 校正の道具 (`px-calib`)

```sh
# 合成データセット (元絵は CC0 の実物 64 件)．gitignore されているので作り直す
cargo run -p px-calib --release -- gen                 # 種 0 で再現する
cargo run -p px-calib --release -- sweep               # 検証 300 件を掃引
cargo run -p px-calib --release -- report              # 指標と運転点

# 測定の口 (推定の経路ではない．仮説を測ってから実装するために使う)
cargo run -p px-calib --release -- bands               # 帯ごとの再構成誤差と位相
cargo run -p px-calib --release -- confidence          # 信頼度の各項
cargo run -p px-calib --release -- recon               # 再構成統計の分離能
cargo run -p px-calib --release -- recon --include-resized   # 格子が無い件も混ぜる
cargo run -p px-calib --release -- diagnose            # 実データの誤棄却を 1 件ずつ解剖

# 掃引を回さずに関門を掛け替える (recon の CSV から estimate_grid を再現する)
cargo run -p px-calib --release -- recon --include-resized   # 先に CSV を取る
cargo run -p px-calib --release -- replay --verify --gates   # 再現の食い違いと落ちた関門
cargo run -p px-calib --release -- replay --edge-slope 0.01 0.0125 0.015 --phase-tolerance 0.25 0.28 0.30

# lint (良い絵 → 誤爆 ・負例 → 捕捉)
cargo run -p px-calib --release -- lint                     # CC0 の実物 64 枚
cargo run -p px-calib --release -- lint-gen                 # 負例を作る (8 種 x 8 枚)
cargo run -p px-calib --release -- pillow                   # ルール 13 の特徴量を 3 群で測る
cargo run -p px-calib --release -- jaggy                    # ジャギー検出を良い絵に掛けて数える
cargo run -p px-calib --release -- jaggy --apply            # px smooth を実際に掛けて壊れないか見る
cargo run -p px-calib --release -- jaggy-truth              # 真値のある場面 (円板 ・階段) で誤検出を測る
cargo run -p px-calib --release -- mixel-exact              # ルール 9 を厳密判定に替えた上限 (D172)
cargo run -p px-calib --release -- jaggy-seam               # 継ぎ目の例外の «上限» (16 通り．D170 で «採らない» を決めた)
cargo run -p px-calib --release -- mixel                    # 局所推定の窓 (真値のある一様 / ミクセル)
cargo run -p px-calib --release -- mixel --dump out/mixel   # 場面を PNG で出して px lint / px conform に食わせる
cargo run -p px-calib --release -- aa                       # px aa を良い絵に掛けて壊れないか見る
cargo run -p px-calib --release -- outline                  # px outline を 5 分類とも掛けて見る
cargo run -p px-calib --release -- flip                     # ルール 7 の閾値 (px shade の出力と左右反転)
cargo run -p px-calib --release -- ao                       # --ao の閾値 (凸 ・凹 ・実素材 ・画面いっぱい)
cargo run -p px-calib --release -- compose                  # 合成の余白 ・併合したパレット ・実際に合成した結果
cargo run -p px-calib --release -- compose --clip           # 切る側も測る (何画素捨てるか)
cargo run -p px-calib --release -- direction                # 方向展開 (2 群 x 3 段階の一致度)
cargo run -p px-calib --release -- tileset                  # タイル (3 モードの削減率 ・測れる割合)
cargo run -p px-calib --release -- dither                   # ディザの位相 (設計書 4.3 の主張の確認)
cargo run -p px-calib --release -- tween                    # 中割り (真値のある平行移動 ・余白 ・トポロジー)
cargo run -p px-calib --release -- smear                    # おばけ (union ・掃引 ・重心を取り除いた掃引 ・刻み幅)
cargo run -p px-calib --release -- extrapolate              # 外挿 (真値のある平行移動 ・切れた件数)
cargo run -p px-calib --release -- squash                   # 体積保存 (2 通りの決め方 x 画布を広げる / 広げない)
cargo run -p px-calib --release -- subpixel                 # サブピクセル (接線法 2 通り ・高速法 ・移動率の効き)
cargo run -p px-calib --release -- afterimage               # 残像 (どれくらい動けば見えるか)
cargo run -p px-calib --release -- lint --shadow-hue 1 3 5  # ルール 6 の色相差 (正例と負例を同時に)
cargo run -p px-calib --release -- lint --outline-interior 0.02 0.05 0.1   # ルール 4 の門
cargo run -p px-calib --release -- lint --dir testdata/lint-cases/negative
cargo run -p px-calib --release -- lint --dir grid-eval --dataset --grid-like-ratio 0.05

# 実データ枠 (関門を切り替えて «どの変更が効いたか» を分けて見られる)
cargo run -p px-calib --release -- real
cargo run -p px-calib --release -- real --phase-tolerance 0.25 --phase-agreement 1.01 --allow-unmeasurable
cargo run -p px-calib --release -- real --dir testdata/grid-eval/real/local
```

- **検証セットで決め，テストセットでの再調整はしない**
- 採点の定義は掃引 CSV からやり直せる (`Row::outcome_at` は行の内容から毎回求める) ．
  **$\mathrm{min\_confidence}$ は掃引をやり直さずに掃ける** — 掃引は 0 で回している
- **リサイズありの件の正解は「棄却」である** ($\mathrm{round}(s \cdot r)$ は正解にしない)
- 選ぶ指標は**マクロ平均**．素の正解率だと「ほぼ全部棄却」が勝つ (格子なしが 2/3)
- **掃引の既定の $\varepsilon$ の格子は «絶対値» 時代のもの** (2e-4 〜 2e-2) ．
  いまの既定は «画像分散に対する割合» なので，`--epsilon` を明示しないと
  運転点が格子の外に出る

### 校正した既定値

$\varepsilon = 0.15$ (**画像分散に対する割合**) ・$\delta = 0.15$ ・$\tau = 0.05$ ・
帯 4 (**足りなければ 3 ・2 と減らす**) ・許容 $\theta = 0.28$ ・許容の下限 0 画素 ・
**曲線の食い違い 0.18** ・**半セルずらしの比 1.12** ・**測れない候補は棄却** ・
位相は整数 (副画素は既定で使わない) ・帯あたり最小セル数 2 ・
$\mathrm{min\_confidence} = 0.095$ (**$\hat{s}$ で割って使う**．D72 で 0.10 から) ・
**セル境界の当てはめ 2 階差分 ・傾き 0.0125 ・残差 0.15 ・境界 4 本以上**
(**帯ずれだけを肩代わりする**)

### 成績 (D66 の区分)

| 区分 | 現状 | 目標 |
| --- | --- | --- |
| A (nearest ・整数倍) | **26 / 26 (100%)** | 95% — **満たした** |
| **B (bilinear ・bicubic)** | **39 / 50 (78.0%)** | 80% — あと 1 件 |
| C (lanczos) | 8 / 25 (32.0%) | 目標値を置かない |
| **D (誤答 + 誤受理)** | **9 / 300 (3.0%)** | **5% 以下 — 満たした** |

マクロ平均 **84.4%** ・完全一致 73 / 101 ・正棄却 192 / 199．
**D71 で A が 92.3% → 100% ・D が 4.0% → 3.0%，D72 で B が 76% → 78%．**

実データ枠 (別枠) は同梱 148 件が完全一致 **60** ・誤答 2 ・誤受理 0，`local/` 92 件が
完全一致 8 ・誤答 0 ・誤受理 2 である (**D72 の前後で 1 件も動かない**) ．

> [!warning] `px-calib report` の «選んだ閾値» は既定値と食い違う
> 自動の選び方はマクロ平均だけなので $\mathrm{min\_confidence} = 0.09$ を選ぶ
> (B 40 / 50 で目標を満たす) ．**既定を 0.095 にしているのは `local/` 92 件で
> 誤答 0 → 1 ・誤受理 2 → 3 と後退するから**である．`report` は既定値での区分も
> 並べて出すので，**マクロ平均だけを見て書き換えないこと** (`grid.rs` の doc を読む) ．

### 素材の状況 (格子推定の分はすべて調達済み)

| 用途 | 状態 |
| --- | --- |
| 合成データの種 | 64 件 (CC0．Kenney 16x16 が 32 ・Dungeon Crawl 32x32 が 32) ．**実データ枠とは重ならない** |
| 実データ 同梱 | 148 件 (正例 94 / 負例 54) |
| 実データ `local/` | 92 件 (正例 23 / 負例 69) ．再配布不可なので追跡しない |
| lint 正例 | `grid-eval/seeds/` をそのまま使う (CC0 の実物 64 枚) |
| lint 負例 | **`px-calib lint-gen` で生成** (**13 種 x 8 枚**．追跡している) |

## 落とし穴

実装中に踏んだもの．**同じ穴に落ちないこと．**

| 落とし穴 | 対処 |
| --- | --- |
| `Turn(chain)` をラン長の最小値で決める | 単発ジャギーを毎回見逃す．**G2 の曲率場から求める** (D60) |
| 単調区間を符号の反転だけで分ける | L 字の角が 1 本になる．**主軸に直交する歩みが 2 歩続いたら区切る** (D61) |
| `.hex` でアルファ 0 の色を飛ばす | 以降の添字が全部ずれる．**透明も 1 行として書く** |
| 局所格子推定で平坦な窓を投票させる | 「窓に収まる最大の $s$」が選ばれる．**信頼度 0 は投票しない** |
| `Srgb` へ変換してから成分を切り詰める | 色相がずれる．**`clip_to_gamut` で色相を保つ** |
| ディザ検出の入れ替わり率を 0.5 超に置く | ランダムディザを 1 件も拾えない (期待値が $2p(1-p) \le 0.5$) |
| 領域ラベリングを 8 連結にする | 市松が 1 領域になりディザ検出が壊れる．**4 連結** |
| 帯ごとの位相を帯の左端基準で測る | 帯ごとに違う物差しになる．**画像の原点を基準に測る** |
| 帯の実験で再構成検査を切る ($\tau = 1$) | 過大推定が野放しになる．問いは「今の検査が通した答えを選り分けられるか」である |
| 信頼度の対照群に小さい $s$ を入れる | $\bar{V}(s)$ は $s$ とともに単調に増えるのでマージンが負に潰れる (D63) |
| 校正の成績を素の正解率で選ぶ | 「ほぼ全部棄却」が 1 位になる．**マクロ平均で選ぶ** |
| 試験の一時ファイルを全試験で共有する | 稀に落ちる．**試験ごとにディレクトリを分ける** |
| **エラー文を「落ち方」と読み違える** | `real` の「信頼度が下限を下回った」は**最終的な答え**の話で，真の $s$ を何が落としたかではない．**`px-calib diagnose` で見る** |
| **落ち方を 1 つだけ数える** | 2 つの関門が同時に落としている件が先頭の関門に付け替わり，**犯人を取り違える**．`diagnose` の `failed_gates` 欄を見る |
| **関門を単独で測る** | 前の関門が落としてくれる相手まで背負わせて評価してしまう．真の $s$ が既に消えている件では，後ろの関門を何に取り替えても成績は動かない |
| **分離能の高さで統計を採る** | 均衡正解率 87.1% の統計が関門としては 15 / 101 だった．**選択規則を回して完全一致数で見る** |
| **測る口が «飛ばした件» を数えない** | `let Ok(..) else { continue }` で失敗を落としていると，**道具が全件で落ちていても «測れた件だけ» の表が出る**．透明の宣言が無い絵で `squash` が全滅していたのに気付かなかった (D128) |
| **読む側だけ拡張子を見る** | `.px.toml` に書いたつもりが中身は `.aseprite` になり，次の道具で «UTF-8 でない» と落ちる．**書く側も `save_frames` に寄せる** (D127) |
| **設計書の別々の節が矛盾していないか見ない** | 6.11 の掃引は 6.9 の包含定理から «union と同じ集合» になる．**節をまたいで代数を突き合わせる** (D120) |
| **«使った色が増えないこと» を不変条件にする** | 中間色を置くとは «パレットの中のまだ使っていない色を使い始める» ことなので**増えるのが正しい**．守るべきは «パレットの外へ出ないこと» (D124) |
| **clap の «id» で引数を引く** | `--part` の id は `parts` である．**利用者が打つ名前 (長い名前) で引く** (D130) |
| **宣言した出力だけをキャッシュに貯める** | 道具が隣に書くファイル (`.hex` など) が戻らず，**«当たり» と言って壊れた木を作る** (D132) |
| **キーからパスをまるごと外す** | 出力名が中身に入る道具があるので取り違える．**相対パスだけ混ぜる** (D131) |
| **CSV の行を `[vars]` の列に開く** | 行の «組» が消えて直積になる．**行は書き下す** (D133) |
| **飽和した統計に線を引こうとする** | 帯ずれは落としたい候補で $\lfloor s/2 \rfloor$ に張り付く (巡回距離の上限) ．**どこで切っても分かれない** — 統計の «形» を先に見る (D68) |
| **«掃引は済んでいる» という記録を，関門を変えた後も信じる** | D70 の記録は «$\theta$ の運転点は動かない» と書いていたが，D71 で 0.35 → 0.28 へ動いた．**掃引の結論には «どの関門の下で» を必ず添え，関門を足したら読み直す** |
| **関門だけ変えて閾値を据え置く** | 関門を変えれば通ってくる相手が変わる．$\varepsilon$ ・$\delta$ ・$\tau$ は**組で選び直す** — 実測で +2.3 ポイントを取り逃す |
| **CLI の既定値に数値を書き写す** | 校正値と黙って食い違う (実際 $\varepsilon$ が 0.02 対 0.2) ．**`GridParams::default()` から引く** |
| 正解の無い件と「格子が無い件」を同じ `unknown` にする | 誤受理が見えない．**`no_grid` を立てて棄却が正解として採点する** |
| 「入力が拡大済みか」を周期推定や元絵復元で判定する | 短い軸では当てずっぽうになる．**$k \times k$ ブロックの一様性を厳密に見る** |
| 負例を縮小して保存する | 補間が「格子が無い」という性質を触る．**中央を切り出す**．面積は中立ではない |
| `--force-period` に実際のブロックの約数を指定する | $s$ と $2s$ が両方成立し**正解が一意に決まらない**．拒否する |
| `ingest` の連番を毎回 0 から振る | 同じ区分へ 2 回取り込むと 1 回目を黙って上書きする (**24 件消した**) |
| 旗で既定値を潰す (`--normalize-epsilon`) | 旗が無いと `false` になり**既定と違う設定で測ってしまう**．`Option<bool>` にする |
| ダウンロード素材をそのまま読ませる | `com.apple.quarantine` が付いていると EPERM で落ちる．**`xattr -cr` してから渡す** |
| **距離場の勾配をそのまま面の法線に使う** | 符号付き距離場は内側ほど大きいので $\nabla d$ は内側を指す．**光と影が丸ごと裏返る** (M3) |
| **稜線を «近傍の法線の平均» で埋める** | 稜線の近傍は互いに向かい合う．円板の中心では**足すと零ベクトル**になり，埋めたい真ん中で失敗する．設計書どおり**コピー**する (M3) |
| **ラスタライズした形の曲率をそのまま使う** | 縁の階段が高周波でラプラシアンを支配する．凸な円板でも 5% 点が最小値に張り付き，凹んだ形と区別が付かない．**均してから取る** (M3) |
| **落とした件数を «総数» で数えて的を選ぶ** | $\varepsilon$ 4 件 ・再構成 9 件と見えていたが，**B の的はそれぞれ 1 件 ・0 件**だった (残りは C) ．未達の区分は 1 つなので，**区分ごとに数えてから案を選ぶ** (D72) |
| **閾値の掃引の «格子» を疑わない** | 信頼度の格子が 0.08 の次に 0.10 と粗く，**間が測れていなかった**．下限は $\hat{s}$ で割るので $\hat{s} = 2 \ldots 3$ では 0.005 の差が 0.002 の差になる．«掃いたが動かない» の前に **刻みが運転点の周りで足りているか**を見る (D72) |
| **`replay` の数字で採否を決める** | 再現は本物より甘く，**関門を緩めるほど差が開く** (既定 +1 ・曲線を緩めると +3) ．`replay` は案を選ぶ道具で，**採否は本物の掃引** (D72) |
| **統計の «形» を変えれば新しい量になると思う** | 曲線の正規化を $(A - M) + \lambda A$ に一般化したが，**既存の閾値の付け替えでしかなかった** ($\lambda$ 0.15 ・曲線 0.18 と $\lambda$ 0 ・曲線 0.30 が件数まで一致) ．**掛け替えた 2 点が同じ答えを出すかを先に見る** (D72) |
| **通す側の問題を «関門を足して» 直そうとする** | AND は候補を減らすことしかできない．真の $s$ が関門で消えているなら，足すのではなく**肩代わりさせる** (D71 で 200 通り掃いて 1 つも超えなかった) |
| **肩代わりを «まとめて» やる** | 帯ずれと曲線は役目が違う (曲線は D68 で «棄却を引き受ける» ために入れた) ．まるごと肩代わりすると，取り戻した完全一致と同じだけ誤受理が戻る |
| **境界の位置を «位置から» 添字付けする** | 非整数の周期でずれが溜まり，途中で添字が 1 つ飛ぶ．**隣どうしの間隔から積む** — 1 本の飛びが 1 か所にしか効かなくなる |
| **1 階差分で境界の峰を拾う** | bilinear はセル中心の間を直線で結ぶので 1 階差分が区間ごとに平らになり，頂点が定まらない (真の $s$ の間隔のずれが 90% 点で 0.170) ．**2 階差分で拾う** |
| **同じ «向き» の関門ばかり足す** | $\varepsilon$ ・再構成 ・帯ずれ ・曲線はすべて «セルの中が揃っているか» を見ており，滑らかな絵では全部が真になる．**格子が «在る» ことを確かめる関門が要る** (D69) |
| **捨てた量を «用途が違っても» 捨てたままにする** | 半セルずらしは «$s_*$ と $2 s_*$ を分ける» 用途で捨てたが，«滑らかな絵を落とす» 用途では効いた．**分けたい 2 群が変わったら測り直す** |
| **自己整合性を «合成した形» だけで見る** | 円板 ・胴体 ・谷折り ・箱では blocking が 1 件も出ないが，**実素材のシルエットでは 8 件出た** (光へ正対した 1 画素) ．滑らかな形は «最上段が 1 画素だけ立つ» 場面を作れない．**実素材を通す** (M3) |
| **試験の素材を間引く** | 落ちていた 2 枚は 64 枚中 31 ・58 番目で，**8 枚ごとに拾うとどちらも入らない**．間引いた試験は «通った» と言うだけの番人になる．全件でも 1 秒未満だった (M3) |
| **`Palette::nearest` で透明画素の色を引く** | この関数は**透明色を色距離の対象から外す**ので，アルファ 0 に引くと近くの不透明色が返る．校正の測定が背景を «暗い面» にした絵で lint を掛けていた (D79) |
| **輪郭追跡の停止条件を «開始点へ戻ったら» で書く** | 幅 1 画素の部分を持つ形で**永久に止まらない** (最小の例は横に並んだ 2 画素) ．**最初の 2 点の並びがもう一度現れたら止める** (D82) |
| **上限で打ち切った結果をそのまま返す** | 打ち切られた輪郭が «正しい輪郭» として下流へ流れ，**ラン数が 5 倍に膨らんだ状態で誤検出率を測っていた**．率が低く見えるときは**分母を疑う** (D82) |
| **設計書が指定した特徴量が «取れる» と思い込む** | D60 の «曲率場から反転点を求める» は，単調区間の中では符号が反転しないので**1 度も働いていなかった**．規則を入れたら «実際に発火するか» を測る (D80) |
| **付ける道具と外す道具で «同じもの» の定義がずれる** | AA を端から 35% / 65% に置くと，中点から 0.05 以内しか見ない除去では外せない．**往復の試験を書くと落ちる** (D83) |
| **冪等性を «窓を広げて» 買おうとする** | 半径 1 → 2 → 3 で置いた画素が 636 → 98 → 17 ・冪等でない絵が 29 → 3 → 0 枚．**冪等になる頃には何も塗らない**．諦めて «1 度だけ掛ける» と言う方が正直である (D83) |
| **`f32::signum(0.0)` が 0 を返すと思う** | `1.0` が返る．«向きが無い» 軸で 1 歩ずれ，斜めに探して隣の画素を拾う (D84) |
| **色を «1 画素ずつ» 決める** | 内側の色ごとに違う色ができ，**1 度しか使わない色**が生まれる — ルール 3 が «孤立ピクセル» と呼ぶ形になる．**全部集めてからまとめて決める** (D84) |
| **色数の予算を «作った数» に掛ける** | 2 度目は上位が既にあるぶん予算が余り，**次の候補を作る**．掛けるたびに色が増える．予算は «上位いくつを見るか» に掛ける (D84) |
| **lint の閾値を負例から先に決める** | 良い絵に掛ければ**負例を 1 枚も作らずに**誤りが出ることがある (blocking が 64 枚中 64 枚で鳴っていた) ．**正例が先** |
| **負例が鳴らないのを «ルールが緩い» と読む** | ルール 10 の 3 件は**すべて生成の穴**だった (欠陥が検出窓に届いていない) ．閾値を動かす前に «見逃しか作り方か» を分ける |
| **負例を «壊しすぎる»** | 境界画素の 40% を隣の色と入れ替えると，ラン長がすべて 1 になって**谷そのものが消える** — 負例の方が良い絵より静かになる．**欠陥は «検出器の相手» の形に作る**．3 通り作り直して初めて成立した (D85) |
| **合成した形だけで «閾値が高すぎる» と読む** | 環境遮蔽を凸 ・凹の合成形だけで測ると，どちらも画像の端に触れないので «画像の外» の誤りが出ない．**実素材を混ぜ，画面いっぱいの絵とシルエットのある絵を分けて**数えて初めて «26 枚が全部 25.0%» という判で押した形が見えた (D91) |
| **距離場に «画像の外は空» と言わせたまま端の絵を測る** | シルエットが端まで届いている絵では端がまるごと窪みになる．外は «空» ではなく **«分からない»** — マスクを複製して余白を付ける (D91) |
| **advisory のルールに自己整合性の試験を書かない** | 実素材の試験はどれも «blocking が増えていないか» を見ている．**advisory は素通りする** — ルール 14 が `px shade` の出力で鳴っていたのは，端から端まで CLI で通して初めて分かった (D88) |
| **負例を «自分の道具» で作る** | `px aa` を過剰な設定で 4 巡掛けても，角にしか置かないので縁が埋まらない (3.9 〜 10.2% で良い絵の 90% 点を下回る) ．**道具の «やらないこと» が負例の限界になる** — 縁を全部ぼかす形に作り直した (D88) |
| **画像の端を «シルエットの外» に数える** | 縁取りは透明に対して描くものである．端を外に数えると，画面いっぱいのタイルの端の帯が «縁取り» になり 1 枚で 30 件鳴った (D87) |
| **面積の上限がある判定をそのまま «割合» に使う** | `px clean --remove-aa` の 16 画素の上限は «消してよいか» の安全弁であって «中間色か» の定義ではない．割合に使うと **AA が多いほど数が減る** (D88) |
| **点光源を «向き» として扱う** | 点 ・線 ・面の光源では $\ell$ が画素ごとに違う．絵全体で平均した勾配と突き合わせると，`px shade --preset night` が 64 枚すべてで一致度 $-0.54$ 〜 $-1.0$ になった — 平行光源とまったく別の分布である (D89) |
| **«逆を向いていたら鳴らす» で反転を捕まえようとする** | 既定の光源は斜めなので，左右反転で変わるのは $x$ 成分だけ．一致度は $-1$ ではなく 0 付近になり，$< 0$ では 95 / 256 しか捕まらない．**«合っていないと鳴らす»** でなければならない (D89) |
| **キャンバスの «透明添字» をパレットのアルファだと思う** | `.aseprite` の透明添字は «透明として扱う添字» の**宣言**であって，その色が本当にアルファ 0 とは限らない．信じたまま L0 へ書くと，**添字 0 が実色の素材で往復 12006 画素が変わる**．**パレットのアルファで決める** (D109) |
| **設計書の «だから» を確かめずに処方だけ実装する** | «タイルの幅は必ず偶数なので同一タイルを並べるとディザが連結する» は**逆だった** (偶数幅の反復は 0 / 16) ．処方 (位相バリアントの交互配置) をそのまま実装すると，**合っている継ぎ目を壊す** (0 → 16) ．**根拠の側を先に測る** (D105 ・D106) |
| **«測れない» を 1 つの理由で報告する** | ルール 7 が `None` を返す理由は «標本が足りない» と «平均した勾配が 0 (対称)» の 2 つある．autotile の 7 枚を «画素が 64 未満» と報告していたが，16x16 全面不透明の標本は 196 で**標本不足では決してない**．**«測れない» の理由まで分ける** (D104) |
| **対称を «左右» だけだと思う** | 勾配を打ち消すのは «向きを反転させる対称» すべて — 左右 ・上下 ・180 度回転．`0x5f` は 180 度回転で重なるので，«測れない ⇒ 左右対称» と書いた試験が落ちた (D104) |
| **«変換が要った» を «束ねた» と数える** | 正規形はバイト列最小なので，**1 度しか現れないタイルでも**元の向きと違えば旗が立つ．«束ねた枚数» として数えると，**削減率 0% なのに 101 升**という辻褄の合わない数が出る．数えるのは «同じタイルを 2 通り以上の向きで使っている» 群だけ (D99) |
| **検査が «鳴らない» のを素材の性質と切り離して読む** | タイル 16 画素では反転に頼る升が実素材に 1 つも無く，8 画素では測定下限に構造的に届かない．**素材が検査を働かせられない**だけで，検査が効いている証拠にはならない．**働くことは組んだ場合で別に確かめる** (D100) |
| **«推定して検査する» を «別の絵を検査する» と混ぜる** | 同じ絵を推定して検査するのは同語反復である (D89) が，**元の絵から向きを取り反転した絵を検査する**のは同語反復ではない．一方，宣言を素材と無関係に決めると**正例が先に鳴って負例の効果が測れない** — 実素材はプリセットの既定光源で 82% が元の絵の時点で鳴った (D96) |
| **検査の «取りこぼし» を性能の問題と読む** | 反転で裏返るのは明度勾配の $x$ 成分だけなので，$\lvert \ell_x \rvert \le 0.474$ の光源では反転しても矛盾が起きない．**見逃しではなく «矛盾が無い»** である．境目は閾値から代数的に決まるので**校正の対象ではない** (D96 ・D92 と同じ性質) |
| **«直る» 手段を既定にする前に «何を失うか» を測らない** | 反転後の陰影再導出はルール 7 を必ず消す (両群とも 0 件) が，**手描きの絵は中央 100% の画素が書き換わる**．直ることと既定にしてよいことは別である (D97) |
| **同じ測定を «前に出した答え» のまま使い回す** | 実素材が画布の縁に接しているという同じ事実から，`px outline` は «内側に描く» ・`px compose` は «画布を広げる» と**逆の結論**になる．縁取りは絵を書き換える道具 ・合成は絵を組む道具だからである．**測定を引くときは «その道具にとって画布は入力か結果か» を先に言う** (D93) |
| **併合したパレットへ «全項目» を写す** | 併合は使っている色しか集めない (未使用色まで入れると 256 色を使い切る) ので，全項目を写すと**未使用の色が «併合先に無い» と言って落ちる**．写すのは «画素が実際に指している添字» だけ．逆に**パレットの範囲外の添字はエラーにする** — 黙って透明にすると元の絵の壊れが «合成が消した» ように見える (D94) |
| **展開のある指定を «展開 1 つずつ» で検査する** | `--part-delay` の鍵は `--for-each` の展開ごとにパーツ名が変わる (`cap_red` ・`cap_green`) ．展開ごとに «当たらなければ誤り» と言うと**正しい指定が必ず片方で落ちる**．**どの展開でも 1 度も当たらなかったものだけ**を誤りとする．展開が 1 通りしかない単体試験では出ない (D95) |
| 帯の曲線を判定と診断で別々に作る | 数字を並べて比べられなくなる．**`band_curves` 1 か所にまとめる** (作り直すと推定の費用も倍になる) |

## 決まっている作法

- 日本語は読点 `，` 句点 `．`
- 閾値には**暫定である旨を doc コメントに書く**．根拠が無い数字を黙って置かない
- 決定論性の 3 規則 (設計書 6.15) を守る．同点には必ずタイブレークを付ける
- `testdata/` は CC0 原則．**MIT は著作権表示を同梱すれば受け入れる** (例外を
  `testdata/SOURCES.md` に明記済み) ．CC-BY / CC-BY-SA / GPL は除外．
  再配布できないものは `grid-eval/real/local/` (追跡しない)
- このリポジトリの `.md` に「generated by Claude Code」フッタは付けない
  (Obsidian vault 側は付ける)
- **測ってから直す．** 設計や閾値を変える前に必ず測る — D68 では «切り方» の
  4 方向をオラクルで潰してから統計に手を付けた
- **データセットが見ていない場面を代償にしない．** 軸のまとめ方を max から平均にすると
  検証セットでは 2 件得をするが，評価データのリサイズが等方なので «片方の軸だけが
  非整数» という入力が 1 件も無い — 採らなかった
