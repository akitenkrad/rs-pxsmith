# 引き継ぎ

セッションをまたぐときの出発点．**まずこれを読み，次に `docs/status.md` を読む．**

- 更新: 2026-08-12
- 仕様: Obsidian の `設計書/ドット絵CLI-Rust/` — `設計書.md` / `実装計画書.md` / `開発ノート.md`
- **`開発ノート.md` に「実装して初めて分かったこと」が全部入っている．** 設計書だけ読むと
  同じ穴に落ちる

## いまの状態

| 項目 | 値 |
| --- | --- |
| フェーズ | M0 ・M1 ・M1a 完了．**M2 は マクロ平均 80.7%**．D66 の D (黙って誤答しない率) は**満たした** |
| テスト | 441 件すべて通過 |
| 品質 | `cargo fmt --all --check` と `cargo clippy --workspace --all-targets -- -D warnings` がクリーン |
| クレート | `px-core` / `px-io` / `px-view` / `px-macro` / `px-lint` / `px` / `px-calib` |
| ブランチ | `main` |

> [!note] 2026-08-12 の変更 — 位相の検査を «曲線どうしの比較» で作り直す (D68)
> **帯ずれ (帯ごとの argmin の食い違い) は飽和していた．** 落としたい候補の中央値は
> どの $s$ でも $\lfloor s/2 \rfloor$ — 巡回距離の**上限**である．argmin が無相関に
> なれば必ずこの値が出るので，補間で滲んだ本物の格子も同じ値を取る．
>
> 先に «切り方» を疑い，**件ごとの閾値を許した上限 (オラクル) で方向を潰した**．
> $s$ ごとに変えてよくても +1.3 ・**補間ごとに変えてよくても +0.3** — 画像の性質に
> 応じて許容を動かす案は，どんなに巧くやっても届かない．
>
> そこで argmin を取らずに**位相曲線をそのまま突き合わせる**量を足した．
>
> | 検証セット 300 件 | 完全一致 | 正棄却 | マクロ |
> | --- | --- | --- | --- |
> | 旧 ($\theta = 0.25$) | 59 / 101 | 182 / 199 | 74.9% |
> | + 測れない候補を棄却 | 60 / 101 | 182 / 199 | 75.4% |
> | $\theta = 0.35$ ・**曲線なし** | 62 / 101 | **151 / 199** | 68.6% |
> | + 曲線 0.16 | 62 / 101 | 183 / 199 | 76.7% |
> | **+ 閾値を選び直す** | **66 / 101** | **185 / 199** | **79.2%** |
>
> 3 行目が対照である — **曲線は «帯ずれを緩めた分の棄却» を引き受けている**．
> 実データ (同梱 148 件) でも同じで，$\theta$ だけ緩めると誤答が 4 → 20 件に増える．

> [!note] 2026-08-12 の変更 — 関門を変えたら閾値も選び直す
> $\varepsilon$ ・$\delta$ ・$\tau$ を $\theta$ ・曲線の許容と**同時に** 486 通り掃引した．
> **関門で +1.8 ・閾値で +2.3 とほぼ足し算**になる (片方だけ測ると取り逃す) ．
>
> | | 旧閾値 | 新閾値 ($\varepsilon$ 0.15 ・$\delta$ 0.15 ・$\tau$ 0.05) |
> | --- | --- | --- |
> | 旧関門 | 74.9% | 76.9% |
> | **新関門** | 76.7% | **79.2%** |
>
> **実データ枠も同じ向きに動いた** (同梱 完全一致 48 → 58 ・`local/` 6 → 8) ．
> 誤答は 4 件のまま ・誤受理が 1 → 2 件．合成と実データが揃うのは D67 に続き 2 度目．

> [!note] 2026-08-12 の変更 — 格子が «在る» ことを確かめる関門 (D69)
> 誤受理 14 件を解剖すると **7 件が全関門を «余裕ゼロ» で通っていた** (帯ずれ 0.000 ・
> 曲線 0.000 ・不一致率 0.000) ．$\hat{s} = 2 \ldots 4$ の縮退で，**閾値をどう締めても
> 関門では触れない**．原因は関門の «向き» で，$\varepsilon$ ・再構成 ・帯ずれ ・曲線の
> すべてが «セルの中が揃っているか» を見ており，滑らかな絵ではどれも真になる．
>
> **位相を半セルずらして «崩れるか» を見る**関門を足した．本物の格子はセル境界が
> セルの真ん中へ来て崩れるが，滑らかな絵は何も変わらない．
>
> | 検証セット 300 件 | D68 | **D69** |
> | --- | --- | --- |
> | マクロ平均 | 79.2% | **80.7%** |
> | 格子なし 正しい棄却 | 185 / 199 | **191 / 199** |
> | **D (誤答 + 誤受理)** | 6.7% | **4.0% — 初めて要件を満たした** |
>
> 比は **1.12**．検証セットだけなら 1.15 が最良だが，**実データの AI 出力の正例が
> 23 → 19 / 28 件へ落ちる** — 元絵が連続階調の «本物の格子» は合成データセットに
> 1 件も無い (種は実物のドット絵) ので，**データセットが構造的に見られない場面**である．
> 膝の手前を採って実データ枠のどこにも後退が無いことを確かめた (同梱 完全一致 58 → 59 ・
> 誤答 4 → 3 ・誤受理 2 → 1) ．
>
> **半セルずらしは «$s_*$ と $2 s_*$ を分ける» 用途では捨てた量である．** 用途が違えば
> 測り直すこと．

> [!warning] CLI の既定値が校正値と食い違っていた
> `px conform` の clap 既定値に数値を**書き写して**いたため，$\varepsilon$ が
> 0.02 対 0.2 ・$\theta$ が 0.25 対 0.35 とずれていた — **`px-calib` の数字と `px` の
> 挙動が別物**だった．`GridParams::default()` から引く形に直したので，今後は
> **既定値を書き写さないこと**．

> [!warning] テストセット 200 件には 1 度も触れていない
> 目標を満たしてから 1 度だけ使う．評価データセットの元絵は **CC0 の実物のドット絵**
> であり，これに差し替えたときに 88.8% → 67.5% と落ちた経緯がある (旧データセットの
> 数字とは比べられない) ．

## 動作確認のしかた

```sh
cd /Users/akitenkrad/Documents/workspace/rust/rs-pxforge
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --all --check

# M0 の完了条件 (実ファイルのバイト一致往復)
PXFORGE_REQUIRE_ASEPRITE_CORPUS=1 cargo test -p px-io --test aseprite_roundtrip

# 端から端まで動かしてみる
cargo run -p px-io --example degrade -- testdata/generated/sample.aseprite /tmp/big.png --scale 6
cargo run -p px -- conform /tmp/big.png /tmp/small.png
cargo run -p px -- lint /tmp/small.png
```

## 次にやること

> [!note] 課題の整理と戦略は Obsidian の [[課題分析と戦略]] にある
> 段階 1 (再構成検査の置き換え) は 4 案とも外れて変更なしで閉じ，段階 3 (信頼度) が
> D67，**段階 2 (位相ずれ検査) が D68 として当たった．**

### 0. 完了条件はあと A と B だけになった

| 区分 | 現状 | 目標 |
| --- | --- | --- |
| A (nearest ・整数倍) | 24 / 26 (92.3%) | 95% — **あと 1 件** |
| B (bilinear ・bicubic) | 36 / 50 (72.0%) | 80% — あと 4 件 |
| C (lanczos) | 6 / 25 (24.0%) | 目標値を置かない |
| **D (誤答 + 誤受理)** | **12 / 300 (4.0%)** | **5% 以下 — 満たした (D69)** |

A の 1 件は `px-calib diagnose` で個別に見れば分かる．**B が主戦場**である．

> [!warning] B は閾値では動かない — 測って確かめた
> 外している 13 件の内訳は **帯ずれ 7 ・曲線 3 ・信頼度 2 ・$\varepsilon$ 1**．
> $\theta$ を緩めると B は +2 になるが **A が −2** になる ($2 s_*$ が «閾値を満たす
> 最大の $s$» を奪う) ．$\theta$ ・$\tau$ ・比を 3 次元に掃いても，A $\ge$ 24 かつ
> D $\le$ 15 の範囲では **B は 37 / 50 が上限**だった．
>
> 許容の画素の下限は**もう «正棄却を壊す» ことはない** (棄却を引き受ける関門が 2 つ
> 増えたため) ．下限 3 px ・比 1.25 で検証セットは B 39 / 50 ・D 12 まで行く — が，
> **実データ枠が逆を向く** (完全一致 59 → 55 ・AI 出力 23 → 18 / 28) ので採らない．
> しかも下限 3 は $s \le 6$ で帯ずれの検査を無効化する (巡回距離の上限が $s/2$) ．
>
> **B を動かすには構造を変えるしかない．** 残る的は帯ずれが落としている 7 件だが，
> **帯の位相という «族» の中はすべて閉じた** — 7 件はどれも «3 本が揃って 1 本だけ
> 外れる» 形で，外れ 1 本を許すと $2 s_*$ も同じだけ得をして A が 24 → 22 ・D が
> 13 → 21 になる (中央絶対偏差も同様) ．外れ帯の位置は端 9 / 中 9 と**偶然どおり**で，
> «端を避ける» という直し方も無い．**失敗は argmin の雑音**である．

### 1. 位相の検査にはまだ 16 件ぶんの余地がある

この検査が真の $s$ を 1 度も落とさなければ完全一致 **82 / 101 ・マクロ 87.1%** である
(現状 66 / 101 ・80.7%) ．関門を順に通すと真の $s$ は $\varepsilon$ で 4 件 ・
再構成で 8 件 ・**位相で 19 件**落ちており，**律速はいまも位相の検査**である．

> [!warning] $\theta$ の掃き直しは済んでいる — **運転点は動かない**
> $\theta \in [0.25, 0.60]$ × 曲線の許容 $\in [0.08, 0.40]$ を 2 次元で掃いた．現行
> (0.35 ・0.16) が平らな面 ($\theta \in [0.34, 0.37]$) の中央で，$\theta = 0.38$ の尾根
> (+0.4 ポイント) は**量子化の境目** ($s = 8$ の帯ずれ 3 が $3/8 = 0.375$ から通る)
> でしかなく，**実データ枠は同梱 ・`local/` とも 1 件も違わない**．
>
> **$\theta \ge 0.5$ は検査を外すのと同じである** — 帯ずれは巡回距離なので $s/2$ が
> 上限で，実測の最大がちょうど 0.5000 ・超える候補は 3938 件中 0 件だった．
> $\theta$ を緩める方向はここで尽きている．
>
> $\theta = 0.32$ は D66 の D (5% 以下) をちょうど満たす唯一の点である (15 / 300) ．
> 完全一致は検証で 66 → 62 ・実データ同梱で 58 → 54 に落ちる．**D が律速になったときの
> 選択肢**として `grid-calibration.md` に記録してある．

残っている安い手は 1 つである．

- 曲線の «正規化のしかた» は 3 通りしか試していない (谷の深さ ・曲線の高さ ・画像分散)．
  谷の深さが最良だった (72.9% 対 62.4% 対 46.4%) が，その周辺は探索していない

### 2. 測る道具が速くなった — **掃引を回さずに関門を掛け替えられる**

`px-calib recon` の CSV に画像分散 ・大きさ ・位相 ・曲線の食い違いを足したので，
**CSV だけで `estimate_grid` の答えを再現できる** (新しい既定値で 66 / 185 と完全一致)．

関門の案を測るときは，まず CSV 上で選択規則を回して完全一致数を数えること — 2 秒で
1400 通り比べられる．**分離能で採否を決めない**という作法を守ったまま総当たりできる．

### 3. lint 閾値 (素材待ち)

`testdata/lint-cases/positive/` (CC0 の良質なドット絵．**`grid-eval/seeds/` がそのまま
使える見込み**) と `negative/` (自作の失敗例) が要る．**コードだけでは完結しない．**

### 4. M3 (整形と検証) へ

## 校正の道具 (`px-calib`)

```sh
# 合成データセット (元絵は CC0 の実物 64 件)．gitignore されているので作り直す
cargo run -p px-calib --release -- gen                 # 種 0 で再現する
cargo run -p px-calib --release -- sweep               # 検証 300 件を掃引
cargo run -p px-calib --release -- report              # 指標と運転点

# 測定の口 (推定の経路ではない．仮説を測ってから実装するために使う)
cargo run -p px-calib --release -- bands               # 帯ごとの再構成誤差と位相
cargo run -p px-calib --release -- confidence          # 信頼度の各項
cargo run -p px-calib --release -- recon               # 再構成統計の分離能
cargo run -p px-calib --release -- recon --include-resized   # 格子が無い件も混ぜる
cargo run -p px-calib --release -- diagnose            # 実データの誤棄却を 1 件ずつ解剖

# 実データ枠 (関門を切り替えて «どの変更が効いたか» を分けて見られる)
cargo run -p px-calib --release -- real
cargo run -p px-calib --release -- real --phase-tolerance 0.25 --phase-agreement 1.01 --allow-unmeasurable
cargo run -p px-calib --release -- real --dir testdata/grid-eval/real/local
```

- **検証セットで決め，テストセットでの再調整はしない**
- 採点の定義は掃引 CSV からやり直せる (`Row::outcome_at` は行の内容から毎回求める) ．
  **$\mathrm{min\_confidence}$ は掃引をやり直さずに掃ける** — 掃引は 0 で回している
- **リサイズありの件の正解は「棄却」である** ($\mathrm{round}(s \cdot r)$ は正解にしない)
- 選ぶ指標は**マクロ平均**．素の正解率だと「ほぼ全部棄却」が勝つ (格子なしが 2/3)
- **掃引の既定の $\varepsilon$ の格子は «絶対値» 時代のもの** (2e-4 〜 2e-2) ．
  いまの既定は «画像分散に対する割合» なので，`--epsilon` を明示しないと
  運転点が格子の外に出る

### 校正した既定値

$\varepsilon = 0.15$ (**画像分散に対する割合**) ・$\delta = 0.15$ ・$\tau = 0.05$ ・
帯 4 (**足りなければ 3 ・2 と減らす**) ・許容 $\theta = 0.35$ ・許容の下限 0 画素 ・
**曲線の食い違い 0.16** ・**半セルずらしの比 1.12** ・**測れない候補は棄却** ・
位相は整数 (副画素は既定で使わない) ・帯あたり最小セル数 2 ・
$\mathrm{min\_confidence} = 0.10$ (**$\hat{s}$ で割って使う**)

### 成績 (D66 の区分)

| 区分 | 現状 | 目標 |
| --- | --- | --- |
| A (nearest ・整数倍) | 24 / 26 (92.3%) | 95% |
| **B (bilinear ・bicubic)** | **36 / 50 (72.0%)** | 80% |
| C (lanczos) | 6 / 25 (24.0%) | 目標値を置かない |
| **D (誤答 + 誤受理)** | **12 / 300 (4.0%)** | **5% 以下 — 満たした** |

**B は 60% → 72% ・D は 6.7% → 4.0%．A は 1 件も動いていない．**

実データ枠 (別枠) は同梱 148 件が完全一致 **59** ・誤答 3 ・誤受理 1，`local/` 92 件が
完全一致 8 ・誤答 0 ・誤受理 2 である．

### 素材の状況 (格子推定の分はすべて調達済み)

| 用途 | 状態 |
| --- | --- |
| 合成データの種 | 64 件 (CC0．Kenney 16x16 が 32 ・Dungeon Crawl 32x32 が 32) ．**実データ枠とは重ならない** |
| 実データ 同梱 | 148 件 (正例 94 / 負例 54) |
| 実データ `local/` | 92 件 (正例 23 / 負例 69) ．再配布不可なので追跡しない |
| lint 正例 / 負例 | **未** |

## 落とし穴

実装中に踏んだもの．**同じ穴に落ちないこと．**

| 落とし穴 | 対処 |
| --- | --- |
| `Turn(chain)` をラン長の最小値で決める | 単発ジャギーを毎回見逃す．**G2 の曲率場から求める** (D60) |
| 単調区間を符号の反転だけで分ける | L 字の角が 1 本になる．**主軸に直交する歩みが 2 歩続いたら区切る** (D61) |
| `.hex` でアルファ 0 の色を飛ばす | 以降の添字が全部ずれる．**透明も 1 行として書く** |
| 局所格子推定で平坦な窓を投票させる | 「窓に収まる最大の $s$」が選ばれる．**信頼度 0 は投票しない** |
| `Srgb` へ変換してから成分を切り詰める | 色相がずれる．**`clip_to_gamut` で色相を保つ** |
| ディザ検出の入れ替わり率を 0.5 超に置く | ランダムディザを 1 件も拾えない (期待値が $2p(1-p) \le 0.5$) |
| 領域ラベリングを 8 連結にする | 市松が 1 領域になりディザ検出が壊れる．**4 連結** |
| 帯ごとの位相を帯の左端基準で測る | 帯ごとに違う物差しになる．**画像の原点を基準に測る** |
| 帯の実験で再構成検査を切る ($\tau = 1$) | 過大推定が野放しになる．問いは「今の検査が通した答えを選り分けられるか」である |
| 信頼度の対照群に小さい $s$ を入れる | $\bar{V}(s)$ は $s$ とともに単調に増えるのでマージンが負に潰れる (D63) |
| 校正の成績を素の正解率で選ぶ | 「ほぼ全部棄却」が 1 位になる．**マクロ平均で選ぶ** |
| 試験の一時ファイルを全試験で共有する | 稀に落ちる．**試験ごとにディレクトリを分ける** |
| **エラー文を「落ち方」と読み違える** | `real` の「信頼度が下限を下回った」は**最終的な答え**の話で，真の $s$ を何が落としたかではない．**`px-calib diagnose` で見る** |
| **落ち方を 1 つだけ数える** | 2 つの関門が同時に落としている件が先頭の関門に付け替わり，**犯人を取り違える**．`diagnose` の `failed_gates` 欄を見る |
| **関門を単独で測る** | 前の関門が落としてくれる相手まで背負わせて評価してしまう．真の $s$ が既に消えている件では，後ろの関門を何に取り替えても成績は動かない |
| **分離能の高さで統計を採る** | 均衡正解率 87.1% の統計が関門としては 15 / 101 だった．**選択規則を回して完全一致数で見る** |
| **飽和した統計に線を引こうとする** | 帯ずれは落としたい候補で $\lfloor s/2 \rfloor$ に張り付く (巡回距離の上限) ．**どこで切っても分かれない** — 統計の «形» を先に見る (D68) |
| **関門だけ変えて閾値を据え置く** | 関門を変えれば通ってくる相手が変わる．$\varepsilon$ ・$\delta$ ・$\tau$ は**組で選び直す** — 実測で +2.3 ポイントを取り逃す |
| **CLI の既定値に数値を書き写す** | 校正値と黙って食い違う (実際 $\varepsilon$ が 0.02 対 0.2) ．**`GridParams::default()` から引く** |
| 正解の無い件と「格子が無い件」を同じ `unknown` にする | 誤受理が見えない．**`no_grid` を立てて棄却が正解として採点する** |
| 「入力が拡大済みか」を周期推定や元絵復元で判定する | 短い軸では当てずっぽうになる．**$k \times k$ ブロックの一様性を厳密に見る** |
| 負例を縮小して保存する | 補間が「格子が無い」という性質を触る．**中央を切り出す**．面積は中立ではない |
| `--force-period` に実際のブロックの約数を指定する | $s$ と $2s$ が両方成立し**正解が一意に決まらない**．拒否する |
| `ingest` の連番を毎回 0 から振る | 同じ区分へ 2 回取り込むと 1 回目を黙って上書きする (**24 件消した**) |
| 旗で既定値を潰す (`--normalize-epsilon`) | 旗が無いと `false` になり**既定と違う設定で測ってしまう**．`Option<bool>` にする |
| ダウンロード素材をそのまま読ませる | `com.apple.quarantine` が付いていると EPERM で落ちる．**`xattr -cr` してから渡す** |
| **同じ «向き» の関門ばかり足す** | $\varepsilon$ ・再構成 ・帯ずれ ・曲線はすべて «セルの中が揃っているか» を見ており，滑らかな絵では全部が真になる．**格子が «在る» ことを確かめる関門が要る** (D69) |
| **捨てた量を «用途が違っても» 捨てたままにする** | 半セルずらしは «$s_*$ と $2 s_*$ を分ける» 用途で捨てたが，«滑らかな絵を落とす» 用途では効いた．**分けたい 2 群が変わったら測り直す** |
| 帯の曲線を判定と診断で別々に作る | 数字を並べて比べられなくなる．**`band_curves` 1 か所にまとめる** (作り直すと推定の費用も倍になる) |

## 決まっている作法

- 日本語は読点 `，` 句点 `．`
- 閾値には**暫定である旨を doc コメントに書く**．根拠が無い数字を黙って置かない
- 決定論性の 3 規則 (設計書 6.15) を守る．同点には必ずタイブレークを付ける
- `testdata/` は CC0 原則．**MIT は著作権表示を同梱すれば受け入れる** (例外を
  `testdata/SOURCES.md` に明記済み) ．CC-BY / CC-BY-SA / GPL は除外．
  再配布できないものは `grid-eval/real/local/` (追跡しない)
- このリポジトリの `.md` に「generated by Claude Code」フッタは付けない
  (Obsidian vault 側は付ける)
- **測ってから直す．** 設計や閾値を変える前に必ず測る — D68 では «切り方» の
  4 方向をオラクルで潰してから統計に手を付けた
- **データセットが見ていない場面を代償にしない．** 軸のまとめ方を max から平均にすると
  検証セットでは 2 件得をするが，評価データのリサイズが等方なので «片方の軸だけが
  非整数» という入力が 1 件も無い — 採らなかった
