# 引き継ぎ

セッションをまたぐときの出発点．**まずこれを読み，次に `docs/status.md` を読む．**

- 更新: 2026-08-11
- 仕様: Obsidian の `設計書/ドット絵CLI-Rust/` — `設計書.md` / `実装計画書.md` / `開発ノート.md`
- **`開発ノート.md` に「実装して初めて分かったこと」が全部入っている．** 設計書だけ読むと
  同じ穴に落ちる

## いまの状態

| 項目 | 値 |
| --- | --- |
| フェーズ | M0 ・M1 ・M1a 完了．**M2 は位相ずれ検査まで済み (75.2%)．目標は 95%** |
| テスト | 366 件すべて通過 |
| 品質 | `cargo fmt --all --check` と `cargo clippy --workspace --all-targets -- -D warnings` がクリーン |
| クレート | `px-core` / `px-io` / `px-view` / `px-macro` / `px-lint` / `px` / `px-calib` |
| ブランチ | `main` (初回コミット `3d41bfe` 済み) |

> [!warning] M2 はまだ閉じない (マクロ平均 75.2% / 目標 95%)
> 位相ずれ検査 (D62) で反比例は崩れた — 完全一致 66.3% → 85.1% と正しい棄却
> 18.6% → 65.3% が**同じ閾値の組で同時に**立つようになった．残っているのは
> $\hat{s} = 2$ への転落と，それを止めるはずの信頼度が逆向きに働いていること．
> 詳細は `docs/investigations/grid-calibration.md`．
>
> Obsidian 側は `設計書.md` ・`実装計画書.md` が変更済み，`開発ノート.md` が未追跡．

## 動作確認のしかた

```sh
cd /Users/akitenkrad/Documents/workspace/rust/rs-pxforge
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --all --check

# M0 の完了条件 (実ファイルのバイト一致往復)
PXFORGE_REQUIRE_ASEPRITE_CORPUS=1 cargo test -p px-io --test aseprite_roundtrip

# 端から端まで動かしてみる
cargo run -p px-io --example degrade -- testdata/generated/sample.aseprite /tmp/big.png --scale 6
cargo run -p px -- conform /tmp/big.png /tmp/small.png
cargo run -p px -- lint /tmp/small.png
```

## 次にやること

### 1. 信頼度の定義を測り直す

**$\hat{s} = 2$ への転落が残りの誤答の 86% を占める** (格子なしで答えを返した 69 件の
うち 59 件) ．候補が位相ずれ検査で落ちた後，「2 倍の格子」という縮退した答えへ落ちる．
滑らかな画像では $2 \times 2$ のセルがどれも平坦に見えるので，$\varepsilon$ ・
$\delta$ ・$\tau$ ・位相ずれのすべてを通ってしまう．

設計ではここを信頼度が受け持つはずだった (設計書 6.1) ．ところが**逆を向いている**．

| | 信頼度の中央値 |
| --- | --- |
| $\hat{s} = 2$ の誤答 (59 件) | 0.011 |
| 正解した件 (86 件) | 0.000 |

$\mathrm{min\_confidence}$ を 0.01 に上げると完全一致率が 85.1% → 38.6% になる．
ECE 0.482 もこれを映している．

正解の中央値が 0 なのは，対照群の最小分散が $\bar{V}(\hat{s})$ とほぼ同じでクランプに
潰れているためと見ているが，**確かめていない**．やることは式を直すことではなく，
**どの候補どうしが競っているのかを測ること**である (掃引 CSV には $\hat{s}$ の
$\bar{V}$ しか入っていないので，対照群の分散を出す口が要る) ．

### 2. 数値の校正 (1 の後)

決めるもの: $\varepsilon$ ・$\delta$ ・$\tau$ ・信頼度閾値 ・$w_L$ ・窓サイズ．
道具は揃っている．

```sh
cargo run -p px-calib --release -- gen      # 合成 500 件 (種 0 で再現する)
cargo run -p px-calib --release -- sweep    # 検証 300 件を掃引
cargo run -p px-calib --release -- report   # 指標と運転点
```

- **検証セットで決め，テストセットでの再調整はしない．** テストセットにはまだ
  1 度も触れていない
- 採点の定義は掃引 CSV からやり直せる (`Row::outcome` は行の内容から毎回求める) ．
  定義を変えても掃引はやり直さなくてよい
- **リサイズありの件の正解は「棄却」である** (整数の格子が存在しないため) ．
  $\mathrm{round}(s \cdot r)$ は正解にしない — 理由は `sweep::Outcome` の doc コメント
- 選ぶ指標は**マクロ平均**．素の正解率だと「ほぼ全部棄却」が勝つ (格子なしが 2/3)

### 3. lint 閾値 (素材待ち)

`testdata/lint-cases/positive/` (CC0 の良質なドット絵) と `negative/` (自作の失敗例) が
要る．**コードだけでは完結しない．**

### 4. M3 へ

## 落とし穴

実装中に踏んだもの．**同じ穴に落ちないこと．**

| 落とし穴 | 対処 |
| --- | --- |
| `Turn(chain)` をラン長の最小値で決める | 単発ジャギーを毎回見逃す．**G2 の曲率場から求める** (D60) |
| 単調区間を符号の反転だけで分ける | L 字の角が 1 本になる．**主軸に直交する歩みが 2 歩続いたら区切る** (D61) |
| `.hex` でアルファ 0 の色を飛ばす | 以降の添字が全部ずれる．**透明も 1 行として書く** |
| 局所格子推定で平坦な窓を投票させる | 「窓に収まる最大の $s$」が選ばれる．**信頼度 0 は投票しない** |
| `Srgb` へ変換してから成分を切り詰める | 色相がずれる．**`clip_to_gamut` で色相を保つ** |
| ディザ検出の入れ替わり率を 0.5 超に置く | ランダムディザを 1 件も拾えない (期待値が $2p(1-p) \le 0.5$) |
| 領域ラベリングを 8 連結にする | 市松が 1 領域になりディザ検出が壊れる．**4 連結** |
| 帯ごとの位相を帯の左端基準で測る | 帯ごとに違う物差しになり，本物の格子でもずれが出る．**画像の原点を基準に測る** |
| 帯の実験で再構成検査を切る ($\tau = 1$) | 過大推定が野放しになり，正解する件が nearest ばかりになる．問いは「今の検査が通した答えを選り分けられるか」である |
| 校正の成績を素の正解率で選ぶ | 「ほぼ全部棄却」が 1 位になる (格子なしの件が 2/3) ．**マクロ平均で選ぶ** |
| 試験の一時ファイルを全試験で共有する | `fs::write` が中身を空にする隙に別の試験が読み，稀に落ちる．**試験ごとにディレクトリを分ける** |

## 決まっている作法

- 日本語は読点 `，` 句点 `．`
- 閾値には**暫定である旨を doc コメントに書く**．根拠が無い数字を黙って置かない
- 決定論性の 3 規則 (設計書 6.15) を守る．同点には必ずタイブレークを付ける
- `testdata/` は CC0 原則．**MIT は著作権表示を同梱すれば受け入れる** (例外を
  `testdata/SOURCES.md` に明記済み) ．CC-BY / CC-BY-SA / GPL は除外
- このリポジトリの `.md` に「generated by Claude Code」フッタは付けない
  (Obsidian vault 側は付ける)
